# agent-runtime — scope

How agent orchestration runtimes (LangGraph, smolagents, the Claude Agent SDK itself) structure a running agent's control flow. Relevant to Wingman's own dispatch model (references/orchestration-patterns.md, commands/adaptive/boardroom.md's parallel seat dispatch).

**No `TRUTH-*.md` documents yet.** Per `research/README.md`'s evidence-gate, this domain's real
research is written only when a Wingman capability in this space is actually about to be built —
never populated ahead of that need. See `research/template-truth-doc.md` for the shape a real
entry here will follow.
