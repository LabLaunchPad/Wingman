# Eval: dogfood

Tests `plugins/wingman/commands/dogfood.md` — does its mode-detection correctly distinguish
Wingman's own dev-repo checkout (maintainer mode, where `dogfood-gap-classification` may write to
`plugins/wingman/`) from a founder's installed copy (founder mode, where it never does), and does a
real maintainer-mode run actually exercise the pipeline end to end and produce the structured
output the gap-classification skill depends on?

## Fixtures

- `evals/fixtures/setup-dogfood-simple.sh <target-dir>` — a genuinely minimal project (an internal
  status API, one route, zero conditional signals) for the simple path.
- `evals/fixtures/setup-dogfood-complex.sh <target-dir>` — reuses `setup-fetch-app.sh`'s fixture
  (Design/Data/Legal-Security/DevOps signals — 4 conditional departments) for the complex path.

## Procedure

1. **Mode detection (deterministic, no subagent needed)**: confirm `scripts/validate-structure.mjs`
   exists relative to this repo's root (maintainer mode signal) and confirm it does **not** exist
   in a fresh fixture directory generated by either `setup-dogfood-*.sh` script (founder-mode
   signal, since a fixture is a throwaway project, not a Wingman plugin checkout).
2. Run each fixture setup script.
3. Spawn a fresh subagent with only `commands/dogfood.md` (not this eval doc), instructed to run in
   maintainer mode against each fixture in turn — real pipeline stages, real `AskUserQuestion`
   answers (subagent stands in for the founder), real Boardroom dispatch, real TDD, a real
   `git push` through the actual hooks.
4. Independently verify: the structured JSON run record exists and has the right shape, the
   `gates_expected_*`/`gates_actual_*` fields match what the fixture's signals imply, and a retro
   entry was written regardless of whether a gap was found.

## Expectations

| Check | Expected |
|---|---|
| Mode detection, this repo | `scripts/validate-structure.mjs` found → maintainer mode |
| Mode detection, a fresh fixture dir | Not found → founder mode |
| Simple path run | `gates_actual_dormant` includes `management-board-activation`; `.wingman/state.json`'s `active_managers` stays `[]` throughout |
| Complex path run | Conditional department count reaches 4 (Design/Data/Legal-Security/DevOps); `management-board-activation` correctly activates once the project's active leads cross the corrected 3+ conditional threshold |
| Structured output | `evals/dogfood-runs/<timestamp>-<path>.json` exists, valid JSON, matches the schema in `commands/dogfood.md` |
| Retro entry | `docs/wingman/retros.md` gets a new `## Retro:` entry for the run, even if `observed_gaps` is empty |
| No `plugins/wingman/` writes without a gap | If `observed_gaps` is empty, `dogfood-gap-classification` is never invoked and nothing under `plugins/wingman/` changes |

## Trust level

`authored, pending first run` — the fixtures and command are written and the fixtures independently confirmed to run cleanly (see Run 0 below), but the full maintainer-mode subagent run (steps 3-4 above) has not yet executed. See the task list / `docs/wingman/retros.md` for the mandatory first real dogfood-of-the-dogfood run this plan requires before this case can be graded.

## Run log

### Run 0 — 2026-07-15 (fixture smoke test only, not the full eval)

Both `setup-dogfood-simple.sh` and `setup-dogfood-complex.sh` run cleanly and produce a real git
project (confirmed directly, not via subagent): the simple fixture has one route (`GET /jobs`) and
zero conditional signals; the complex fixture (reusing `setup-fetch-app.sh`) has all 4 conditional
signals (Design/Data/Legal-Security/DevOps) as documented. This is fixture-level smoke testing
only — the actual behavioral run (mode detection under real pipeline execution, structured output,
gap classification) is still pending and is the subject of the mandatory first real
dogfood-of-the-dogfood run tracked separately.
