## 2026-07-22 - [Check-Fixtures Async I/O]
**Learning:** Running multiple parallel processes (like 49 setup-fixture bash scripts via an async worker pool in Node.js) while making blocking synchronous file-system calls (`readFileSync`, `readdirSync`, `mkdtempSync`, `rmSync`) inside each task blocks the single-threaded event loop. This degrades parallel execution performance and process management latency. Replacing them with non-blocking async promises (`node:fs/promises` APIs) unblocks the event loop completely, optimizing signal detection and file manipulation during heavy parallel task executions.
**Action:** Always prefer promise-based async I/O (`fs/promises`) inside any concurrent or parallel process execution loop (such as child process execution pools) in Node.js to keep the event loop highly responsive.

## 2026-07-23 - [Swarm-Intelligence & Flat Skills Layout Integration]
**Learning:** During major refactoring transitions (such as flattening a directory structure), references in benchmarks and validation scripts can easily break if they rely on hardcoded paths. Building structural metrics that re-derive correctness dynamically against real on-disk files prevents documentation rot. Incorporating a Swarm-Intelligence Predictive Layer allows the AI boardroom to simulate transitive blast radiuses, dependency cascades, and specialist evolutionary requirements, preventing premature abstraction while providing rigorous foresight.
**Action:** Always cross-check historical benchmarks and structural validation scripts when performing directory flattening or restructuring refactors, and systematically introduce predictive simulation patterns (like swarm intelligence) with clean, concise, and tested multi-angle perspectives.
