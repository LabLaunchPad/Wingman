# Human To-Dos

Decisions on the Wingman project itself that only a human (not Claude) can make. This is separate from `docs/PROJECT.md`'s "Known open items," which tracks engineering status generally — this doc is specifically the subset that needs *you*, not more engineering work.

**How this list works:** an item lands here when a decision genuinely requires human judgment or external action (an account, a credential, a business call) rather than more Claude Code work. Once resolved, move it to `docs/PROJECT.md`'s decisions log with the outcome, and remove it from here.

## Open

### Credentials / environment
<!-- wingman:log type=todo category=credentials status=resolved -->
- **Checked, none needed.** Swept the repo for any `.env`, API key, or secret requirement — Wingman's plugin itself is markdown + one Node hook script, no external service integration, no runtime dependency that needs a credential. The only API-key mentions in the repo are inside eval fixture *content* (a deliberately fake, seeded test snippet in `evals/cases/full-pipeline-e2e.md`) and the security checklist text in `boardroom-security.md` — neither is a real credential this project needs you to provide. Nothing to do here unless that changes (e.g. a future specialist or department lead integrates a real third-party service).

### Publishing / release
<!-- wingman:log type=todo category=publishing status=resolved -->
- ~~This branch has never been merged~~ — **merged.** PR #1 (https://github.com/LabLaunchPad/Wingman/pull/1) and a small follow-up, PR #2 (https://github.com/LabLaunchPad/Wingman/pull/2, folded into #1's diff before merging), are both merged into `main` as of 2026-07-08, on your explicit go-ahead. Before merging: re-ran `validate-structure.mjs` fresh on the merged result (PASS), directly re-confirmed the hook-validation fix genuinely catches the original bug (simulated it again, got the expected failure, restored the real file), and checked that every behavioral file changed since its last eval verification had already been re-exercised by a later real pipeline run — nothing was merged on unverified evidence. `main` now has everything this project has built.
<!-- wingman:log type=todo category=publishing status=open -->
- **Vendor-pattern integration (v9–v12) merged 2026-07-13 as PR #12** (https://github.com/LabLaunchPad/Wingman/pull/12). This was the big founder-action item: 16 commands + 25 skills + 5 agents vendored from 6 repos, `plugin.json` bumped to `0.1.2`, and a critical `ExitPlanMode` over-block bug (found by `/wingman:audit`) fixed with a regression test. All CI green (validate/actionlint/install-smoke/version-gate) before merge. `CHANGELOG.md` and `ANNOUNCEMENT.md` (release-draft) added.
<!-- wingman:log type=todo category=credentials status=open -->
- **Gap-closure batch 1 (0.1.3) merged 2026-07-13 as PR #14** — curated founder-lens vendor-mining loop. Added `skills/memory`, `skills/research` + `commands/research.md`, `skills/founder-cfo/cmo/cro` + `commands/advisory.md`, and three safety hooks (`secret-guard` PreToolUse, `stop-loop` Stop, `prompt-guard` UserPromptSubmit) wired into `hooks.json`. New vendors (`alirezarezvani/claude-skills`, `jeremylongshore/...`, `ComposioHQ/awesome-claude-skills`, `avelikiy/great_cto`) added as reference submodules; `docs/wingman/GAPS.md` is the living gap ledger. 30 skills / 18 commands / 5 agents; 97/97 tests pass. Remaining gaps queued in GAPS.md.
<!-- wingman:log type=todo category=credentials status=open -->
- **Gap-closure batch 2 (0.1.4, gap G4) merged 2026-07-13 as PR #15** — added `hooks/secret-scanner.mjs` (`PostToolUse`, warn-only output secret-scanner) wired into `hooks.json`; `plugin.json` bumped `0.1.3` → `0.1.4`; 102/102 tests pass. Remaining gaps queued in GAPS.md.
<!-- wingman:log type=todo category=publishing status=open -->
- **Gap-closure batch 3 (0.1.5, gap G9) merged 2026-07-13 as PR #16** — added `skills/code-review` (plain-language second pass on code quality before ship; never edits code). `plugin.json` bumped `0.1.4` → `0.1.5`; 105/105 tests pass. Remaining gaps queued in GAPS.md.
<!-- wingman:log type=todo category=publishing status=open -->
- **Gap-closure batch 4 (0.1.6, gap G10) merged 2026-07-13 as PR #17** — added `skills/simplify` (post-change tidy-up pass; renders a plan, never edits code silently). `plugin.json` bumped `0.1.5` → `0.1.6`; 108/108 tests pass. Remaining gaps queued in GAPS.md.
<!-- wingman:log type=todo category=publishing status=open -->
- **Gap-closure batch 5 (0.1.7, gap G11) merged 2026-07-13 as PR #18** — added `skills/incident-response` + `commands/incident.md` (`/wingman:incident`: calm ordered runbook; stabilize before debugging; rotates exposed keys). `plugin.json` bumped `0.1.6` → `0.1.7` (33 skills, 19 commands); 112/112 tests pass.
<!-- wingman:log type=todo category=credentials status=open -->
- **Gap-closure batch 6 (0.1.8, gap G12) merged 2026-07-13 as PR #19** — added `references/secrets-policy.md` + `references/persona-template.md` (cross-cutting: secrets handling + advisor scaffold). **This closes the entire original G1–G12 gap ledger** — all gaps shipped across batches 1–6. `plugin.json` bumped `0.1.7` → `0.1.8`; 114/114 tests pass. Future gaps, if any, start a new G13+ ledger in GAPS.md.
<!-- wingman:log type=todo category=publishing status=open -->
- **Marketplace listing content.** `.claude-plugin/marketplace.json` and `plugin.json` are structurally complete, but nothing has been submitted anywhere yet (Anthropic's official plugin marketplace, or wherever you intend to list this). That's an account/submission action only you can take, and it may have its own content requirements beyond what's in this repo. As of 2026-07-13, `CHANGELOG.md` and `ANNOUNCEMENT.md` (release-draft) were added to support submission; the content side is ready, the submit is still your account action.
<!-- wingman:log type=todo category=publishing status=open -->
- **Repo visibility and settings.** Confirm `LabLaunchPad/Wingman`'s visibility (public/private) matches intent before any wider release, and decide on branch protection for `main` if multiple people will push to this repo later.
<!-- wingman:log type=todo category=publishing status=resolved -->
- **Branch protection for `main` — SET via API 2026-07-13 (corrected).** Earlier noted as "404 / needs web UI" — that was a misread: the first attempt returned 404 only because of a malformed request body, not a permission denial. A correctly-formed `gh api` PUT succeeded. `main` now requires status checks `validate`, `actionlit`, `install-smoke`, `version-gate` (strict, up-to-date) with `enforce_admins: true`; `allow_force_pushes`/`allow_deletions` disabled. Direct pushes to `main` are now blocked — use PR + squash-merge (which is how all gap-closure batches landed). Moved to Resolved.
  - **Required checks**: `validate`, `actionlint`, `install-smoke`, `version-gate`.
<!-- wingman:log type=todo category=credentials status=open -->
- **Add the `ANTHROPIC_API_KEY` repo secret to enable the two optional CI features (2026-07-10).** The new `.github/workflows/evals.yml` (weekly + on-demand headless eval runs) and the `@claude` workflows (`claude.yml` for `@claude` mentions, `claude-code-review.yml` for auto PR review) both read `secrets.ANTHROPIC_API_KEY` — a single secret serves all three. **Settings → Secrets and variables → Actions → New repository secret**, name `ANTHROPIC_API_KEY`. Until it's set, those workflows degrade gracefully (the eval runner falls back to a keyless dry-run integrity check; the `@claude` jobs simply have no token to run). There is real per-run token cost once enabled — the eval workflow is deliberately weekly/dispatch-only, never per-PR, to keep it bounded. **2026-07-13 status: still pending — the token hasn't been provided. Once you paste it, run `gh secret set ANTHROPIC_API_KEY` (in the repo root) and the secret will be created; the workflows then grade for real.**

### Demo content — genuinely can't be produced from this sandbox
<!-- wingman:log type=todo category=demo-content status=open -->
- **Screenshots or a short demo (GIF/video) of Wingman actually running.** The repo currently has zero visual content anywhere — `README.md` is text-only. Every "real" run in this project's evals happened against throwaway fixtures with sandbox workarounds (simulated `AskUserQuestion` answers, general-purpose agents standing in for named personas) — none of it is footage of the real, installed plugin running in a real Claude Code session against a real project, which is what a screenshot/demo needs to show honestly. See `docs/DEMO-CHECKLIST.md` for exactly what's worth capturing and in what order, so this doesn't require guessing.
<!-- wingman:log type=todo category=demo-content status=resolved -->
- ~~`README.md` is stale relative to actual status~~ — refreshed to reflect actual build/eval status and link to `PROJECT.md`/`HUMAN-TODOS.md`/`evals/README.md`. The demo content above still needs a real session — text alone can't substitute for it.

### Legal / compliance sanity check
<!-- wingman:log type=todo category=legal status=open -->
- **Independent review of the attribution work before any public release.** `ATTRIBUTIONS.md` documents provenance for 16 vendored repos. A self-check pass just found and fixed a real error: `andrej-karpathy-skills` was documented across `ATTRIBUTIONS.md`, `docs/ARCHITECTURE.md`, and `engineering-minimalism/SKILL.md`'s own header comment as having "no LICENSE file," when it actually declares MIT in three places (`plugin.json`, `README.md`, and the `SKILL.md` frontmatter) — just not as a standalone `LICENSE` file. Corrected everywhere it appeared. This didn't create any real exposure (the content was always restated in Wingman's own words, never quoted, regardless of the license status), but it's exactly the kind of error worth a genuinely independent human/legal pass catching before public release, since I did this attribution work myself and just found my own mistake in it. **2026-07-13 status:** `ATTRIBUTIONS.md` remains comprehensive (16 vendor repos in the index + per-file provenance records); a fresh self-review of the v9–v12 additions confirmed every new skill/command source is cited. Still recommend the independent human/legal pass before any submitted/public release, per the original note.

### Carried over from before
<!-- wingman:log type=todo category=commit-signing status=open -->
- **Commit signing.** This repo's commit history shows as "Unverified" on GitHub (no GPG/SSH signature — author identity itself is correct, this is purely a signature-badge issue). Decide whether to set up commit signing for this repo, and if so, which method (GPG vs. SSH signing) — this requires a key you control, so it isn't something Claude Code can set up unilaterally.
<!-- wingman:log type=todo category=environment status=open -->
- **`check-fixtures.mjs` fails on Windows (environmental, not a code bug).** It shells out to `/bin/bash` for `evals/fixtures/*.sh`; on a Windows checkout without a working bash-in-`PATH` that path resolution fails even though the fixture file exists. This is pre-existing and **not part of CI** (CI runs only `validate`/`actionlint`/`install-smoke`/`version-gate`). Run it on a machine with bash (Linux/macOS/ Git-Bash-on-PATH) if you want a green Layer-1 check there; don't treat the Windows failure as a regression.
<!-- wingman:log type=todo category=publishing status=open -->
- **Real dogfooding: install and a first real `/wingman:plan` run are now proven; full pipeline execution with a real founder in the loop still isn't.** A real `claude` CLI was used to genuinely `plugin marketplace add` + `plugin install` Wingman, then actually run `/wingman:plan` against a live throwaway test project ("TaskFlow") — see `docs/PROJECT.md` decisions log for the two real bugs this found and fixed (manifest install-breaking issues, then a checkpoint-persistence + hook-enforcement gap). Confirmed for real: the plugin installs and shows `enabled`; a fresh session lists all 13 `wingman:*` commands, 5 `boardroom-*` agents, and 10 skills; the `boardroom-checkpoint` hook registers (`--debug` log: "Registered 1 hooks from 1 plugins") and now correctly enforces the founder's actual decision, not just the bottom line; a real `/wingman:plan` run genuinely asked clarifying questions, created a real `dept-product` lead, wrote a real plan, and dispatched a real 5-seat Boardroom review that came back `GO WITH CHANGES` with genuine per-seat findings. Not yet exercised: a real founder actually answering `AskUserQuestion` boardroom checkpoints interactively (confirmed that tool is entirely unavailable in headless `claude -p` sessions — this was tested via `-p` only, since that's what's driveable from this environment; a true interactive session, which is how a real founder would actually use this, still hasn't been tried), and the rest of the pipeline (`/wingman:build`/`secure`/`ship`) against this same real project. That's the next step — needs either a real interactive terminal session, or continuing the headless run further (`/wingman:build`) to see how much of the rest of the pipeline holds up.

## Founder runbook — remaining actions (Claude prepared, you execute)
Claude closed all G1–G12 code gaps and set branch protection via API. The rest need your accounts/credentials/presence and cannot be done from this sandbox:

<!-- wingman:log type=todo category=credentials status=open -->
1. **`ANTHROPIC_API_KEY` repo secret** — enables real eval grading + `@claude` PR review. Run locally (do **NOT** paste the token in chat — it would live in the transcript):
   `cd "<repo root>" && gh secret set ANTHROPIC_API_KEY`  (paste the key at the prompt)
   Confirm: `gh secret list` should show `ANTHROPIC_API_KEY`.
<!-- wingman:log type=todo category=demo-content status=open -->
2. **Demo footage** — record a real installed session (see `docs/DEMO-CHECKLIST.md`). Needs an interactive terminal; the sandbox can't produce video.
<!-- wingman:log type=todo category=legal status=open -->
3. **Independent legal pass** — have a human/legal reviewer confirm `ATTRIBUTIONS.md` (16 pinned vendor submodules, plus 5 non-vendored/browsed-only design-inspiration credits in its own table). Self-review done; external pass still recommended before public release.
<!-- wingman:log type=todo category=publishing status=open -->
4. **Marketplace submission** — `.claude-plugin/marketplace.json` (repo root) + `plugins/wingman/.claude-plugin/plugin.json` are structurally complete and committed. Submit via your Anthropic/marketplace account (account action only you can take).
<!-- wingman:log type=todo category=publishing status=open -->
5. **Restore the `version-gate` hard gate (optional but recommended).** Branch protection currently requires only `validate`/`actionlint`/`install-smoke`. `version-gate` still runs as a *soft* gate on plugin-content PRs (fails if shipped content changes without a `plugin.json` version bump) but isn't required, so it can be overridden. To make it a hard gate again, the `version-gate.yml` `paths:` filter must be removed and a "skip if no plugin content changed" guard added (so it produces a context on every PR and passes for docs/CI PRs). **This requires the `workflow` OAuth scope**, which the `LabLaunchPad` gh token lacks — so apply it via the GitHub web UI (edit `.github/workflows/version-gate.yml` directly) or with a token that has `workflow` scope, then re-add `version-gate` to the required checks in Settings → Branches. (Claude drafted the exact change but couldn't push it: "refusing … without `workflow` scope".)

## Resolved

<!-- wingman:log type=todo category=publishing status=resolved -->
- **Branch protection for `main` set via API 2026-07-13** (corrected earlier 404 misread — the first attempt failed only because of a malformed request body, not a permission denial; a correctly-formed `gh api` PUT succeeded). Requires **`validate`/`actionlint`/`install-smoke`** (strict); direct pushes blocked, PR + squash-merge remains the path. **`version-gate` is intentionally NOT a required check** (see Open item below) — the `LabLaunchPad` gh token lacks the `workflow` OAuth scope, so the always-run fix can't be pushed via this token, and requiring it would deadlock any non-plugin PR (its context would never appear).
<!-- wingman:log type=todo category=hooks status=resolved -->
- **Confirm the plan-mode safety hook actually fires in a real install.** Resolved by the real-install session above: Claude Code's own `--debug` log shows `Read hooks.json for plugin wingman (enabled=true)` and `Registered 1 hooks from 1 plugins` for the exact installed plugin — the `PreToolUse`/`ExitPlanMode` hook wiring is genuinely picked up, not just correct in isolation. (Directly triggering `ExitPlanMode` itself wasn't possible headlessly — it's a UI-only tool not exposed to `claude -p` — but hook *registration*, which is what this item was about, is now directly confirmed.)
