# Wingman — Project Status

Living document. Update this alongside any change that affects status, not as a separate afterthought — stale status here is worse than no status doc at all.

## Current state (as of this writing)

**Live counts (run `node scripts/wingman-health.mjs` for the current numbers, not the bullets below):** the command/seat/skill counts in this section are a frozen snapshot from PR #12 (pre-MVP1/MVP2 rearchitecture, 2026-07-13) and are known stale. A hand-copied "actual current state" figure was kept here and drifted at least twice (most recently caught 2026-07-20, off by one command/skill each after `knowledge-export`/`prompt-diff-check` shipped) — removed in favor of pointing at the script directly, since a number restated in prose next to a "don't trust this" warning keeps getting trusted anyway. See the Decisions log below (v13 onward) for what actually changed and why; the PR #12 bullets below are kept as historical record of what that PR shipped, not updated in place.

**Built and merged to `main`** (PR #1 + PR #2, 2026-07-08 — `claude/init-6eg3d1` is no longer the live branch; future work starts fresh from `main`). **PR #12 (2026-07-13)** merged the full v8–v12 vendor-pattern integration (`plugin.json` → `0.1.2`), 85/85 tests passing, and a critical `ExitPlanMode` over-block bug found by `/wingman:audit` and fixed with a regression test — see the decisions log and `CHANGELOG.md`.
- Marketplace + plugin scaffold (`.claude-plugin/marketplace.json`, `plugins/wingman/.claude-plugin/plugin.json`)
  - 16 commands: `plan`, `build`, `secure`, `ship`, `boardroom`, `retro`, `learn`, `evolve`, `harness`, `telemetry`, `launch`, `hotfix`, `audit`, `over-engineering-review`, `bloat-audit`, `debt-ledger`
- 5 Boardroom seats: `boardroom-founder`, `boardroom-engineer`, `boardroom-security`, `boardroom-design`, `boardroom-cost`
  - 25 skills (full list in `plugins/wingman/.claude-plugin/plugin.json`): the original 10 — `plain-language-checkpoint`, `verification-before-completion`, `writing-plans`, `systematic-debugging`, `systematic-auditing`, `design-taste`, `engineering-minimalism`, `token-economy`, `department-lead-activation`, `evolve-promotion` — plus `ponytail-debt-harvesting`, `platform-native-reference`, `anti-rationalization`, `doubt-driven-development`, `interview-one-question-at-a-time`, `evidence-gated-catalog`, `test-driven-development`, `subagent-driven-development`, `verification-loop`, `council`, `spec-handler`, `definition-of-done`, `security-checklist`, `testing-patterns`, `doc-index`
- `hooks/hooks.json` + `boardroom-checkpoint.mjs` — the `ExitPlanMode` gate enforcing a recorded Boardroom verdict before plan mode can exit. Registered under `PreToolUse` (fixed from an invalid `PermissionRequest` event name — see decisions log; the gate had never actually fired before this).
- `.wingman/checkpoints.jsonl` + `state.json` writer, wired into `/wingman:boardroom` (flat files; no server — see `docs/status/DATABASE.md`). `state.json` now also tracks `active_specialists`, alongside `active_department_leads`.
- `plugins/wingman/scripts/validate-structure.mjs` — mechanical structural validator (see `docs/status/SRS.md` NFR-6)
- `.github/workflows/` — a 7-workflow CI/CD suite engineered from the vendor survey (see decisions log): `validate` (the three mechanical validators + `check-fixtures.mjs`), `actionlint` (lints the workflows themselves), `install-smoke` (a real `claude plugin install` — blocking), `version-gate` (plugin.json bump enforcement), `evals` (weekly/on-demand headless behavioral tier), and `claude`/`claude-review` (the gated official `@claude` Action). Plus `scripts/check-fixtures.mjs` and `evals/run-headless.mjs`.
- `docs/status/ARCHITECTURE.md`, `docs/roadmap/AGENT-ROSTER.md`, `docs/status/PRD.md`, `docs/status/SRS.md`, `docs/status/DATABASE.md`, this file, `ATTRIBUTIONS.md`, `LICENSE`
- **v2, department-lead activation**: `skills/governance/department-lead-activation` — the shared signal-check-and-create mechanism, wired into `plan`/`build`/`secure`/`ship`. Writes department-lead files to the *founder's own project* (`.claude/agents/dept-*.md`), never into Wingman's own plugin directory.
- **v3, evolve specialist-promotion**: `skills/governance/evolve-promotion` — gathers signal from `LEARNINGS.md`, `docs/history/retros.md` (now a canonical single file — see decisions log), and `.wingman/checkpoints.jsonl`; requires 2+ genuine occurrences; writes every promoted artifact (command/skill/agent) into the founder's own `.claude/`, never Wingman's plugin directory. Bundles its own runtime copy of the specialist catalog (`references/specialist-catalog.md`) since `docs/roadmap/AGENT-ROSTER.md` isn't part of what ships to an installed plugin — see decisions log.
- **`commands/adaptive/launch.md`** — the `dept-growth` delegating command; extended `boardroom.md` to accept content passed directly by a calling command (not just a plan file or diff), since launch copy is neither.
- **`commands/adaptive/hotfix.md`** — the production error-correction loop, delegating root-causing to `systematic-debugging` by phase name and reusing `build.md`'s test-first discipline for the fix.
- **`evals/`** — a lightweight behavioral eval harness (fixture scripts + case doc + run log), scoped down from the Tier 1/2/3 pattern in `addyosmani-agent-skills`. `cases/department-lead-activation.md` and `cases/evolve-promotion.md` are `verified` (positive + negative cases each, independently checked against real file trees rather than the tested agent's self-report). `evolve-promotion`'s negative case specifically confirmed the skill doesn't mistake "same incident logged twice" (a learning entry plus its own checkpoint/retro) for two genuine occurrences.
- **`cases/full-pipeline-e2e.md`** (`verified`) — the full `plan → build → secure → ship` pipeline run against a continuous realistic project, three times now, each stage a fresh subagent recovering context purely from the fixture's committed git history and `.wingman/` state — no shared conversational memory across stages, closer to how a real founder works across separate sessions than one long conversation. **Run 1** (`evals/fixtures/setup-waitlist-app.sh`, a fresh zero-dependency Node.js waitlist service): all 4 stages passed; found and fixed one real process defect (the build-stage Boardroom checkpoint was never actually recorded despite all 5 seats genuinely reporting — `boardroom.md` now requires re-reading both `.wingman/` files from disk before reporting a checkpoint done). **Run 2** (`evals/fixtures/setup-ledger-fixture.sh`, a mid-life project with real prior history and two deliberately seeded adversarial elements): all 4 stages passed with genuine, real 5-seat dispatch throughout (no synthetic verdicts) — including a real fix-recheck loop at plan stage, an independently-reconfirmed real Cost-seat finding at build stage, the new `founder-todos.md` convention's first real exercise at secure stage, and a self-caught, honestly-disclosed process defect at ship stage (a feature branch created after its commits already landed, not before). Neither run ever produced a genuine `NO_GO` via real dispatch — both times because good engineering judgment prevented the seeded problem rather than needing the Boardroom to catch it after the fact — so a **Run 3** was added with an *already-committed* adversarial diff (SQL injection + plaintext passwords) handed over as "ship it," which finally produced the first real-independent-dispatch `DO NOT SHIP` (4/5 seats `NO_GO`, `next_stage` pinned), promoting this case to `verified` (2026-07-08). `evals/cases/boardroom-gate-rule.md` separately covers the `DO NOT SHIP` file-writing mechanics with real verified writes, just via synthetic verdicts. A post-ship `evolve-promotion` integration check on Run 2 correctly found insufficient signal and proposed nothing (the seeded rollback-incident precedent was applied correctly rather than repeated, so no genuine second occurrence existed to find) — a correct negative result, not a gap. Two testing-environment limitations recur across both runs, documented as such rather than treated as Wingman bugs: named custom `subagent_type` dispatch isn't available in this sandbox (general-purpose-agent-with-persona-as-prompt substitution used instead), and neither fixture has real GitHub remote/auth (PR descriptions written to a file instead of opened via API).
- **v5, systematic-auditing**: `skills/discipline/systematic-auditing` + `commands/adaptive/audit.md` — codifies the multi-angle-parallel-review pattern this project itself was built with (scope into distinct concerns, dispatch one narrowly-file-scoped subagent per concern in parallel, never trust a finding without independently verifying it against the real filesystem/execution) as a standing, on-demand mechanism, triggered either by `/wingman:audit` or by natural-language phrasing ("audit this," "evidence-driven," "test-driven," "production-grade," "find all the gaps"). Not a 9th department — a cross-cutting process skill, paired with `systematic-debugging`. Wired into `secure.md` for founder-requested deep scrutiny. Also introduces `docs/wingman/founder-todos.md` (per-project, mirrors `retros.md`'s convention): `secure.md`'s founder-accepted-risk step now appends there, giving founders one scannable list of business-risk decisions only they can make.
- `docs/HUMAN-TODOS.md` — a maintained, dedicated snapshot of decisions on the Wingman project itself that need a human, not more Claude Code work (currently: commit signing, confirming the hook fix fires in a real install) — surfaced separately from `docs/status/PROJECT.md`'s broader "Known open items" so it's easy to scan for exactly what's blocked on you.
- Three new eval cases for previously-untested, high-value files: `cases/systematic-auditing.md` (the brand-new audit skill's first real test — found all 3 seeded issues plus 2 legitimate bonus ones, zero false positives), `cases/verification-before-completion.md` (the project's single most load-bearing discipline skill, never directly tested before — correctly caught a regression a naive code-read would have missed), `cases/retro.md` (zero prior exercise — produced a specific, appropriately short, well-reasoned retro). All `provisional`, one scenario each.
- 16 vendor repos as git submodules under `vendor/`, each researched and mined for specific transferable patterns (see `docs/status/ARCHITECTURE.md` §9)
- **v8–v12, vendor-pattern integration (merged PR #12, 2026-07-13, `plugin.json` → `0.1.2`)**: completed the six-vendor-pattern build-out — ponytail minimalism/debt tooling + `session-start.mjs` hook (v8); the gstack `EXIT PLAN MODE GATE` section-enforcement + gsd-plugin threat register in `secure.md` (v9); the `doc-index` discipline skill (v10); the 6 missing behavioral eval cases bringing the suite to 25 (v11); and a maintenance pass that rephrased 5 skill descriptions to `Use when…`, added the missing `## Verification` section, and drove `validate-structure.mjs` to **zero warnings** (v12). A `systematic-auditing` pass on the merged result found a critical defect — `boardroom-checkpoint.mjs` was over-blocking every `ExitPlanMode` by validating required sections against *every* source including inline plan text — fixed so each source is judged independently and an approved checkpoint requires the marker + `ship it` + all 7 sections, with a regression test. All **85/85** unit tests pass; `validate-structure` and `check-repo-consistency` both PASS. Release notes in `CHANGELOG.md`; draft announcement in `ANNOUNCEMENT.md`.

**Not yet built:**
- The MCP state-store server documented in `docs/status/DATABASE.md` (deliberately deferred — flat files cover the current need)
- Any specialist from the roster (none should exist yet — this is expected, not a gap)

**Known open items** (see also `docs/HUMAN-TODOS.md` for the subset that specifically needs a human, not more engineering):
- ~~Safety-gate robustness (`boardroom-checkpoint.mjs`)~~ — **fixed 2026-07-10** (see decisions log). The gate now evaluates both the inline `plan` text and the most-recent plan file, opens only if one of them is a fully approved "ship it" checkpoint (marker + no "DO NOT SHIP" + `Founder decision: ship it`, all in the same source), and applies a global "DO NOT SHIP" veto across both sources. The false-deny is gone, the gate is now *stricter* on explicit rejections, and the header comment matches the code. Verified across all original paths plus adversarial cross-source cases (inline rejection vs. stale file approval, and vice versa).
- ~~`boardroom-checkpoint.mjs` `ExitPlanMode` over-block (found by `/wingman:audit`, 2026-07-13)~~ — **fixed**. The gate validated required report sections against *every* source (including inline plan text with no section headers), denying *every* `ExitPlanMode`. Now each source is judged independently; an approved checkpoint requires the marker **+** `ship it` **+** all 7 sections; regression test added in `tests/hooks-integration/hooks-integration.test.mjs`.
- The commit history shows as "Unverified" on GitHub (missing GPG/SSH signature — identity itself is correct). Unresolved by design pending the founder's decision on whether to set up commit signing.
<!-- wingman:log type=todo category=eval status=open -->
- **Watch item: log the first `memory/` read-loop occurrence when it actually happens.** `docs/status/DATABASE.md`'s `memory/` section names a real, still-open gap: `evals/cases/memory.md`'s write path is `verified`, but no run has ever proven a *later* session actually reads `memory/*.md` back and changes its behavior as a result — it's write-verified, not read-loop-verified. Checked both `LEARNINGS.md` and `docs/history/retros.md` (2026-07-18): **zero occurrences** logged under any memory-related category — `query-wingman-knowledge.mjs --recurring`'s category list has no "memory" entry at all. Per `docs/roadmap/AGENT-ROSTER.md`'s own promotion rule (2+ genuine occurrences before building anything), there's nothing to build yet, and manufacturing an occurrence to hit the threshold would defeat the point of the rule. Actionable now: the **next time** a real session (pipeline stage, dogfood run, or founder session) genuinely needs `memory/*.md` read back — and either succeeds in reading it and demonstrably changes behavior as a result, or silently fails to check it when it should have — log it as `<!-- wingman:log type=learning category=memory status=open occurrence=1 -->` in `LEARNINGS.md`, following the existing occurrence-tracking format (see the `hooks`/`pipeline` entries there for the pattern: concrete evidence, not a hypothetical). A second, differently-shaped occurrence after that would clear the evidence-gate for `/wingman:evolve` to actually propose something.
  - **Eval suite count is stale here by construction — this paragraph describes v11-era state (25 cases) and was never updated as the suite grew; run `node scripts/wingman-health.mjs` for the live count (70 cases as of 2026-07-22, 54 `verified`/16 `provisional` — this exact snapshot number will itself go stale again as the suite keeps growing, which is precisely why the tool, not this prose, is the thing to trust) rather than trusting a hand-maintained number in prose, per this doc's own drift problem. Historical detail retained below for the v11 authoring record.** Includes `full-pipeline-e2e`, promoted after a Run 3 adversarial fixture finally produced the one thing it was missing: a genuine `DO NOT SHIP` from real independent 5-seat dispatch rejecting a real bad diff (SQL injection + plaintext passwords). Also includes `harness.md`, closed via a real self-audit of Wingman's own repo (see decisions log) — CI is now wired up (`.github/workflows/validate.yml`).
  - Dedicated eval cases for `learn.md`/`telemetry.md` and the 4 discipline skills (`plain-language-checkpoint`, `design-taste`, `engineering-minimalism`, `token-economy`) were **authored in v11** (`evals/cases/{learn,telemetry,plain-language-checkpoint,design-taste,engineering-minimalism,token-economy}.md`) — each to the verified-case format with a pending-grading run log. Behavioral grading needs `ANTHROPIC_API_KEY` + `/bin/bash` (unavailable in the authoring environment); the deterministic `check-fixtures.mjs` gate still validates them on every PR. Eval suite is now **25 cases** (the 19 prior + 6 authored).
- `docs/wingman/founder-todos.md`'s convention was behaviorally exercised for the first time in `full-pipeline-e2e` Run 2's secure stage (PASS) — not pursuing a separate dedicated eval, same reasoning as the Boardroom personas.

## Decisions log

Durable decisions only — not every turn-level choice. Newest first.

<!-- wingman:log type=decision category=documentation status=resolved -->
- **Full 19-layer validation pass against a founder-supplied validation framework found `docs/status/SRS.md` materially stale; rewrote it from the real tree, and closed a suspected data-risk gap by checking rather than building.** The framework's Layer 10 (SRS) check caught four real defects, each verified by direct inspection rather than assumed: (1) **FR-1/FR-2 specified `/wingman:plan`** — a command that no longer exists (`ls commands/*/` confirms); (2) **FR-8 specified `/wingman:secure`** — deleted, folded into `build.md`'s Definition-of-Done gate; (3) the **Interfaces section documented the hook contract as `PermissionRequest`/`ExitPlanMode`** — but `PermissionRequest` is a **known-fake event** that once left the plan-mode safety gate *silently inert*, a bug already fixed in `hooks.json` and mechanically guarded by `validate-structure.mjs`'s `VALID_HOOK_EVENTS`; the SRS had been enshrining the bug as its specification. (4) the **Verification section claimed behavioral requirements were "verified by manual walkthrough"** and described the eval harness as a future addition, when 90 eval cases and 484 tests already existed. It was also missing 5 of the 9 sections the framework requires (assumptions, dependencies, quality attributes, acceptance criteria, traceability). Rewrote the whole document: 20 functional requirements re-derived from the real 34-command/8-seat/46-skill/17-engine surface, all 6 *real* hook events named, the 12 real traceability prefixes listed, and every count verified against the tree before writing (34 = 14 pipeline + 20 adaptive, 8 agents, 46 skills, 17 engines, 12 prefixes, `schema_version: 5` — all confirmed by direct `ls`/`grep`, not memory). Deliberately **did not** claim two things the check suggested but inspection disproved: FR-12's `ARCHITECTURE.md §9` citation is **correct** (the description-trap finding really is in §9, line 861), and FR-20's MCP-server "(Planned)" status is **consistent** with `DATABASE.md` — flagging either as stale would have been a false finding. Separately, the framework's Layer 19 flagged **backup/recovery** as a possible medium-to-high data risk; checked `.gitignore` directly and found **no `.wingman/` entry**, so founder project state is committed to git by default — **git already is the backup mechanism**. Recorded as assumption AS-3 in the new SRS rather than building a backup system for a problem that doesn't exist. Two layers were reported as failing (Layer 2 business intent, Layer 10 SRS); this closes Layer 10. Layer 2 stays open as a genuine founder decision (no revenue/monetisation content exists anywhere in `docs/status/` or `README.md`, and `LICENSE` is MIT — plausibly deliberate, but nowhere recorded either way). Dev-repo-only: no `plugins/wingman/` change, so no version bump.

<!-- wingman:log type=decision category=publishing status=resolved -->
- **Post-9-PR-build sweep of `docs/HUMAN-TODOS.md`: closed a real engineering task the doc had wrongly filed as human-only, plus fixed 7 stale marker/status mismatches.** Asked to work through the remaining HUMAN-TODOS items; before touching any, distinguished genuine human-only items (credentials, account actions, business decisions — none of which changed) from one that turned out to be **actionable after all**: the "restore the `version-gate` hard gate" item was blocked in a 2026-07-13 note on the `LabLaunchPad` gh token lacking the `workflow` OAuth scope. Re-checked the premise directly rather than trusting the stale note — this session's own git push had *just* succeeded in creating an entirely new workflow file (`.github/workflows/benchmark-schedule.yml`, PR9) and the installed GitHub App's permission list (surfaced via a routine PR search) explicitly showed `workflows: write`. Grounded, not assumed: pushed the actual fix (`.github/workflows/version-gate.yml` — removed the `paths:` filter, added a `git diff`-based skip-guard so a docs/test-only PR gets a passing context instead of none at all), and verified the shell logic directly against two real commit ranges from this repo's own history (a plugin-content-changing range correctly reports `changed=true`; a no-op range correctly reports `changed=false`) before considering it done. **What's left is genuinely the founder's**: adding `version-gate` to the required checks in Settings → Branches — a repo-settings action no `git push` can perform. Separately, spot-checked 2 of 7 stale `status=open` markers sitting on entries whose own prose already said "merged" (PR #12, #19 — confirmed `merged: true` via a direct GitHub API call, not trusted from the doc's own claim) and, on that basis, flipped all 7 matching entries to `status=resolved` — a marker-hygiene fix, not a re-litigation of already-settled work. Everything else in the doc (Figma/voice credentials, `ANTHROPIC_API_KEY`, Codex CLI model-provider credentials, demo footage, the independent legal pass, marketplace submission, repo visibility) remains genuinely open and was left untouched — none of it is something a coding session can substitute a human for.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **PR 9 of the AI Engineering Operating System build — the final PR: multimodal vision ingest + continuous benchmarking.** `/wingman:discovery`'s Discovery.1 step now folds real images/screenshots into its findings — read directly via the `Read` tool (no new dependency), cited specifically (a labeled button, a stated price, a visible error state) rather than described from a general impression, and turned into `DISC-*` rows the same as any other evidence. Figma and voice were assessed and **deliberately not built**: both need a real external service (a Figma API token; a speech-to-text provider, local or paid) this zero-dependency plugin doesn't ship with, and a stub/mock would be worse than the honest gap — indistinguishable from working until a founder tries it for real. Documented as credential-gated adapter contracts in `docs/HUMAN-TODOS.md`, naming exactly what each needs from the founder, not silently dropped. **Continuous benchmarking was explicitly re-scoped before building anything**, because the repo's own decisions log had *twice already declined* a service-style MLOps benchmark (p95 latency, throughput, cache-hit-rate) since Wingman has no persistent runtime to instrument — asked directly, confirmed the intent was to automate the *existing* agent-weakness coverage benchmark on a schedule, not introduce a new metric type. Built `scripts/check-benchmark-regression.mjs` (pure `checkRegression()` + a thin CLI, same wiring/logic split as `constitution-check.mjs`) + `docs/status/benchmark-baseline.json` (the recorded floor: 92% covered, 92% measured) + a weekly `.github/workflows/benchmark-schedule.yml`, matching `evals.yml`'s own scheduled-tier pattern. `--update-baseline` can only raise the floor, never lower it to silently make a regression disappear — verified directly: raising the baseline above the real current score correctly fails with the specific %/% gap named, restoring it passes clean again. **This completes all 9 PRs of the AI Engineering Operating System build** — Traceability chain, Constitution/Governance/Risk/Acceptance-Criteria, WKOS, Context Engine, Memory Engine, Research Gate, the 20-layer Engine surface + Evaluation Layer, Code Intelligence/Tool Runtime/Knowledge searchability, and this final phase. v0.7.37.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **PR 8 of the AI Engineering Operating System build: the 3 remaining engines — Code Intelligence, Tool Runtime, Knowledge searchability.** Closes the 3 gaps PR7 honestly disclosed as not-yet-built rather than faked. **Code Intelligence** ships `skills/codebase-comprehension` — reads a project's existing structure and cites the specific established pattern being reused before generating new code, wired into `commands/pipeline/build.md`'s Before-starting step immediately after `package-manager-selection`, directly answering a real gap named from prior dogfood runs (a parallel utility generated next to one that already did the same job). **Tool Runtime** ships `references/tool-runtime.md` — a declarative intent→tool map (read/write/git-push/GitHub/browser/DB/cloud/docs) composed with `references/permission-model.md`'s existing Level 0-4 scale, never a second one — plus `skills/tool-selection` applying it at a genuinely ambiguous tool-choice moment; it names the required permission level, it does not itself enforce (the existing hooks still do). **Knowledge searchability** ships `scripts/knowledge-index.mjs` (+ `scripts/engines-check.mjs`-style pure/CLI split): builds an in-memory keyword index over `references/*.md` (title/heading/path scoring, no persisted index file, no embeddings) — same zero-dependency shape as `scripts/query-founder-knowledge.mjs`. Deliberately keyword-matching, not semantic: `install-smoke.yml` asserts `node_modules` never appears, so an embedding dependency would break a CI-enforced invariant, and the semantic variant already exists in `agnostic-boardroom/` (LanceDB + FastEmbed) — this engine's scope stops at a real, working, dependency-free search over a 22-doc catalog small enough that a scoring model adds no real value over honest keyword matching. All 17 `docs/status/ENGINES.md` engines are now `Built`. 2 new skills registered in `plugin.json` (44 → 46); 7 new tests for `knowledge-index.mjs`'s pure functions, including a real query against the real reference tree. v0.7.36.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **PR 7 of the AI Engineering Operating System build: the 20-layer engine surface + Core Operating Loop.** 17 `plugins/wingman/engines/<name>/ENGINE.md` manifests, deliberately built as a naming and ownership layer over machinery that mostly already existed (~14 of 17 engines were fully built before this PR, per the ground-check the absorbed 17-engine plan already did) rather than a rebuild. Each manifest declares purpose, inputs, output artifacts, members, state read/written, escalation, and permitted tool tiers (composed with `references/permission-model.md`, not a second scale). `scripts/validate-engines.mjs` + `scripts/engines-check.mjs` (wiring/logic split, matching `constitution-check.mjs`/`wkos-check.mjs`'s precedent) enforce the rule that keeps this from repeating the 2026-07-22 blueprint's fate: every command/skill/hook/reference has exactly one engine owner, no orphans. **Caught a real self-introduced bug on its first run**: two companion scripts (`query-founder-knowledge.mjs`, `memory-tiers.mjs`) were initially declared inside the enforced `## Members` list, but the check's own declared scope is commands/skills/hooks/references only — scripts are deliberately out of scope, matching the master plan's literal wording. Fixed by moving them to prose-only "Companion script" notes; the check then passed clean against the real 119-file plugin tree on the very next run. Further verified by deliberately deleting one real member declaration and confirming the checker reports the resulting orphan before the file was restored. Also ships **`references/evaluation-dimensions.md`** — the Evaluation Engine's 12-dimension map onto the 8 real Boardroom seats, derived directly from each seat's own existing "What you check" text (not invented), explicitly **not** a second numeric-scoring system next to the existing `GO | GO_WITH_CONCERNS | NO_GO` verdict contract (a completeness map, not a replacement — per `references/constitution.md` rules 3 and 4). New `docs/status/ENGINES.md` (the engine→implementation index) and `docs/status/CORE-LOOP.md` (the 12-phase Vision→Understand→Research→Ground→Plan→Design→Architect→Execute→Evaluate→Improve→Remember→Learn loop, mapped onto the 17 engines and the real 14 pipeline stages). Fixed two stale docs found in passing: `docs/status/PRD.md` still described the retired 7-stage/3-checkpoint "Planning Milestone" model, and `docs/status/REGRESSION-CHECKLIST.md`'s Layer 1 list had silently drifted — `validate-wkos.mjs` (shipped in PR3) was never added to its CI-checks description, fixed alongside `validate-engines.mjs`'s own new entry. 7 new tests. v0.7.35.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **PR 6 of the AI Engineering Operating System build: the research-first philosophy, made mechanical.** New `research/` matrix at the repo root (dev-repo-only, like `docs/`/`evals/`/`tests/` — never ships with the plugin): 13 domain folders, each holding real `TRUTH-<capability>.md` documents following a locked 5-study framework (Pioneers, Current Best Implementations, Community Experience, Engineering Trade-offs, Our Synthesis). Populated **only when a real capability needs it** — matching the same evidence-gate discipline applied everywhere else in this build, not all 13 speculatively at once. Two domains are populated for real using actual `WebSearch` results, not recalled training data: `02-context-engineering/TRUTH-context-engine.md` (Tobi Lütke/Shopify coining "context engineering" June 2025; Sourcegraph/Kubiya's reliability-first framing, matching this project's own dogfood-found `state_stage_mismatch` motivation) and `03-memory/TRUTH-memory-tiers.md` (MemGPT/Letta's Core/Recall/Archival 3-tier design; the "don't use one retrieval strategy for every tier" finding that independently validated PR 5's own no-merge `readAllTiers()` design, applied to a scope axis rather than Letta's retention axis). The other 11 domains ship as honest stub `README.md`s stating plainly that no `TRUTH-*.md` exists yet — never faked as populated. New `skills/research-gate` (maintainer-only, scoped identically to `skills/dogfood-gap-classification` — never founder-facing, only applies inside Wingman's own dev-repo checkout) enforces "Research Complete? → Architecture Approved? → Requirements Complete? → Implementation Starts," carrying the Human Approval Framework (Must ask / Should ask / Can decide automatically) and a Decision Record template. Its mechanical consumer, `scripts/check-research-truth-doc.mjs` (dev-repo-only), checks all 5 required section headings plus real non-empty content under each, all 5 "Our Synthesis" sub-fields present and non-empty, and a References section — and **caught a real bug on its very first run against this PR's own new content**: both populated `TRUTH-*.md` files used a modified `**Our Improvements over the general ... pattern:**` label instead of the template's exact `**Our Improvements:**`, fixed in the docs rather than loosening the checker. Locked in as a named regression test rather than silently fixed and forgotten, matching this build's own repeated pattern (`constitution-check.mjs`, `wkos-check.mjs` each caught a real defect on first run too). 7 new tests. v0.7.34.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **PR 5 of the AI Engineering Operating System build: the Memory Engine's remaining 6 tiers.** `scripts/memory-tiers.mjs` (new) resolves Global (`~/.wingman/global/`) and Org (`~/.wingman/org/<slug>/`) — both outside any repo, which stays compatible with `docs/status/ARCHITECTURE.md` §2's "no persistent runtime" rule since that rule permits file-backed state opened on demand and forbids only an always-on daemon — plus Product, Project, Feature, Task, and User. **Product and Project deliberately resolve to the identical store**: Wingman has no multi-product-per-project concept anywhere in its real architecture, so a separate tier would be inventing structure with no consumer, which `skills/evidence-gated-catalog`/`references/constitution.md` rule 3 forbids; documented as a collapse, not silently dropped. `readAllTiers()` returns every applicable tier's entries narrowest-first without merging, so a genuine cross-tier contradiction stays visible to the reading session rather than being silently resolved — the founder's own explicit design requirement. Promotion to Global/Org is **mechanically** gated, not just documented: `writeTierEntry()` throws unless called with `{ approved: true }`, which only a real `AskUserQuestion` confirmation should ever set. Secret rejection reuses `hooks/secret-guard.mjs`'s exact `SECRET` pattern list rather than a second copy. `query-founder-knowledge.mjs` gains `unifyTiers()` as a new, separate entry point; `unify()` is untouched, staying project-tier-only for full backward compatibility with `skills/context-assembly` and `hooks/session-start.mjs`'s existing recall. Along the way, `docs/status/DATABASE.md`'s file tree was found to have drifted a second time since its 2026-07-15 fix — 7 more real `.wingman/` paths existed with no tree entry (`sdd/`, `okf-export/`, `loop.json`, `loop-counter.<sessionID>.json`, `session-state.json`, `session-health.json`, `pending-warnings.json`, `context-monitor.json`) — fixed in the same pass. Verified directly, not just by test: a fact written at Global tier from one throwaway project is visible from a second, unrelated project; a Project-tier fact is not. 441 → 456 tests, v0.7.33.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **PR 4 of the AI Engineering Operating System build: promoted `query-founder-knowledge.mjs` to the Context Engine and mechanized the memory read-back loop.** The script had real dogfood evidence behind it (a 4-session run proving a cold session halts on a real `DO NOT SHIP` verdict read from `--summary` alone) but zero automated test coverage — added it (`tests/hooks-integration/query-founder-knowledge.test.mjs`, 9 cases covering `unify`/`query`/`summary` and the `state_stage_mismatch` drift detector). New `skills/context-assembly` reads this unified state before any stage-specific work starts; wiring it into `references/pipeline-stage-boilerplate.md`'s new Context Assembly section surfaced a real gap — **7 of the 13 non-`build`/`ship` pipeline stages never cited the shared boilerplate file at all**, so "wire once, all 14 inherit" wasn't actually true until this pass added the citation to each. Also mechanized `skills/memory`'s operating rule 4 ("On SessionStart, surface a one-line recall"), which `references/constitution.md` rule 9 had disclosed as instruction-only: `hooks/session-start.mjs` now prints a `Recall:` line from the most recent memory/decision entry, reusing `query-founder-knowledge.mjs`'s existing parsers rather than re-implementing them. Verified directly against a real fixture (not just tests): a project with only a `decisions.md` entry produces the recall line; a project with no memory produces neither a line nor a crash. 428 → 441 tests, v0.7.32.

<!-- wingman:log type=decision category=governance status=resolved -->
- **Founder directed adoption of a full "AI Engineering Operating System" plan — a 20-layer Master Build Prompt, a research-first development philosophy, and a ~130-document Knowledge Operating System (WKOS) — in their own words "no matter what… achieve fully."** Recorded as a deliberate, disclosed override, not as the evidence gate clearing itself; see `docs/status/ARCHITECTURE.md` §8g for the full framing. What it reverses was surfaced back to the founder first: the 2026-07-22 decision cutting a pasted ~60-file org-governance blueprint down to 10 files after an audit found "the blueprint's files have zero consumer… dead weight on creation" (WKOS is ~130 files and the same shape); `docs/roadmap/ROADMAP.md` item 16's explicit *drop* of local vector search; and `docs/history/audit-reorg-2026-07-20.md`'s "no semantic search over skills exists or should be built." **The design constraint that makes this buildable rather than a repeat of 2026-07-22**: the earlier blueprint failed one specific test — its files had no producer and no consumer — so WKOS is built as the *output contract of machinery that already exists* (the 14 pipeline stages already produce ~14 of its documents; `docs/status/` already holds ~9 more), and any document with no producing stage ships as a `templates/` entry rather than an empty file in a founder's repo, asserted mechanically by a validator rather than promised. Three constraints hold regardless of the override, because breaking them breaks the product: zero runtime dependencies (`install-smoke.yml` asserts `node_modules` never appears, so vector/semantic search stays in `agnostic-boardroom/`), the unconditional any-`NO_GO`-blocks rule, and no bulk creation without a consumer. **Landed in v0.7.30**: `references/constitution.md` (the 10 rules, each naming the real mechanism enforcing it, with rule 9 disclosing that the memory read-back loop is instruction-only today); `scripts/constitution-check.mjs` + a `validate-structure.mjs` error check giving the constitution a mechanical consumer on day one — it caught two real defects in the constitution's own first draft, two rules citing `docs/status/*.md` as enforcement (`docs/` does not ship, so a founder's install cannot reach it) and an earlier line citing the dev-repo-only root `scripts/check-repo-consistency.mjs` from shipped content; `skills/acceptance-criteria` (the one genuinely absent layer, confirmed by grep, carrying an explicit boundary table against `definition-of-done` so the two gates don't merge); and a risk-dimension ownership map in `references/permission-model.md` mapping the founder's 10 risk dimensions onto the 8 existing Boardroom seats rather than creating a second risk scale — which surfaced a real gap: **model risk has no owning seat**, split informally between CTO and Research today, named once and deliberately *not* promoted to a new seat since `docs/roadmap/AGENT-ROSTER.md`'s 2+-occurrence rule governs new roles.

<!-- wingman:log type=decision category=harness-breadth category2=governance status=resolved -->
- **Founder explicitly directed a full agent-agnostic native-support build across 6 harnesses (Codex CLI, OpenCode, Gemini CLI, Cursor, Cline, OpenHands), deliberately overriding a stance this project had declined 3 times (2026-07-18, re-confirmed 2026-07-22 and 2026-07-25) on evidence-gate grounds.** Before scoping anything, the founder was shown that full history (the Boardroom's `AskUserQuestion`/`ExitPlanMode`+gating-hooks/parallel `Task`/`Agent` dispatch are structurally Claude-Code-specific; every prior real demand for a *specific* harness was answered narrowly, never with a full rewrite) and asked to confirm this was a deliberate override, not new organic evidence — confirmed via `AskUserQuestion`. Fresh 2026-07-27 capability-matrix research across all 6 target harnesses found **no harness has full parity**; the plan-mode/`ExitPlanMode`-equivalent gate is the universal weak point (Codex CLI: none, long-requested, no maintainer commitment; OpenCode: undocumented experimental flag, likely weak; Gemini CLI: real but auto-escalates to YOLO on exit; Cursor/Cline: UI-click-only, no interception point; OpenHands: only a blanket confirmation-mode toggle) — but two real corrections emerged versus the 2026-07-25 record: **Codex CLI now has confirmed genuine parallel subagent dispatch** (`spawn_agent`/`send_message`/`wait_agent`/`close_agent`, superseding the prior "unconfirmed at scale" note), while **OpenCode's dispatch is confirmed still sequential** (open bug `anomalyco/opencode#29638`). Gemini CLI turned out to have the closest overall match (real hooks, real parallel subagents, a real `ask_user` tool comparable to `AskUserQuestion`) of any harness researched. Founder further decided: cover all 6 harnesses (not just the 2 previously targeted); build the best available behavioral equivalent per harness for the 2-3 gaps each has, disclosing every substitution as a tradeoff rather than claiming silent parity; keep founder-checkpoint scope to interactive sessions only (headless/single-shot invocations stay out of scope). **Phase 1 (this session) lands the foundation**: `generate-harness-adapters.mjs`/`check-harness-adapter-drift.mjs` refactored from two hardcoded harness branches into a descriptor-driven model (`scripts/harness-targets/*.mjs`), verified behavior-preserving (`--check` still produces the identical 73 files, modulo the two corrected harness notes above), plus the two Codex CLI/OpenCode note corrections themselves. Remaining phases (Gemini CLI/OpenHands/Cline/Cursor adapters, capability-profile-aware branching in canonical `boardroom.md`/`evolve.md`/etc., new eval cases, and the full `docs/status/ARCHITECTURE.md` §8f + `harness-adapters/README.md` rewrite) are tracked as open follow-on work, not yet built — see the full plan for the exact sequencing and per-harness disclosed-substitute decisions.

<!-- wingman:log type=decision category=harness-breadth category2=verification status=resolved -->
- **Asked to bring the OpenCode adapter to "full 100% harness coverage" (agents + commands + skills + a real installer). Agents (8/8) and commands (32/32) were already complete; delivered a real install script, ported all 40 skills with a major real finding, and honestly downgraded a previously-"unverified" gate to "confirmed likely broken."** Three real, disclosed pieces of work:
  1. **Real installer** (`install.mjs`, replacing 3 manual `cp -r` steps): copies the adapter's `.opencode/` into a target project, optionally installs the git pre-push DoD gate via the existing `install-git-hooks.mjs`, and auto-writes a minimal `opencode.json` when the target has none. Verified live (5 tests, `tests/opencode-gate/opencode-install.test.mjs`): 41→92 file parity (before/after skill-porting), idempotent re-run, `--with-git-hook` gating, refuses a non-existent target.
  2. **All 40 skills ported with zero translation — a major, real finding, not assumed.** Live investigation found OpenCode's project-level skill discovery (`.opencode/skills/<name>/SKILL.md`) reads the exact same `SKILL.md` YAML-frontmatter format Claude Code uses, confirmed via OpenCode's own built-in `customize-opencode` skill's documented path table. This reverses the earlier framing that only `git-pr-workflow`/`package-manager-selection` were genuinely harness-agnostic — that framing was about Claude Code's own auto-invocation mechanism, not the file format's portability. One real, non-obvious requirement found along the way and not documented anywhere before: project-level skill discovery silently found zero skills until the target project had an `opencode.json` present (even a minimal one) — confirmed by a direct A/B test, now handled automatically by `install.mjs`. A live fresh install confirmed all 40 skills discovered by `opencode debug skill`, not a subset. `check-harness-adapter-drift.mjs` extended with a strict byte-equality check (skills are meant to be verbatim copies, unlike the Boardroom seats' faithful-translation check) — confirmed clean across all 40.
  3. **`wingman-gate.js`'s `plan_exit` wiring downgraded from "unverified" to "confirmed likely broken," using a genuinely free OpenCode model (`opencode/deepseek-v4-flash-free`, zero cost, zero API key) to avoid the earlier OpenCode Zen billing wall entirely.** `opencode debug agent plan` showed the real `tools` list is `invalid, question, bash, read, glob, grep, edit, write, task, webfetch, todowrite, skill` — `plan_exit` is **not** in it, only appearing as a `permission` entry; directly invoking it (`opencode debug agent plan --tool plan_exit ...`) failed with `"Tool plan_exit not found for agent plan"`. This contradicts the earlier research-based assumption (a GitHub issue referencing `plan_exit` by name) that it was a real, model-invokable tool — plan-mode exit appears to be a TUI/session-level action, not something `tool.execute.before` would ever see fire via the standard tool-call path. The pure decision logic (`evaluateCheckpoint`) is confirmed correct and now has permanent test coverage (5 scenarios, `tests/opencode-gate/opencode-gate.test.mjs`) independent of whatever the eventual correct OpenCode wiring turns out to be. The correct real hook point remains unknown — would need OpenCode's own plugin-API source or a maintainer answer, not resolvable from this sandbox alone.

<!-- wingman:log type=decision category=dogfood category2=verification status=resolved -->
- **First real dogfood run of the whole `agnostic-boardroom/` chain (memory → routing → live Agno Maker/Checker loop) — closing the gap where only mocked wiring tests had ever exercised it end to end.** `.claude/commands/ship-feature.md`'s described chain (`retrieve_memories_tool` → `route_task` → the Maker/Checker loop) had never been run live together; `tests/test_end_to_end_dry_run.py` proves the wiring with a mocked `call_model`, and `tests/test_pipeline_wiring.py` proves real routing feeds the Maker's prompt, but neither used real memory retrieval + real routing + a real live model together in one pass. Ran `engine.pipeline.run_ship_feature_dry_run()` for real, in a throwaway scratch SQLite/LanceDB substrate (not touching this repo's own `.data`/`.lancedb`): seeded one real memory fact ("bug-reproduction scripts must include a bare `assert`, not a print statement"), then asked for a genuine small task ("write a minimal Python script that reproduces a bug: a function meant to add two numbers instead subtracts them"). **Three real, disclosed findings, not smoothed over**: (1) the seeded memory fact was genuinely retrieved and present in `memory_hits`; (2) `route_task` hit its own documented `low_confidence_fallback` path for real on a realistic task for the first time (routed to `engineering-minimalism` at `best_similarity=0.43`, below the ~0.5 threshold) — previously this path had only been exercised by a synthetic nonsense query (`test_nonsense_query_falls_back_but_still_returns_a_skill`), so this is the first live confirmation the gap is real, not just a test artifact; (3) despite the mismatched skill context, the live Maker/Checker loop still accepted a genuinely correct solution on the first iteration (real cost `$0.2179`, 2 real Checker-named concerns about test coverage breadth) — informative that the Checker wasn't fooled by the wrong context *this time*, not proof the low-confidence-routing gap is safe in general across other tasks. This closes Phase 2b's own stated gap ("the described chain has never been exercised end to end") with a real, live, single-pass proof rather than another mocked test.

<!-- wingman:log type=decision category=harness-breadth status=partially-resolved -->
- **Second real gateway provider added: `gateway/providers/opencode.py`'s `OpenCodeProvider`, honestly disclosed as only partially live-verified.** Asked how to proceed on harness breadth given Codex CLI isn't installed in this sandbox at all and OpenCode had no API key configured; the founder chose to supply a fresh OpenCode Zen key for a real live test rather than skip it or ship an unverified adapter (the key was exported as a shell env var only, never written to any repo file, and unset immediately after use — same discipline as the prior OpenCode key exchange). Two real, non-obvious findings from direct testing before any adapter code was written: (1) `opencode run --format json` streams newline-delimited JSON events (`text`/`step_finish`/`error` types), a genuinely different wire shape from `claude -p --output-format json`'s single JSON object; (2) OpenCode resolves its project config (and therefore whether the `{env:OPENCODE_ZEN_API_KEY}` substitution in `opencode.json` actually applies) via the **`PWD` environment variable**, not merely the subprocess's OS-level working directory — confirmed by a direct A/B test where `cwd=` alone silently misrouted to a different default provider (GitHub Copilot) with a malformed-auth-header failure. **Honest, disclosed gap**: 2 genuinely successful live completions were captured via the raw CLI (proving the wire format and the `PWD` mechanic), but the OpenCode Zen workspace then hit a real account-level billing wall ("No payment method", confirmed non-transient across 3 consecutive retries) before the `OpenCodeProvider` class itself could complete a live end-to-end round-trip. Rather than force it (asked the founder; declined adding billing for now) or silently claim full verification, the parsing logic is instead tested at zero cost against the *actual* JSONL captured from the 2 real successful/error calls (`tests/test_opencode_provider.py`, fixtures are real captured output, not fabricated), plus a `Router` two-provider dispatch test (`tests/test_gateway_router.py`). `RunResult.total_cost_usd` for this provider is explicitly flagged as unconfirmed against real Zen billing (every real call so far reported `cost: 0`), unlike `ClaudeCliProvider`'s cost figure which has been cross-checked against real non-zero spend elsewhere in this repo's history.

<!-- wingman:log type=decision category=verification status=resolved -->
- **Closed the one real behavioral gap the Agno-native rewrite (below) left open: the rewrite's mocked tests proved the `Workflow`/`Loop` orchestration mechanics, but the 2 real `live_model`-marked tests (`test_loop_live.py`) had only ever been run against the old hand-rolled loop, before the rewrite — never reconfirmed against the new implementation.** Also caught and fixed, before this run, a real squash-merge divergence: the branch these commits landed on had drifted from `main` since the earlier gateway PR (#114) merged, producing a genuine `mergeable_state: dirty` (real content conflicts via `git merge-tree --write-tree`, not a false "behind" flag) — resolved via `git rebase --onto origin/main`, re-verified with a fresh 76/76 pytest pass, force-pushed, and merged clean as PR #115. With that merged, ran `pytest tests/test_loop_live.py -m live_model` for real (small, real dollar cost, explicitly authorized): both tests passed — `test_live_checker_genuinely_rejects_an_obviously_wrong_solution` (a live Checker call genuinely rejected a deliberately-wrong `add()` implementation) and `test_live_full_loop_accepts_a_correct_simple_solution_and_logs_real_cost` (a full live Maker→Checker pass accepted a correct solution with real, non-zero logged cost) — confirming the Agno rewrite's live behavior, not just its mocked control flow, actually holds. Harness breadth (a second model-CLI adapter beyond `claude`) was explicitly deferred until after this proof, per the stated reasoning: building a second harness adapter on top of an unproven-live rewrite would have been building on an unconfirmed foundation.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Follow-up to the gateway-addition decision below: asked to do a full Agno-native rewrite of `agnostic-boardroom/`'s loop/graph, combined with directory reorg, in one pass — surfaced and resolved the one real risk before writing rewrite code, rather than assuming Agno's `Model` interface would accept a subprocess-based provider.** Every documented Agno `Model` example (LMStudio, Ollama, OpenAI-compatible providers) speaks an HTTP client; this backend's actual model call is `subprocess.run(["claude", "-p", ...])`. Asked the founder whether to spike this risk separately first or proceed anyway; the founder chose to proceed on the risk directly — so the spike was sequenced as the literal first code written in this pass, not skipped: `models/agno_claude_cli_model.py` implements Agno 2.8.2's real installed `Model` abstract base (confirmed via `inspect.getsource`, not docs alone — its 6 abstract methods are `invoke`/`ainvoke`/`invoke_stream`/`ainvoke_stream`/`_parse_provider_response`/`_parse_provider_response_delta`), delegating to the existing `ClaudeCliProvider`. A real `Agent(model=AgnoClaudeCliModel()).run("say hi")` call was run and inspected before any further code was written — it returned a genuine `RunOutput` with content `"hi"`. `agents/loop.py` → `loop/workflow.py` (Agno `Workflow`+`Loop`, `end_condition` reading the Checker's verdict, `max_iterations=3`) and `agents/graph.py` → `graph/pipeline_workflow.py` (a plain sequential `Workflow` of per-stage `Step`s, `StepOutput(stop=True)` halting at the first checkpoint-requiring stage) both preserve their exact prior public contracts (`LoopResult`, `PipelineResult`), confirmed via 3 real empirical scenarios each (accept-first-try/accept-with-concerns/escalate-at-cap for the loop; full-completion/mid-pipeline-stop/no-handler-registered for the graph) rather than assumed from Agno's docs. `engine/boardroom_engine.py`'s `to_boardroom_verdict()` mapping needed zero logic changes as a direct result. One new, confirmed-not-assumed dependency: `agno.workflow`'s own package `__init__` unconditionally imports `agno.workflow.remote.RemoteWorkflow`, which imports `fastapi` — a real `ModuleNotFoundError` on first import, now pinned in `pyproject.toml`. The directory reorg itself was scoped narrowly: no `sdk/`-shaped rename, no speculative `Protocol`/tool-registry layer from an earlier, separately-declined pasted proposal — `core/`, `db/`, `knowledge/`, `mcp_server/`, `eval/`, `gateway/` kept their existing names, moved (not renamed) alongside the new `loop/`/`graph/`/`engine/`/`models/` directories. Verified via a real pytest collection diff (76/78, matching the pre-change baseline exactly) both inside a temporary parallel `sdk_new/` tree and again after cutover to the final location — a real path-depth bug surfaced during the parallel-tree phase (`REPO_ROOT = Path(__file__).resolve().parents[2]` in `knowledge/vector_store.py` resolved one level short while nested inside `sdk_new/`), confirmed as a testing-artifact of the temporary nesting rather than a code defect, and confirmed resolved at the final path depth. `plugins/wingman/` untouched throughout.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Asked to "full refactor" the repo directory structure for modularity; asked what was actually driving it before touching anything, per this project's own evidence-gate discipline — the answer named a real, concrete ambition (an "AI Agent SDK with workflow/loop/graph engineering, an AI gateway, built-in tools"), and a scoped audit found it's mostly already built.** Two `Explore` passes plus a `Plan` pass (all read-only, dispatched before any file was touched) found: `plugins/wingman/` (the shipped plugin) already went through 2 evidence-driven directory refactors this session and has no new named friction point, so it stays untouched by this change. `agnostic-boardroom/` — the separate, additive, not-shipped experimental Python backend — already has the loop (`agents/loop.py`'s Maker/Checker), the graph (`agents/graph.py`'s 7-stage dispatcher), and built-in tools (`mcp_server/`'s FastMCP tool registry); only an "AI gateway" was genuinely missing (confirmed by grep — zero prior occurrences anywhere in the repo). Real precedent research (LangGraph's/CrewAI's/Mastra's own project scaffolds, Agno's actual module layout, LiteLLM as the concrete gateway precedent) found most of `agnostic-boardroom/`'s existing directories already map correctly to how real agent-SDK scaffolds organize this content — **zero renames of existing, tested files were made**, since no concrete friction justified moving anything already correctly named; motion for its own sake was explicitly ruled out. Added `gateway/router.py`'s `Router` class, modeled on LiteLLM's own `router.py` (stateless, per-call) as distinct from LiteLLM's `proxy/` (an always-on HTTP server) — this project needs the former, not the latter, given `docs/status/ARCHITECTURE.md` §2's "no persistent runtime" rule. `agents/model_runner.py` became a thin backward-compatible shim over the new `gateway/providers/claude_cli.py` (the exact same subprocess logic, moved verbatim, including the documented anti-stale-env-var fix) so all 8 real call sites (`agents/loop.py`, `agents/boardroom_engine.py`, `agents/pipeline.py`, `agents/boardroom/cto_evaluator.py`, `agents/departments/engineering_maker.py`, `eval/live_ab_run.py`, `eval/checker_rubric_ab.py`) — confirmed by grep before writing any code — kept working with zero forced import changes. Real pytest baseline confirmed directly before and after: 71/73 collected before, 76/78 after (5 new tests for the router, zero existing tests changed or dropped). This does not soften or revisit the Phase 5 live A/B finding that the new engine erred in the more dangerous, too-lenient direction — the gateway addition organizes already-scoped work, it isn't new evidence of readiness. Nothing under `plugins/wingman/` was touched by this change.

<!-- wingman:log type=decision category=platform-conventions status=resolved -->
- **Founder provided a real OpenCode Zen API key directly in chat; used it for a genuine live-inference test, closing the OpenCode half of the "unverified model inference" gap the prior entry below left open.** The key was never written to any file in this repository — it was exported as a shell environment variable only, referenced from a throwaway scratch project (outside this repo) via `opencode.json`'s `{env:OPENCODE_ZEN_API_KEY}` substitution, and unset immediately after the test. Ran `opencode run --agent boardroom-cto "..."` against a deliberately bad plan (plaintext password storage); the response came back correctly formatted per the seat's own `## <SEAT> VERDICT`-equivalent contract, rejecting the plan with the actual defect and concrete remediation — genuine persona-driven reasoning, not a templated echo. Found and disclosed a real, non-obvious mechanic along the way: `opencode run --agent <subagent-name>` doesn't invoke the subagent directly (it warns and falls back to the default primary agent), but that primary agent then automatically delegates to the named subagent by description-matching — confirmed by directly inspecting OpenCode's own local session database (`~/.local/share/opencode/opencode.db`'s `message` table), not just from the printed transcript. Also disclosed, not smoothed over: local telemetry reported `cost: 0` for every message in the run — not independently confirmed against Zen's own billing, so not claimed as proven-free. `plugins/wingman/references/harness-adapters/opencode/README.md` and `docs/HUMAN-TODOS.md` updated with the full finding; Codex CLI's own live-inference half of the same gap remains open (no credential provided for it).

<!-- wingman:log type=decision category=platform-conventions status=resolved -->
- **On explicit request to "ship" all 3 harness adapters (Claude Code/Codex CLI/OpenCode) properly, ran fresh, direct web research against primary sources before building anything, and resolved every previously-open question the Codex CLI adapter had.** Fetched OpenAI's official Codex hooks docs (`learn.chatgpt.com/docs/hooks`) live, not recalled from training data: (1) hooks are confirmed **enabled by default** (a secondary source claiming opt-in-required was checked directly against the primary doc and found wrong); (2) the `apply_patch` (file-edit) hook's scannable-content field is confirmed as `tool_input.command` — the same field `Bash` uses — resolving the one detail an earlier pass explicitly declined to guess at. Built the real artifact this unblocked: `references/harness-adapters/codex-cli/.codex/hooks/secret-guard.mjs`, a schema-verified port of the shipped `secret-guard.mjs`, tested via a real synthetic stdin/stdout JSON round-trip (destructive command → `deny`, secret pattern → `deny`, clean input → `allow` — all 3 confirmed against Codex's exact documented hook-response schema) and wired into `.codex/hooks.json` for both `Bash` and `apply_patch` matchers. Disclosed alongside the resolution, not smoothed over: real, currently-open GitHub issues (`openai/codex#16732`, `#20204`) report `apply_patch` hook events not firing consistently across all Codex CLI versions in practice, despite being documented as supported. Also resolved via direct fetch: Codex CLI **does** support real single-message parallel Boardroom dispatch (natural-language-driven — "spawn N agents in parallel for X, Y, Z" — with a real `agents.max_concurrent_threads_per_session` config), while OpenCode **does not** (confirmed sequential/orchestrator-driven via a direct docs fetch, with a live GitHub issue — `anomalyco/opencode#29638` — reporting even explicitly-requested parallel subagent work runs sequentially in practice). Both README's own guidance updated with concrete, sourced recipes rather than left as an open question. Still genuinely unverified, and correctly so: actual live model inference under Codex CLI/OpenCode (no configured model provider in this sandbox — tracked in `docs/HUMAN-TODOS.md`, requires the founder's own credentials).

<!-- wingman:log type=decision category=evidence-gate status=resolved -->
- **Re-checked every row in `docs/roadmap/AGENT-ROSTER.md`'s "Deferred mechanism ideas" table against every real source of dogfooding evidence this project has (`LEARNINGS.md`, `docs/history/retros.md`, this decisions log, `docs/HUMAN-TODOS.md`, all 5 `evals/dogfood-runs/*.json`, `query-wingman-knowledge.mjs --recurring`) rather than re-asserting stale prose.** 9 of 10 rows: zero new organic evidence for their specific revisit conditions — confirmed dated, not left as an unchecked assumption. 1 row ("AI-agent-agnostic transformation") updated in place: its own revisit condition ("a specific non-Claude-Code harness is actually targeted") has genuinely been met, but by a different, already-shipped, more narrowly-scoped mechanism (`check-harness-adapter-drift.mjs`, a real live-verified `codex plugin marketplace add` install, `generate-harness-adapters.mjs`) — not this row's own flatten-repo-root/`dist/`/`renames-map.json` proposal, which stays declined on its own separate merits. Also surfaced, honestly, two adjacent findings that aren't deferred-table rows but deserve the same evidence discipline: the founder-project `memory/*.md` read-loop "watch item" still shows zero real occurrences (unchanged since first checked), and a real, twice-recurring `Task`/`Agent`-dispatch friction (`Agent type not found` for `dept-*`/`mgr-*`/`boardroom-*` subagent types, both `2026-07-15` dogfood runs) was confirmed as an already-correctly-triaged sandbox capability limitation, not a cross-harness format gap the agent-agnostic row's condition was actually asking about.

<!-- wingman:log type=decision category=governance status=resolved -->
- **Cleared the full open-PR queue (4 PRs) with individual, evidence-based dispositions rather than a blanket merge — 2 merged, 2 closed.** **Merged #107** (`agnostic-boardroom` Phase 6/7 rubric+severity fixes, the flatten-nested-list generalization check, the `test_mcp_server_live.py` isolation-bug fix, and the 90%→93% `pytest-cov` coverage audit) directly — fully tested, zero conflicts, nothing to weigh. **#105 and #97** were two independent, stale Jules/Bolt PRs proposing the identical fix (caching `checkTestPresence`'s redundant test-file re-scan in `dod-structural-gate.mjs`) — compared both designs on the merits (#97's call-scoped local-array cache has no shared mutable state to leak across calls; #105's module-level `let` cache needs a manual reset every invocation, a real footgun if ever missed) and re-implemented #97's design fresh onto current `main` as **new PR #108** (both originals were multiple versions stale with zero CI ever run), verified with the full `node --test` suite + all 4 validators, merged, then closed #105/#97 as superseded with an explanation posted to each. **#96** ("Swarm-Intelligence Predictive Layer" + `/wingman:predict`) was investigated for real evidence before any decision, not just re-cited from the prior deferred-candidate log entry: grepped `LEARNINGS.md`/`docs/history/retros.md`/`docs/HUMAN-TODOS.md`/`docs/status/PROJECT.md`/every `evals/dogfood-runs/*.json` directly — zero organic mentions of a real need anywhere in this project's actual history, only the self-referential deferred-candidate row already logged. Beyond the evidence gap, the PR's own content failed independent inspection: its eval case (`evals/cases/predict.md`) documents an explicitly *simulated* subagent run, not a real one, and its added `ponytail-integration.test.mjs` tests are tautological (`calculateRisk()` redefined inline in the test file and asserted against its own literal output, never touching real production code — there is none, the "swarm" is prose asking an LLM to roleplay PSO/ACO/GWO terminology). The one separable, legitimate piece (`docs/status/AGENT-WEAKNESS-BENCHMARK.md`'s stale `skills/discipline/...` paths from the flat-skills refactor) was confirmed **already fixed on `main`** (`wingman-metrics.mjs`: 0 broken references, 0 status mismatches) — nothing left to cherry-pick. Closed with the full reasoning posted as a comment. **Post-merge, ran the complete health/validator suite against the real merged `main`** (a clean detached worktree, not the working branch): all 4 core validators PASS, `check-harness-adapter-drift.mjs` PASS, `wingman-health.mjs` reports 79/79 behaviors verified with 0 uncovered pieces, `wingman-metrics.mjs` clean, 174/174 Node integration tests passing, 71/71 `agnostic-boardroom` pytest tests passing (2 live-model tests correctly excluded) — confirming zero regressions from either merge. Repo ended this pass with zero open PRs.

<!-- wingman:log type=decision category=testing status=resolved -->
- **Ran a real, tool-measured statement-coverage audit of `agnostic-boardroom/`'s full harness (`pytest-cov`, not a manual line-by-line guess) and closed every real gap it found — 90% → 93%, every module at 100% except 2 explicitly-disclosed, deliberate exceptions.** Installed `pytest-cov` (now a real `dev` dependency in `pyproject.toml`), ran `pytest --cov=agents --cov=db --cov=knowledge --cov=mcp_server --cov=core --cov-report=term-missing` against the full fast suite (live-model tests excluded, same as every other run). **Baseline, independently read from the tool's own output, not estimated**: 539 statements, 56 missed, 90% — with `mcp_server/server.py` at a stark 0%, `agents/model_runner.py` at 70%, and smaller gaps across `db/repository.py`, `agents/boardroom/cto_evaluator.py`, `knowledge/vector_store.py`, `mcp_server/memory_tools.py`, `db/connection.py`. Investigated each file's specific missing lines directly (not assumed from the percentage alone) before writing any test: `agents/model_runner.py`'s gap was the real subprocess-response-parsing logic never exercised because every existing test raised `TimeoutExpired` before reaching it — closed with 3 new tests mocking `subprocess.run`'s *return value* (a successful JSON response, a non-zero returncode, an `is_error: true` response), zero live cost, zero real model calls. `db/repository.py`'s gap was substantial: `insert_debt` and `insert_traceability` had literally zero test coverage anywhere in the suite, and `ingest_checkpoint_raw`'s *success* path (a valid payload actually persisting) was untested — only its failure path was. `cto_evaluator.py`'s gap was the second fail-closed branch (a `{}`-shaped block whose contents still don't parse as JSON, distinct from "no `{}` block at all," which was already tested). `db/connection.py`'s gap was `checkpoint_wal`'s invalid-mode `ValueError`. `knowledge/vector_store.py`'s gap was both `FileNotFoundError` paths (a nonexistent skills directory, an unknown skill name) — new, fast `tests/test_vector_store.py`, no embedding cost since both raise before any embedding happens. `mcp_server/memory_tools.py`'s gap was the early-break-at-k branch in `retrieve_memories`, never hit because no prior test had more matching entries than `k`. **9 new tests total, zero production code changes** — every one of these was a genuine already-correct-behavior-with-no-test-coverage gap, not a bug fix. Full suite re-run after: 71 passed (up from 59), coverage 93%.

**The 2 remaining gaps, disclosed rather than gamed to 100%**: `mcp_server/server.py` (0%) is a coverage-tool measurement artifact, not an untested-behavior gap — this thin FastMCP wrapper is only ever exercised as a real subprocess (`tests/test_mcp_server_live.py`'s 4 real MCP-protocol tests), and `coverage.py` running in the parent test process cannot see statements executed inside a separate Python interpreter process without additional `COVERAGE_PROCESS_START` subprocess-propagation plumbing. That plumbing was considered and declined: `server.py`'s own docstring already states it has "almost no logic of its own," it is already functionally verified end-to-end over the real protocol, and adding subprocess-coverage instrumentation for a 36-line pass-through wrapper is a disproportionate engineering investment for a tooling limitation, not a real risk. `knowledge/ab_harness.py`'s 1 remaining line (`if kb is None: kb = build_skill_knowledge()`, a trivial default-construction fallback identical in shape to the function it calls, which is already tested elsewhere) was also declined: closing it would cost a real, slow full-skill-index rebuild for a branch with no distinct logic to break. **The honest headline: 93% real statement coverage, with the remaining 7% named, explained, and deliberately not chased for the sake of a round number** — this project's own engineering-minimalism discipline applied to test coverage itself, not just production code.

<!-- wingman:log type=bug category=testing status=resolved -->
- **A coverage-gap audit of `agnostic-boardroom/` found 2 real, previously-untested code paths (both already had explicit, correct fail-fast/graceful-degradation behavior — the gap was missing test coverage, not missing behavior) and confirmed 2 proposed "fixes" from a pasted audit didn't apply.** Grepped the real call path (`model_runner.py`, `cto_evaluator.py`, `engineering_maker.py`) for any size guard on assembled prompts: none exists, confirmed by direct search, not assumed — added `test_oversized_context_does_not_crash_the_loops_own_wiring` (50KB context, mocked model) proving the loop's own Python wiring doesn't choke, while explicitly disclosing this does NOT prove the real `claude -p` CLI itself degrades gracefully at its own limits (that would need a live, cost-bearing call). Found `knowledge/skill_router.py`'s `route_task()` already raises `ValueError` on an empty index, and `mcp_server/memory_tools.py`'s `retrieve_memories()` already returns `[]` gracefully on an empty store — both real, deliberate, already-correct behavior with zero test coverage; added `test_empty_index_raises_a_clear_error_rather_than_silently_returning_nothing` and `test_retrieve_on_an_empty_store_returns_an_empty_list_not_an_error` to close both gaps. **Two proposed fixes from a follow-up pasted "Grounding Truth Audit" were checked and found not to apply**: an "atomic file writes" fix assumes raw JSON/YAML flat-file state — grepped directly, `agnostic-boardroom` has zero such writes; all state goes through SQLite in WAL mode, chosen specifically for crash-safety in Phase 2 (already documented) — the proposed fix would be dead code with no call site. A "subprocess process-group kill" fix for orphaned children was checked against `run_claude_headless`'s real code: it already uses `subprocess.run(..., timeout=timeout_s)`, which already prevents an indefinite hang; the process-group nuance (a grandchild surviving the immediate child's kill) is a real but minor, unevidenced edge case, not implemented speculatively. A separate pasted test snippet for the same 3 gaps referenced functions/signatures that don't exist in this codebase (`run_claude_command`, `retrieve_top_k`, wrong parameter names/order) and would not have run; the corrected, real-signature tests above are what actually landed and pass (59/59 fast tests, full suite, confirmed directly). Total code change: 3 new tests, zero production code changes (the 2 real gaps were test-coverage gaps, not behavior gaps).

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Ran the genuinely-new scenario Phase 7 left as an open evidence gap — the severity-calibration fix generalizes, confirmed by direct inspection, not by trusting a coincidental tier match.** Added `flatten-nested-list` (flatten an arbitrarily-nested list) to `eval/live_ab_run.py`'s scenario set: orthogonal to `palindrome-check`'s punctuation-handling gap, exercising a different checklist dimension (architecture-fit/scope judgment, not a missed correctness edge case). **Real result**: new engine `GO_WITH_CONCERNS`, real CTO persona `GO_WITH_CONCERNS` — agreement (cost $0.347196). But the harness didn't log *why* the engine reached that tier, and the routing was `low_confidence_fallback` (routed to `platform-native-reference`, an unrelated skill) — the same ambiguity `simple-email-validation` already had in Phase 5/6 (nominal tier agreement, possibly for an unrelated reason, not proof the content-based fix did anything). Fixed the logging gap (`live_ab_run.py` now records `checker_final_concerns` on every future run) and ran one supplementary live call (`eval/checker_generalization_check.py`, $0.017608) re-evaluating the exact accepted artifact directly through the current Checker. **Result settles the ambiguity**: the Checker independently named "No handling for extremely deep nesting causing RecursionError" as a concern — the *exact* risk the real persona called its "biggest technical risk" ("could hit Python's recursion limit and crash"). This is real, direct evidence the Phase 7 concerns/severity-guidance fix generalizes to a scenario it was never tuned against, not just a repeat of the one case it was diagnosed on. Total real spend, this scenario: $0.364804. **Running total across Phases 5-7 plus this generalization check: $2.219720.** Still honestly bounded: this is 2 of however-many-real-world-scenarios exist where the fix has now been checked (`palindrome-check` re-tested, `flatten-nested-list` new) — a real improvement in evidence quality over "one re-tested scenario," but still not a claim this is solved for all future cases; `boardroom_engine.py` remains an engineering-review engine with promising, now more broadly (if still narrowly) evidenced decision quality.

<!-- wingman:log type=bug category=testing status=resolved -->
- **Found and fixed a real test-isolation bug while running `agnostic-boardroom`'s full test suite for the Phase 7 work above: `test_mcp_server_live.py` was silently sharing (and permanently polluting) the same production SQLite/LanceDB store every real dev/CI run uses, causing non-deterministic failures.** `mcp_server/server.py`'s `_conn`/`_kb` used to always point at the hardcoded default paths (`db/.data/agnostic_boardroom.sqlite3`, `mcp_server/.lancedb`) with no override — every test run appended to the same persistent store on disk, forever. As that store grows, top-`k` similarity retrieval becomes non-deterministic: a freshly-inserted marker can get pushed out of the top 5 results by older accumulated entries. Caught directly (not asserted): the same test passed cleanly in isolation (`pytest tests/test_mcp_server_live.py`, 4/4 passed) but failed when run as part of the full suite (`1 failed, 55 passed`) — a real, reproducible discrepancy, not a flaky assumption. Fixed by adding 3 env-var overrides (`WINGMAN_AB_DB_PATH`, `WINGMAN_AB_MEMORY_LANCEDB_URI`, `WINGMAN_AB_MEMORY_TABLE`) to `server.py`, defaulting to the unchanged real production paths (zero behavior change for a real founder session), and pointing `test_mcp_server_live.py`'s subprocess at a fresh `tmp_path_factory` location via those env vars instead. Re-ran the full suite after the fix: 56/56 passed. This is exactly the class of "future blocker" a structural audit should surface — a store with no test isolation gets slower and flakier the longer the project lives, and would have made CI increasingly unreliable over time without ever being an obvious code bug.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 7 fixed Phase 6's over-correction with a structural schema change, not a prompt tweak, and it worked on the real, re-tested case.** Root cause, confirmed by re-reading Phase 5/6's actual logs directly rather than trusting either my own prior "add a checklist" fix or the user's proposed "Collaborative Blindness/enumerate more edge cases" theory: `CheckerVerdict` was strictly binary (accept/reject), with no way to express "shippable, but here's a real named concern" — exactly the real CTO persona's `GO_WITH_CONCERNS` middle tier. Before this fix, `boardroom_engine.py`'s `GO_WITH_CONCERNS` path could *only* ever be reached via routing confidence (`low_confidence_fallback`), never via the Checker's own judgment about the code. Fixed by adding `CheckerVerdict.concerns: list[str]` (threaded through `IterationLog`/`LoopResult.final_concerns`) plus a `_SEVERITY_GUIDANCE` prompt addition instructing the Checker to distinguish a genuine correctness gap (reject) from a real-but-shippable concern (accept + name it) — `boardroom_engine.to_boardroom_verdict()` now combines both the content-based and routing-based signals into `GO_WITH_CONCERNS`, surfacing both when both are present. **Real result, one live call against the exact known-buggy `palindrome-check` code** (`eval/checker_severity_ab.py`, cost $0.098265, logged to `eval/.data/checker_severity_ab_results.jsonl`): `accepted: true` with 2 named concerns (missing punctuation/whitespace-variant handling, no test cases) — this is the first time across 3 real attempts (Phase 5 pre-fix accept-with-no-concern, Phase 6 post-rubric-only escalate/reject, this Phase 7 result) that the engine's verdict shape actually matches the real persona's `GO_WITH_CONCERNS` tier rather than either extreme. A new unit test (`test_accepted_matched_route_with_checker_concerns_produces_go_with_concerns` in `tests/test_boardroom_engine.py`) proves the content-based path is independently reachable under a confident (non-low-confidence) route, not just a byproduct of the routing-based path. Total real spend across the full investigation (Phases 5+6+7): $1.854916. This is a genuine, disclosed fix, not rounded up further than the evidence supports — one re-tested scenario is evidence, not proof the calibration generalizes to unseen cases. **Still open, stated plainly rather than left implicit**: this result is one real call on one previously-known scenario, re-run after a targeted fix — it demonstrates the specific over-correction is resolved, not that severity calibration is solved in general. A genuinely new, previously-unseen scenario (not `palindrome-check` again) has not yet been tried against the fixed Checker. Promoting `boardroom_engine.py` beyond "engineering-review engine with promising but narrowly-evidenced decision quality" would need at least one more live scenario the fix wasn't tuned against, per this project's own evidence-gate standard for graduation decisions.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 6 permanently applied the rubric fix and re-ran the original live A/B — the real result is nuanced, not a clean win, and is disclosed as such rather than rounded up.** After the checklist-injection isolation test confirmed the root cause, `cto_evaluator.py`'s Checker prompt was permanently updated with the same 5-point checklist `boardroom-cto.md` already uses, and `eval/live_ab_run.py` was re-run for real against the identical 2 scenarios. **Real result** (`eval/.data/live_ab_results.jsonl`, entries 3-4, independently re-read): agreement stayed at 1 of 2 — unchanged from the original run — but the *direction* of the one real disagreement flipped. Pre-fix, the Checker was too lenient on `palindrome-check` (accepted a real bug). Post-fix, it escalated (`NO_GO`, blocked entirely) on a solution the real CTO persona would have shipped with a documented caveat (`GO_WITH_CONCERNS`) — over-correction, not resolution. The checklist demonstrably fixed the specific miss it was built to fix (the isolation test proved that in isolation), but calibrating a rubric-bearing Checker to weigh severity the way a real persona does — not just notice more things, but judge how much they matter — is evidently a harder, still-open problem. Total real spend across the full investigation (original A/B + rubric isolation test + this re-run): $1.756651. Logged honestly as evidence against declaring the gap closed, per this project's own anti-rationalization discipline — a worse-in-a-different-way result is not progress just because it changed. See `docs/status/ARCHITECTURE.md` §12 for the full account.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Logged 2 more pasted-document pitches ("Cognitive Operating System" meta-router/golden-RAG/sandbox blueprint, and a 10-table PostgreSQL+pgvector "Golden Schema") as deferred candidates, then ran a cross-cutting, multi-lens read across the full deferred-candidates list rather than evaluating each row in isolation again.** `docs/roadmap/AGENT-ROSTER.md`'s "Deferred mechanism ideas" table now holds 8 rows, all originating from pasted external documents, none from this project's own dogfooding history. Checked from 4 angles: technical (6 of 8 assume a persistent runtime Wingman deliberately doesn't have), governance (3 of 8 propose softening the any-`NO_GO`-blocks gate rule, each declined as a regression for the founder audience it protects), security (none were prompted by an actual caught vulnerability — every real risk this project has caught came from the existing Boardroom/DoD-gate path on a real diff), and research integrity (a mix of real and at least one fabricated citation already caught in the same source document, reinforcing "verify before trusting a cited number" as standard practice here). The actionable takeaway is the pattern itself, not a new proposal: if a real need for any of these ever surfaces, check first whether it's 3 of these ideas wearing different names rather than a genuinely new problem. See `docs/roadmap/AGENT-ROSTER.md`'s new "Cross-cutting pattern across this whole list" subsection for the full read.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Phase 5's `palindrome-check` disagreement was root-caused to a missing rubric, not a deeper architectural flaw — confirmed with one live, surgical test.** Directly comparing the real, verbatim prompt templates found the actual gap: the shipped `boardroom-cto.md` persona has an explicit 5-point checklist ("Correctness — ... missing edge cases", "Architecture fit", "Test coverage", "Maintainability", "Blast radius"); the new engine's Checker (`cto_evaluator.py`) has none of that — its entire prompt is one generic sentence, "Evaluate this solution strictly against the task," with no definition of what "strictly" means. `eval/checker_rubric_ab.py` tested this cheaply: reusing the exact known-buggy code from Phase 5's `palindrome-check` (no Maker re-run needed), one live call injected the old persona's real checklist into the Checker's prompt. Result: the verdict flipped from accepted to rejected, with the response explicitly naming the same gap the real CTO persona had caught — "Correctness gap: it strips spaces but not other whitespace/punctuation." Cost: $0.097233. **Honest scope of this finding**: N=1 confirming N=1 failing — real evidence the specific miss is a cheap, fixable prompt-rubric gap rather than a fundamental Maker/Checker architecture problem, not proof the gap is fully closed across the board. A disclosed instrumentation bug in the throwaway test script (its JSON-parsing regex hit an "unparseable" fallback path despite the model's response being coherent) did not block reading the real result — the raw response text and `accepted: false` value were both genuine, just routed through the wrong code branch. Natural next step, not yet done: inject the checklist permanently into `cto_evaluator.py` and re-run both original Phase 5 scenarios to confirm agreement holds across a slightly larger sample.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 5 runs the live decision-quality A/B every prior phase deferred — the founder asked for it directly, no separate API key, explicitly for token efficiency — and the real result is a caution, not a green light.** `eval/live_ab_run.py`: 2 real scenarios, `max_iterations=2` (not the default 3) to bound spend, one extra live call per scenario for a CTO-persona-equivalent review. Methodology: `boardroom_engine.py`'s real Maker/Checker loop runs to its real conclusion, and that exact artifact is handed to a single live call framed with the real, unmodified `plugins/wingman/agents/boardroom-cto.md` persona (read from disk at run time) to get its independent verdict on the same artifact — the direct test of "does the new engine reach as good a decision as the shipped plugin's real seat." **Real result** (`eval/.data/live_ab_results.jsonl`, independently re-read, not trusted from the script's own printed summary): on `palindrome-check`, the new engine accepted a solution that strips spaces but not punctuation (`GO_WITH_CONCERNS`) — the real CTO persona correctly rejected it (`NO_GO`), naming the exact gap and demanding a fix plus test cases before shipping. On `simple-email-validation`, both reached a shippable verdict, but for different reasons — the new engine's `GO_WITH_CONCERNS` came from routing uncertainty, not a content concern; the persona said clean `GO`. Total real cost: $0.855020. **Honest conclusion, not rounded up**: 2 scenarios can't establish a rate, but the one real disagreement observed points against readiness — the new engine was wrong in the more dangerous direction (accepting a real bug the shipped plugin's persona caught). This is exactly what the "prove decision-quality first" bar (overridden in Phase 4 to build and register the engine anyway) exists to catch: being built and registered is not the same as being ready to decide anything for a real founder. A real bug in the harness itself was found and fixed mid-run: the original script only wrote results to disk after every scenario succeeded, so `simple-email-validation`'s real, paid-for first attempt crashing on a transient CLI error would have silently discarded `palindrome-check`'s already-spent real result — fixed to write incrementally, one scenario at a time, and the already-spent result was preserved by hand from the crashed run's own printed output rather than re-spending to reproduce it. See `docs/status/ARCHITECTURE.md` §12 for the full account.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 4 builds a real, invokable engineering-review engine — the founder explicitly overrode the "prove decision-quality first" bar Phase 3's zero-cost half had just satisfied, on the strength of the token-compression result alone.** Asked directly which of three scopes "implement fully" meant (finish agnostic-boardroom's own remaining gaps; wire it in to replace the plugin's Boardroom; or run the live decision-quality A/B first) and whether the evidence bar should hold — the founder answered: do both finishing-the-gaps and wiring-in-as-replacement, and explicitly override the evidence bar rather than wait for the live A/B. Built, honestly scoped rather than overclaimed: `agents/boardroom_engine.py` composes `retrieve_memories` + `route_task` + `run_maker_checker_loop` into a real `BoardroomVerdict` (the same Pydantic model already faithfully ported from `.wingman/checkpoints.jsonl`'s schema) — accepted+confident route → clean `GO`; accepted but only via `low_confidence_fallback` → downgraded to `GO_WITH_CONCERNS`, never silently reported clean; escalated → `NO_GO`/`DO NOT SHIP`/`blocks_advancement: true`, the real gate rule unchanged. Two new MCP tools (`route_task_tool`, zero-cost; `run_engineering_review_tool`, real live cost, disclosed in its own docstring) expose it, and a new root-level `.mcp.json` registers the server (renamed `wingman-agnostic-boardroom`) so a real Claude Code session can actually connect — genuinely wired in, not just built and left unregistered. `agents/model_runner.py` also stopped hardcoding `"claude"` as the CLI binary (now `WINGMAN_MODEL_CLI`-configurable) — real progress toward harness-agnosticism, though no second adapter ships, since one unverifiable in this sandbox would itself be a fabricated capability, the exact thing this project's `verification-before-completion` discipline exists to prevent. **The one thing NOT done, stated as plainly as the founder's own override was**: this is one seat's worth of technical judgment (an engineering accept/reject gate), not a replacement for the other 7 (business/security/financial personas have no equivalent here), and `plugins/wingman/commands/adaptive/boardroom.md` — Wingman's real, shipped, founder-facing gate — is deliberately left untouched; cutting that over is a separate, much higher-blast-radius decision than building and registering the engine, and required its own explicit confirmation this pass didn't receive. All new deps (agno, pydantic, mcp, lancedb, fastembed, tiktoken) installed for real in this environment and the fast suite run for real: 51/51 passing (4 new `test_boardroom_engine.py` cases covering all 3 verdict paths plus real seeded-memory read-back against the real 40-skill index). See `docs/status/ARCHITECTURE.md` §12 for the full account.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 3 (decision-quality comparison harness, zero-cost) resolves what the readiness review named as the real open question, and defers the harder half explicitly.** After Phase 2b, a real-metrics readiness review was compiled — every figure traced to a test file or a live-confirmed run, never asserted (skill-context compression measured at 76-78%, matching the blueprint's claimed 60-80% range; the memory MCP server's 500-entry/sub-100ms stress bar passed but its exact p95 was never persisted; a single trivial `claude -p` call confirmed at ~$0.26, but a full Maker/Checker escalation's aggregate cost was never logged anywhere retrievable; and, most importantly, no comparison exists anywhere of this backend's actual decision output against the shipped plugin's real Boardroom on the same task). Presented to the founder as an Artifact, then three concrete questions asked: (1) whether the next milestone should be proving decision-quality against the shipped plugin before anything else — **confirmed, yes**; (2) whether a confirmed improvement should mean complementing or replacing `plugins/wingman/` — **left undecided, explicitly, to be revisited only once real evidence exists**, not resolved speculatively now; (3) how much real live-model spend to authorize for the next round — **none yet**, build and prove the comparison *methodology* on scripted, zero-cost scenarios first. Built `eval/decision_quality.py` (a `Scenario`/`run_scenario`/`run_all` harness running the real, unmodified `run_maker_checker_loop` against a scripted `call_model`, checking whether its actual outcome matches a labeled ground truth — the identical harness runs unmodified against a real `call_model` once live spend is authorized, so proving it now costs nothing to redo later) and `eval/scenarios.py` (4 scenarios, each a genuinely different code path: correct first try, wrong-then-fixed, never-fixed-escalates, and a Checker response that fails JSON parsing — proving the fail-closed path isn't silently treated as acceptance). `tests/test_decision_quality.py` adds the harness's own negative case: a scenario with a deliberately wrong expected outcome, proving the harness can report a real mismatch rather than rubber-stamping everything. All 5 new tests run and pass for real in this environment (agno/pydantic weren't installed here — a pre-existing environment gap unrelated to this phase — but this phase's code has zero external dependencies beyond `agents.loop`, confirmed by running just the deps-light suite: 14/14 pass). **Known, disclosed limitation, not fixed here**: this proves the loop mechanism reaches the expected decision for a scripted exchange; it does not yet compare against a live run of `/wingman:boardroom` on the same task — that needs two real model runs on identical input and stays the deferred next step until the founder authorizes real spend for it. See `docs/status/ARCHITECTURE.md` §12 for the full account.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 2b closes three "built in isolation, never proven together" gaps Phase 2 left behind, systematically, via subagents.** After Phase 2 (PR #95) shipped, a status check flagged three follow-ups the founder confirmed: (1) the memory MCP server had never been run as a live subprocess — only its underlying functions were imported directly in tests; (2) `knowledge/skill_router.py`'s `route_task()` and `agents/loop.py`'s `run_maker_checker_loop()` were never called together, despite `route_task`'s own docstring naming this exact gap ("the Checker will catch a wrong pick" has no real teeth until wired); (3) `.claude/commands/ship-feature.md`'s described chain (memory → routing → loop) had never been exercised end to end. A dispatched `Explore` subagent confirmed all three via direct grep before any code was written; a follow-up `Plan` subagent hit a session-wide rate limit mid-run (not retried, since the wall was session-wide not agent-specific) — the final plan was written directly from the `Explore` findings plus this session's own first-hand knowledge of every file involved. **Two real bugs found while building item 1's real MCP client test** (`mcp.client.stdio` + `ClientSession` spawning `mcp_server/server.py` as a genuine subprocess, not importing its functions): Agno's own `INFO` logging writes to stdout by default, corrupting the stdio transport's JSON-RPC framing (fixed with `logging.disable()`); and running the script directly (`python mcp_server/server.py`) puts `mcp_server/` itself on `sys.path` rather than the repo root, breaking `from db.connection import ...` (fixed by invoking as `python -m mcp_server.server`). New `agents/pipeline.py` wires `route_task()`'s output into the loop's `context` (`run_task_with_routing`) and exercises the full real chain (`run_ship_feature_dry_run`), always surfacing `routing.confidence` rather than swallowing a `low_confidence_fallback` result. Verified with real routing/memory retrieval + a fake, zero-cost `call_model` for the loop side — proving the wiring is real without re-spending the real `claude -p` money `test_loop_live.py` already spent proving the loop's own live behavior. 6 new tests (3 MCP-protocol, 2 wiring, 1 end-to-end), 42/42 fast tests total, full Wingman validator suite reconfirmed green. See `docs/status/ARCHITECTURE.md` §12 for the full account.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **`agnostic-boardroom/` Phase 2 (data substrate, memory MCP server, skill router, loop/graph engineering, an experimental slash command) is complete, closing every open question the Phase 2a clarifying-questions artifact raised.** The founder answered all of them in one detailed reply; each was independently fact-checked by two dispatched subagents (`Explore` confirming exact current file signatures/Wingman conventions, `Plan` designing against the answers) before any code was written — this caught a real technical inaccuracy in the founder's own brief (`disable-model-invocation`/`allowed-tools` frontmatter fields, assumed real, confirmed via repo-wide grep to be used nowhere in Wingman's 40 skills/24 commands and not confirmable as real Claude Code fields at all) and one real design tension worth surfacing rather than silently resolving (the skill router's "proceed on low-confidence match, let the Checker catch it" directive has no actual Checker to catch anything until the loop below exists — flagged loudly in code and docs as a known gap, not swept under the rug). **Database**: SQLite (not Postgres), WAL mode, one table per concern mirroring the 5 existing Pydantic models, every query parameterized, every payload Pydantic-validated on ingest (a schema-invalid payload lands in `threat_register` via `log_schema_deviation`, never silently dropped — directly answering a cited real CVE class in a similar LangGraph SQLite checkpointer). **Memory MCP server**: scoped strictly to `.wingman/memory/*.md`-equivalent content, a 3-tier session/project/org taxonomy, stress-tested to the founder's own exact stated bar (500 entries, sub-100ms reads) rather than an invented enterprise SLA. **Skill router**: whole-skill granularity, a real cosine-similarity retrieval budget — required going one API layer below Agno's high-level `Knowledge.search()` (confirmed to expose no similarity score at all) down to `vector_db.vector_search()`'s raw `_distance` field. **Loop/graph engineering — the biggest resolution**: originally looked blocked on a missing `ANTHROPIC_API_KEY`; the founder's answer was to use the AI model already available in the AI coding agent, via headless `claude -p` subprocess calls (`evals/run-headless.mjs`'s own existing mechanism, verified live in this exact sandbox — real session_id, real cost — before committing to the design). A real, bounded 3-iteration Maker/Checker loop with per-file escalation scope and real logged cost (not estimated), wrapped in the real 7-stage pipeline topology mirrored exactly (a control group against the old plugin). Proven with genuine live model calls (`pytest -m live_model`, real dollar cost, deliberately excluded from the default test run), not mocked — a live Checker really rejected an obviously wrong solution with a real reason, and a full live Maker→Checker pass completed with real, non-zero cost. **Slash command placement — a real correction found by running the validator, not assumed**: the founder's own answer called for `plugins/wingman/commands/experimental/`, but `validate-structure.mjs`'s orphan check treats any unregistered `.md` under `plugins/wingman/commands/` as a hard error; landed instead at `.claude/commands/ship-feature.md` (Claude Code's real project-scoped custom-command mechanism), outside the plugin entirely. 36/36 fast tests pass (0 real cost) plus 2/2 real live-model tests (run explicitly). Full validator suite reconfirmed green after the placement correction. See `docs/status/ARCHITECTURE.md` §12 for the full architectural writeup.

<!-- wingman:log type=decision category=architecture status=in-progress -->
- **A pasted "Enterprise Blueprint: The Agnostic AI Boardroom" proposal — full rewrite of Wingman's core as a standalone Python backend (LangGraph/PydanticAI macro-graph + micro-loops, an MCP server, a vector-store skill router, SQLite/Postgres relational state) — was evaluated, found to directly reverse three already-documented decisions, and the founder explicitly confirmed proceeding anyway as a real, in-progress rewrite.** The conflicts surfaced before building anything: (1) replacing `.wingman/checkpoints.jsonl`/`state.json` with a relational DB reverses `docs/status/DATABASE.md`'s flat-file choice, a real revisited decision (schema_version 1→2→3), not an oversight; (2) the blueprint's implicit configurable-strictness framing (bounded Maker/Checker retries escalating to the founder rather than an unconditional block) brushes against the same "Boardroom strictness must not be a dial" concern a near-identical hybrid-framework proposal was declined for earlier; (3) a vector-store semantic skill router directly reintroduces the local-vector-search idea `docs/status/ARCHITECTURE.md`'s v16 audit entry explicitly dropped for zero evidenced need across this project's entire dogfooding history. None of these conflicts were hidden from the founder — each was named directly, with the specific doc/decision it reverses, via `AskUserQuestion` before any code was written. The founder's answer: build it as the full rewrite, and evaluate **Agno** in place of the blueprint's own suggested LangGraph (a live 2026 web-research comparison found Agno's declarative, minimal, transparent-debugging design closer to this project's own `engineering-minimalism` discipline, and its `AgentOS` FastAPI runtime directly satisfies the blueprint's Pillar III MCP-server requirement with less custom plumbing). **Scoped as additive, not destructive**: the new backend lives in a new top-level `agnostic-boardroom/` directory; `plugins/wingman/` — Wingman's actual shipped, working product — is untouched and keeps operating throughout the build-out; nothing is retired until the replacement is proven end-to-end. Execution is phased (Data & Schema → MCP Server → Maker/Checker micro-loops → Macro-graph topology), tracked via the harness's own task list, not attempted in one pass. Phase 1 (Pydantic state schema, `agnostic-boardroom/core/state_schema.py`, faithfully ported from `docs/status/DATABASE.md`'s and the threat-register/debt-ledger docs' real documented shapes, not invented) is complete: 7 tests pass, including two copied verbatim from `docs/status/DATABASE.md`'s own worked JSON examples to prove the schema is a faithful port. **Phase 2a (skill-context A/B testing, follow-up founder request) is also complete**: the founder asked specifically to A/B test the blueprint's Pillar II claim (vector-retrieved skill context vs. today's whole-`SKILL.md`-in-context) with genuinely purposeful logging, not vanity metrics — chosen over two other candidate A/B axes (model-tier comparison, old-plugin-vs-new-backend) via `AskUserQuestion` since it needed no live model inference to measure honestly. `knowledge/vector_store.py` uses Agno's own `Knowledge` abstraction over an embedded LanceDB table + a local FastEmbed model (no API key) to index the real `plugins/wingman/skills/*/SKILL.md` files; `knowledge/ab_harness.py` logs a real `tiktoken` token count and retrieval latency (reusing Agno's own `PerformanceEval` rather than a hand-rolled timer) per variant, append-only, mirroring `.wingman/checkpoints.jsonl`'s convention. A real tuning finding surfaced during this work: Agno's default 5000-char chunk size barely sub-divided a typical ~10K-char `SKILL.md`, giving near-zero compression until tuned down to 800 chars — confirmed empirically, not assumed. At the tuned size, real measured results landed inside the blueprint's claimed 60-80% token-reduction range (`systematic-debugging` 2211→536 tokens, ~76%; `engineering-minimalism` 2427→545 tokens, ~78%), earned by measurement rather than asserted. This layer deliberately does **not** log a decision-quality-preserved field — verifying that needs a live agent run + Definition-of-Done check, which needs model inference not yet wired (Phase 3). 11 tests pass total (7 Phase 1 + 4 Phase 2a). See `docs/status/ARCHITECTURE.md` §12 for the architectural writeup and `agnostic-boardroom/README.md` for phase status.

<!-- wingman:log type=decision category=platform-conventions status=resolved -->
- **First real installs of OpenCode and Codex CLI in this dev sandbox; genuine, evidence-based upgrade of the OpenCode adapter's verification status.** A founder named a concrete need (using Wingman with OpenCode/Codex specifically) rather than a generic "make it agnostic" request — this is what `evidence-gated-catalog` and prior escalations in this log were waiting for. Installed both real CLIs (`npm install -g opencode-ai` → v1.18.4; `npm install -g @openai/codex` → v0.145.0) as global testing tools, not repo dependencies (no `package.json` created, no change to the zero-dependency invariant). Neither harness has a configured API key/model provider in this sandbox, so live model inference (an actual Boardroom review firing) remains unverified for both — that would need real founder-owned credentials. What was genuinely verified against the live installs: **OpenCode** — `opencode agent list` correctly discovered and typed all 8 `boardroom-*.md` files as `(subagent)`; `opencode debug config`'s resolved output reproduced each file's prompt content verbatim; `boardroom-ceo.md`'s `permission: {edit: deny, bash: deny}` frontmatter is genuinely enforced in the resolved permission engine; `.opencode/plugin/wingman-gate.js` is registered in the resolved `plugin` array, confirming its export shape loads without error; its hook name (`tool.execute.before`) and matched tool name (`plan_exit`) are independently confirmed against real sources (documented OpenCode plugin hook; `anomalyco/opencode#18515` referencing `plan_exit` by name) beyond this project's own prior research. Promoted from `authored, unverified` to a new, honestly-scoped tier, `structurally verified (live install)` — distinct from `built + tested`, since behavioral/inference-level correctness is still unconfirmed. **Codex CLI** — the CLI itself installs and runs (`codex doctor` succeeds structurally), but exposes no config-listing command for custom agents the way OpenCode does, so the `.codex/agents/*.toml` files' actual recognition couldn't be directly confirmed this pass; stays `authored, unverified` at the runtime level. Updated: `plugins/wingman/references/harness-adapters/README.md`, `opencode/README.md`, `opencode/.opencode/agent/boardroom-ceo.md`'s header (the shared reference point for all 8 seat files), `opencode/.opencode/plugin/wingman-gate.js`'s header, `docs/status/ARCHITECTURE.md` §8b.

<!-- wingman:log type=decision category=audit status=resolved -->
- **Post-merge Layer-2 audit + real eval coverage for the org-template wiring, both closed clean.** Two parallel audit passes (per `systematic-auditing`'s discipline — independently re-verified, not trusted from self-report) checked the two most recent merges (AGENTS.md canonicalization, `references/org-template/`) for cross-doc consistency and plain-language/doc-index compliance. Found 2 real, minor issues, both fixed: (1) `plugins/wingman/AGENTS.md` (nested, package-scoped) never mentioned `references/org-template/` — added a parallel section to its existing `harness-adapters/` treatment. (2) `references/org-template/README.md` was a genuine doc-index orphan (not cited by any command/skill, unlike `harness-adapters/README.md`'s citation from `git-pr-workflow`) — fixed by citing it from `discovery.md`'s References section. Everything else audited came back clean (confirmed `plain-language-checkpoint` correctly doesn't apply to this agent-facing reference content, per that skill's own explicit "founder-facing output only" scoping). Separately, ran a real, un-briefed behavioral eval (Run 4 of `evals/cases/discovery.md`): a fresh subagent given a founder ask shaped as an unambiguous "freelancing delivery" project correctly matched `references/org-template/project-types/catalog.md`, consulted `freelancing.md` unprompted (per `discovery.md`'s own instruction, not told to), and produced Discovery output independently verified (real file, not self-report) to contain the playbook's specific guidance (client ship/no-ship authority, scope discipline, clean handoff) — genuinely differentiated from the generic Run 3 scenario, not superficial.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Assessed a pasted "master repo blueprint" (~60-file `core/policies/founder/governance/ai-sdlc/project-types/preferences/skills/adapters/schemas/templates/validators/decisions/docs/tools` org-governance tree) on explicit founder request to build it in full; scoped down to a 10-file reference doc after an audit.** Checked against the real repo before building anything: the blueprint's files have zero consumer (no command/skill/script/CI reads YAML config anywhere in this repo — confirmed by grep; there is no `package.json`, so its `tools/*.ts` layer can't run without introducing a TypeScript toolchain from zero), which would make it dead weight on creation and directly violate `skills/governance/evidence-gated-catalog`'s "no speculative bulk creation" rule. It also has the same shape as a 2026-07-18 external "agent-agnostic transformation" proposal already reviewed and declined (`docs/status/ARCHITECTURE.md` §8a) — building it now would silently reverse that decision rather than extend it. Presented the founder with an explicit options table rather than either silently complying or silently refusing; founder chose: **Scope A** (build only 3 evidenced gaps — a project-type catalog/playbooks, founder cross-project preferences, a capability/skill-gap map — not the full blueprint), **Placement B** (shipped reference template only, no generation command, no `.wingman/org/` instance), **Adapter handling A** (no new adapter files — point at the existing, drift-checked `references/harness-adapters/` instead of duplicating it), **Tools format A** (moot at this scope — no script needed). Built `plugins/wingman/references/org-template/` (README + `project-types/` catalog and 7 playbooks + `founder-preferences.md` + `capability-map.md`), all markdown (not YAML — nothing in this repo parses YAML, confirmed by grep, so markdown is strictly more consistent with every other `references/*.md` file), wired into `commands/pipeline/discovery.md` (project-type consult) and `skills/knowledge/memory` (what's worth persisting to `MEMORY.md` — no new store). Also found and fixed 2 real internal duplications in the blueprint itself (11 `core/*.yaml` files each shadowing a same-named folder; both a `decisions/` folder and a `core/decision-log.md` for the same purpose) and 1 real naming collision (a folder named `skills/` would collide with Claude Code's own skill-discovery convention and Wingman's own `plugins/wingman/skills/`) — neither built, both noted as reasons the full blueprint wasn't shipped as-is even under Scope C.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Studied 4 real community "agent-agnostic" plugin repos on explicit request; adopted one bounded, mechanical drift check, declined the rest.** Fetched and read directly (not assumed from name): the AGENTS.md standard's 5-tier cascade, `alirezarezvani/claude-skills`' 13-tool conversion-script model, `wshobson/agents`' single-source-of-truth + `make generate`/`make garden` (drift/dead-link detection), and `fusengine/harness`'s "pure policy core + thin per-harness adapters" runtime library. Ground-checked against Wingman's own `§8b` harness adapters first: the Codex CLI/OpenCode Boardroom persona files are a one-time hand-authored translation, already labeled `authored, unverified`, with **zero mechanism** to notice if a later edit to the canonical `boardroom-*.md` agents (a new seat, a renamed seat, a model-tier change) silently drifted the adapters out of sync — exactly the risk `wshobson/agents`' `make garden` and `fusengine/harness`'s adapter/policy-core split both independently point at. Built `plugins/wingman/scripts/check-harness-adapter-drift.mjs` — a mechanical, structure-only check (seat coverage, `opus`-tier consistency, VERDICT-block presence), deliberately not a fuzzy prose diff (the adapters intentionally condense the canonical prose) or a full markdown-to-TOML regeneration engine (the exact unverified-breadth pattern already declined for a full 1:1 command/skill port). Wired into `.github/workflows/validate.yml`; verified it actually catches drift by constructing two real failure scenarios (a missing adapter, a mismatched model tier) before confirming it passes clean on the real repo state. Declined, same evidence-gate reasoning as every prior agent-agnostic-transformation pitch this project has assessed (`docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table): a full `wshobson`-style generation pipeline for all 24 commands/40 skills, the AGENTS.md standard's `llms.txt`+symlink cascade, and a `fusengine`-style generalized runtime adapter framework — none have real, evidenced demand behind them beyond "the community does it this way." See `docs/status/ARCHITECTURE.md` §8c for the full writeup and `ATTRIBUTIONS.md` for the new `wshobson/agents` credit + non-vendored `fusengine/harness` citation.

<!-- wingman:log type=decision category=audit status=resolved -->
- **A 5-concern-area parallel `/wingman:audit` pass across the whole repo found and fixed 2 real bugs, deferred 2 minor ones, and confirmed 3 areas fully clean.** Scoped per `systematic-auditing`'s discipline: architecture/doc consistency, executable-code/security correctness, CI workflow accuracy, pipeline command cross-references, and eval/test-coverage accuracy — each a separate parallel subagent with an exact file list, every finding independently re-verified against the real filesystem/execution before being trusted. Architecture/docs, CI workflows, and (after one fix) pipeline cross-references all came back clean or were fixed trivially. Two real, independently-reproduced bugs, both fixed: (1) `okf-export.mjs`'s `assertSafeToWipe` guard only refused the filesystem root and home directory exactly, so `--out` equal to (or an ancestor of) `--project-dir` silently wiped the entire project via `rmSync` before writing the bundle — reproduced directly against a scratch project (a real file was deleted) before fixing; now also refuses when `--out` resolves to the project directory or any of its ancestors. (2) `secret-guard.mjs`'s Anthropic-key pattern (`sk-[A-Za-z0-9]{20,}`) never matched a real Anthropic key's actual hyphen-delimited shape (`sk-ant-api03-<...>-<...>`) despite its own comment claiming coverage — verified with a constructed real-shaped key before fixing; added a dedicated pattern. One small doc fix: 4 pipeline command files (`discovery.md`, `define.md`, `architecture.md`, `uxflow.md`) said "Nth of Wingman's 7 planning stages" when only 5 of the 7 total pipeline stages are planning stages (build/ship are execution, not planning) — `implementation-planning.md` already correctly said 5; corrected the other four. One stale-number fix: `docs/status/PROJECT.md`'s own "live count" pointer (this doc, ironically) had drifted within 4 days (said 68 cases/68 verified/0 provisional "as of 2026-07-18"; live was 70/54/16) — corrected and reworded to stop implying a snapshot number would stay current. Two findings deliberately deferred rather than churn-fixed: `session-start.mjs` never reads its own stdin JSON (so it can't honor `input.cwd` the way sibling hooks do) — fixing this is a real design question (does `SessionStart`'s stdin even carry a `cwd` field?), not a one-line fix; and `context-monitor.mjs`/`session-health.mjs`'s `SKIP_TOOLS` anti-recursion check is dead code (compares against hook filenames that `tool_name` can never equal) but causes no wrong behavior today. All fixes covered by new regression tests (5 new: 1 for the Anthropic-key gap, 3 for the wipe-guard, using the exact reproduction scenario); full suite 103/103 passing, all 4 repo validators green.

<!-- wingman:log type=decision category=dogfooding status=resolved -->
- **First genuinely interactive founder-mode dogfood run closes the last "real dogfooding" gap; fixed 2 real bugs it found in `dod-structural-gate.mjs`.** Every prior dogfood run was headless (`claude -p`, where `AskUserQuestion` doesn't exist); a real human answered real `AskUserQuestion` calls at Discovery, the Planning Milestone checkpoint, and the Build stage's Definition-of-Done gate (declining to accept a risk with zero mitigation, forcing an actual code fix rather than a documentation-only acceptance) against a throwaway URL-shortener project, all the way through a genuinely clean Ship-stage push gate. This also exercised the real `dod-structural-gate.mjs` push gate against live, evolving project state for the first time (previously only against synthetic eval fixtures) and found two real bugs, both fixed upstream: (1) the threat-register check matched only the literal substring "OPEN," so any other status word — including an honestly-wrong one ("PENDING") — silently passed; replaced with a real, section-scoped table parse that flags anything other than exactly `CLOSED`. (2) `checkTestPresence` only recognized a test file matching the source file's own basename, wrongly flagging a file with full, real coverage split across several behavior-named test files; added a content-based fallback that checks whether any test file actually imports the source module. Both confirmed against the real dogfood project before being generalized into new unit tests (7 new, 78 total passing). A third suspected bug (an orphaned `UX-001` marker) did not hold up on inspection of the real template — logged as a dogfooding-agent authoring mistake, not a Wingman defect, rather than silently dropped. See `docs/history/retros.md`'s two 2026-07-21 entries and `evals/cases/dod-structural-gate.md`'s Run 4 for full detail; `docs/HUMAN-TODOS.md`'s "Real dogfooding" item is now `status=resolved`.

<!-- wingman:log type=decision category=tooling-proposal status=resolved -->
- **Assessed a "deep research report" proposing a 5-item enhancement roadmap (context compression, parallel agent orchestration, a prompt regression suite, self-verification, semantic caching), citing academic papers — verified citations and architecture before acting, not assumed.** Citation check (direct web search, not trusted at face value): most cited papers are real — IC-Former (arXiv:2406.13618, context compression via a trained cross-attention module), the Biderman reproducibility paper (arXiv:2405.14782), VectorQ (arXiv:2502.03771, semantic-cache dynamic thresholds), a real freshness-aware-caching paper (arXiv:2607.04281) though its specific "97-98%"/"0.1-3.3%" figures weren't confirmable, and a real parallel-multi-agent paper (arXiv:2507.08944) whose actual result (2.2× speedup) is far more modest than the report's vague "achieves up to *speedup*" framing implies. One citation — "Kumar et al. 2025" on self-verification, with suspiciously precise FEVER/TruthfulQA numbers — returned no matching source on direct search: likely fabricated, the same pattern already caught in prior pasted reports this session (mindwalk, the Grok specialist-team report, the MLOps benchmark proposal). **Verdict per item, all ground-checked against the real repo:** Context Compressor and Semantic Cache both **declined** — both require a live, persistent runtime (a hosted neural module; a request-serving loop to cache against) that `docs/status/ARCHITECTURE.md` §2 explicitly rules out and CI (`install-smoke.yml`) enforces zero dependencies against — the identical disqualifying reason the earlier MLOps benchmark proposal was declined; logged as 2 new rows in `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table. Parallel Agent Runner and Self-Verification are both **already built and already in daily use** — `commands/adaptive/boardroom.md`'s 7/8-seat parallel dispatch and `skills/discipline/systematic-auditing`'s "one subagent per concern, in parallel" pattern (the same pattern this session's own 8-domain audit used); 5 dedicated self-verification skills (`verification-before-completion`, `anti-rationalization`, `doubt-driven-development`, `evidence-gated-catalog`, `verification-loop`), all `verified`-eval'd and already scored as `covered-measured` in `docs/status/AGENT-WEAKNESS-BENCHMARK.md` (built earlier this session) — nothing to add. Prompt Regression Suite is the one item with a genuinely narrow, real, not-fully-redundant gap: `evals/` already does behavioral regression testing of skill/command changes, but nothing automatically flags when a changed command/skill/agent has no eval case covering the changed section. Built `skills/knowledge/prompt-diff-check` to close exactly that gap, wired into `/wingman:harness`'s self-audit — deliberately not a parallel "regression suite" system, since that would duplicate `evals/` itself.

<!-- wingman:log type=decision category=quality status=resolved -->
- **Built the agent-weakness coverage benchmark (`docs/status/AGENT-WEAKNESS-BENCHMARK.md` + `wingman-metrics.mjs` §5), on founder request to "encode community-verified coding-agent weaknesses into measurable rules" with "a benchmark and A/B testing."** Ground-checked first: the eval harness *already is* the A/B test (`evals/README.md`'s `verified` level requires a positive + negative scenario pair), and most known agent weaknesses are *already* encoded as skills (`verification-before-completion`, `anti-rationalization`, `engineering-minimalism`, `systematic-debugging`, `token-economy`, `doubt-driven-development`, `evidence-gated-catalog`, `verification-loop`) — all with `verified` evals. The genuine gap was that no single catalog mapped "known weakness → the rule → the eval that measures it," and coverage was invisible. Deliberately **not** an MLOps/performance benchmark (p95/throughput/cost-function) — that shape was already assessed and declined this session (Wingman has no runtime to instrument; fabricating those numbers is what `verification-before-completion` exists to prevent). Built instead a coverage benchmark: a catalog of 12 community-verified failure modes (each cited to real sources — MAST arXiv:2503.13657, the code-spec-verification study arXiv:2508.12358, industry writeups, and this repo's own eval/retro evidence), scored by a new §5 in `wingman-metrics.mjs` that **re-derives** each entry's status from the real rule/eval files rather than trusting the catalog's hand-written status (so it can't silently rot or overstate — verified by deliberately breaking a reference and confirming it flags a broken-ref + status-mismatch). Current honest coverage: 11/12 have a rule (92%), 10/12 measured by a `verified` eval (83%), 1 uncovered (read-before-edit discipline, W12) flagged as an evidence-gated candidate, not built speculatively; 1 covered-unmeasured (API hallucination, W11). Dev-repo-only (catalog in `docs/`, metric in `scripts/`) — no `plugins/wingman/` change, so no `plugin.json` bump and no `CHANGELOG.md` entry (CHANGELOG is plugin-scoped); same tooling class as `wingman-metrics.mjs` itself.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Built `/wingman:knowledge-export` (Google Open Knowledge Format v0.1 bundle export), on explicit founder request following an OKF feasibility assessment.** Verified OKF is real (Google Cloud, announced June 2026 — a directory-of-markdown-with-YAML-frontmatter spec, `github.com/GoogleCloudPlatform/knowledge-catalog`) rather than trusting the name, then mapped it against the real repo: nothing existing is OKF-shaped (skill/agent frontmatter is a different, already-enforced 2-field convention; `checkpoints.jsonl` is JSONL; the `wingman:log` marker convention is inline HTML-comment markers, not one-frontmatter-per-file) and none of that needed to change. Not speculative — the founder asked for it directly, and it answers a gap `docs/status/DATABASE.md` already named explicitly: no file unifies `checkpoints.jsonl`, `state.json`, `traceability.json`, and `memory/*.md` into one "what has this project decided and why" surface. Scoped narrowly to checkpoints + memory (not `state.json`/`traceability.json`, which are plumbing — a stage pointer and an ID counter — not founder-meaningful knowledge). New shipped script `plugins/wingman/scripts/okf-export.mjs` (dependency-free, read-only on source data — verified via `md5sum` before/after — runnable standalone with zero Claude Code involvement, same pattern as `dod-pre-push-check.mjs`) converts `.wingman/checkpoints.jsonl` and `memory/*.md` into a bundle at `.wingman/okf-export/`: markdown concept files with light YAML frontmatter plus OKF's two reserved filenames (`index.md`, `log.md`), so a founder can point a non-Wingman AI tool (Gemini, a ChatGPT connector) at their own project's decision history with no Wingman-specific reader. New command `commands/adaptive/knowledge-export.md`, opt-in only, never runs automatically. `docs/status/ARCHITECTURE.md` §8a gained a new paragraph distinguishing this "knowledge/output portability" from the execution portability the rest of that section discusses. New eval case `evals/cases/knowledge-export.md`, run for real against a synthetic fixture during implementation (not just authored) — confirmed faithful rendering of a rejected `DO NOT SHIP`/`NO_GO` checkpoint, correct handling of a genuinely-missing memory file (no fabricated concept file, no crash), and re-run safety. `plugin.json` bumped `0.5.18` → `0.5.19`.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Re-assessed the "make Wingman AI-agnostic / restructure the repo" request; re-declined the physical restructure, added a doc-only repository zone map to `AGENTS.md` instead.** A carefully-framed re-request (make the repo AI-agnostic, modular, scalable, separate product/runtime/shared-lib/prompt/governance/test/doc zones) was checked against the real tree before acting. Finding: the repo is *already* zoned along those lines, and the physical-restructure version (flatten `plugins/wingman/` to root, per-harness `dist/` bundles, `.lsp.json`, `renames-map.json`, automated promotion) was already assessed and declined on 2026-07-18 — logged in `docs/status/ARCHITECTURE.md` §8a's external-review checkpoint and `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table — with most of its claimed "gaps" found factually wrong. Several zones the generic ask names (product logic, runtime logic, shared libraries) don't exist here because Wingman has no application or persistent runtime (§2); creating those folders would invent empty structure. The one real, cheap gap was navigation: no single whole-repo map of which zone lives where and what ships vs. is dev-repo-only. Fixed doc-only, on the founder's explicit scope choice — a compact "Repository map" table added to `AGENTS.md` (the file non-Claude harnesses look for), plus a pointer from `CLAUDE.md`; the map states the two boundaries the prior external report got wrong (shipped `plugins/wingman/scripts/` vs. dev-only root `scripts/`; the standard two-manifest `marketplace.json`+`plugin.json` split, not duplication). Zero files moved, nothing decoupled, no `plugins/wingman/` change (so no version bump) — reaffirming §8a's standing evidence-gate decision rather than reopening it.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Formalized the `ask_founder` port in `docs/status/ARCHITECTURE.md` §8a, on explicit request — naming only, no new mechanism.** Of the 3 Claude-Code coupling points §8a already named (`AskUserQuestion`, `ExitPlanMode`+hooks, `Task`/`Agent` dispatch), only `ask_founder` was formalized: `ask_founder(question, options) → decision ∈ {ship_it, fix_concerns_first, still_reviewing}`, with its two adapters (the `AskUserQuestion` tool call; the `still_reviewing` checkpoint fallback `commands/adaptive/boardroom.md`'s "Ask for the decision" step already writes when no interactive answer is obtainable) both already-shipped and unchanged — the port declaration just states the contract that already existed implicitly in `boardroom.md`'s prose. Deliberately scoped to this one port, not all three: no evidence exists yet to justify porting `gate_exit` or `dispatch_parallel`, and doing so speculatively would repeat the exact premature-abstraction pattern this section already declines twice over (the "Grok"/agent-agnostic-transformation and mindwalk proposals, both logged in `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table).

<!-- wingman:log type=decision category=tooling-proposal status=resolved -->
- **Studied `cosmtrek/mindwalk` on request; declined runtime integration, logged as a deferred idea.** Verified the repo directly (GitHub API + raw README fetch) rather than guessing from the name: a real, active project (849 stars, MIT, v0.3.0) — a Go backend + React/Three.js frontend that replays a Claude Code/Codex session log as a 3D "night map" of the repo, files the agent explored/read/edited lighting up. Running or depending on it fails the identical, already-documented reason LangGraph/smolagents were rejected as a runtime provider: it's a separate hosted service, which "would turn Wingman into 'a plugin + a product' instead of just a plugin." Vendoring it as design-inspiration-only (the legitimate path per `ATTRIBUTIONS.md`) also isn't warranted yet — no dogfood run or retro has ever logged "can't tell what an agent actually touched during a run" as real, repeated friction, and none of the 3 prior externally-proposed frameworks logged in `docs/roadmap/AGENT-ROSTER.md`'s "Deferred mechanism ideas" table were vendored either, for the same reason: nothing shipped has adopted the idea yet. Logged as a 4th row in that table, including the idea's honest kernel (a lightweight text-based session-footprint summary from already-collected `checkpoints.jsonl`/dogfood-run JSON would need no new dependency if the friction is ever real) and the concrete revisit condition.

<!-- wingman:log type=decision category=audit status=resolved -->
- **A 5-domain × 5-persona parallel audit of `main` (post-PR-#47) found and fixed 5 real defects, confirmed 2 areas fully clean.** Reused existing machinery rather than inventing a new process: `skills/discipline/systematic-auditing`'s concern-area/parallel-dispatch/independent-verification method, paired one Boardroom persona lens per domain. Architecture (CTO lens) and Security/policy (CISO lens) came back clean — both mechanical validators PASS, no stale renamed-agent/renamed-command references, all 10 `hooks.json` entries correctly wired, `secret-guard.mjs`/`boardroom-checkpoint.mjs` both already fixed from earlier session bugs. Real findings, all independently re-verified against live command/file output before fixing (never trusted a subagent's self-report): (1) **`ATTRIBUTIONS.md` overstated vendored-repo count as 20 when only 16 are real pinned submodules** — 4 entries (`alirezarezvani/claude-skills`, `jeremylongshore/claude-code-plugins-plus-skills`, `ComposioHQ/awesome-claude-skills`, `avelikiy/great_cto`) were declared in `.gitmodules` but never actually committed (no gitlink, no directory on disk), while `docs/status/ARCHITECTURE.md` already correctly said 16 — fixed by removing the 4 dead `.gitmodules` declarations and moving their real design-inspiration credit into `ATTRIBUTIONS.md`'s existing "Non-vendored attribution" table (same pattern already used for `fivetaku/fablize`), correcting `docs/HUMAN-TODOS.md`'s repeated "20" claim. (2) **5 eval cases (`bloat-audit`, `debt-ledger`, `over-engineering-review`, `ponytail-debt-harvesting`, `platform-native-reference`) were marked `verified` with no run log at all** — templated, unexecuted "passed a real positive scenario" prose with no dated run section, contradicting this project's own verified bar (2+ differently-shaped scenarios including a negative case); corrected to `authored, pending first run`, matching the exact precedent phrase already used elsewhere in this doc for the same honest-correction move. (3) `docs/roadmap/AGENT-ROSTER.md` called `/wingman:launch` "planned" when `commands/adaptive/launch.md` has existed for a while — one-word fix, `specialist-catalog.md`'s copy was already correct. (4) `docs/status/PROJECT.md`'s own "Known open items" eval-count paragraph was stale at "25 total" against a live count of 68 — corrected to point at `wingman-health.mjs` as the source of truth instead of a hand-maintained number, per this doc's own repeated drift lesson. (5) `codeql.yml`'s header comment claimed path-scoped analysis with no `paths:` filter present (over-scans, not a security gap, just an inaccurate comment) — corrected. All 4 mandatory validators re-verified green after every fix.

<!-- wingman:log type=decision category=tooling-proposal status=resolved -->
- **Declined a pasted "hybrid local/edge/cloud + MLOps benchmark" architecture proposal; built `scripts/wingman-metrics.mjs` instead.** A report proposed a service-tier architecture (local/edge/cloud caching, an event bus, TLS session fingerprinting, policy-as-code CI/CD) plus an MLOps-style benchmark suite (p95/p99 latency, throughput, cache hit rate, CPU/IOPS efficiency, a `J = αL+βC+γE+δR` cost function) framed as "spec-driven TDD" needed to cover Wingman's gaps. Ground-checked before designing anything (a dedicated Explore pass, not assumed): spec-driven TDD already exists and is enforced (`skills/mechanics/spec-handler`, `skills/discipline/test-driven-development`'s strict red-green-refactor, `skills/governance/definition-of-done` + `references/definition-of-done.md`'s test/coverage/type-check gates) — nothing new needed there. The proposed architecture assumes a deployed, running service; `docs/status/ARCHITECTURE.md` §2 states plainly Wingman has no persistent runtime, and `docs/status/DATABASE.md`'s "Why no server yet" section already declined adding any server component absent real evidence. A repo-wide search for MLOps/benchmark/p95/cache-hit-rate/event-bus/TLS-fingerprint terms returned zero prior content, and the one near-miss (`platform-native-reference/SKILL.md`) explicitly recommends *against* a custom event bus. Asked the user to clarify intent; they confirmed wanting efficiency/cost/quality *understanding* for Wingman itself, not the service-benchmark suite verbatim. Built `scripts/wingman-metrics.mjs` instead — four real, computable signals from data already in this repo (Boardroom seat model-tier cost shape, eval-suite verified/provisional ratio, `DEBT.md` ceiling rate when one exists, occurrence-threshold visibility), explicitly not fabricating latency/throughput numbers for a system with no requests to measure. All 4 numbers independently hand-verified against direct `grep`/existing-script output before shipping.

<!-- wingman:log type=decision category=governance status=resolved -->
- **Mechanical, low-risk fixes with existing direct test coverage don't require a retroactive Boardroom review.** After `docs/history/architecture-audit-2026-07-15.md` found 4 Proven, high-confidence findings (rule-text duplication, a DoD push-time governance gap, missing agent-agnosticism scoping, a stale `DATABASE.md` schema), each was implemented directly rather than routed through a full 7-seat Boardroom review first — all 4 are documentation edits or a small, directly-unit-tested hook function, none touch founder-facing behavior or introduce new risk surface, and each already carries its own direct verification (validator suite, `node --test`, or a manual re-check against real files). Given an explicit user priority on token efficiency, spending a full Boardroom dispatch (7 subagent calls) to re-confirm work that's already mechanically verified would be the kind of speculative-ceremony spend `engineering-minimalism` argues against — Boardroom review is for judgment calls and architecturally significant changes, not bookkeeping-shaped fixes with a deterministic pass/fail already in hand. This is a precedent for future similarly-shaped fixes, not a one-off excuse: a change still routes through Boardroom whenever it touches founder-facing pipeline behavior, introduces a new dependency, or lacks its own direct verification.

<!-- wingman:log type=decision category=tooling-proposal status=resolved -->
- **Closed all 3 gaps the Boardroom's pnpm review named — `package-manager-selection` skill wired into `build.md`.** The corepack-pinned-install requirement (CISO), automatic-npm-fallback requirement (CPO/CTO), and the reuse-not-reinvent requirement (Research) are all addressed directly: the new skill defaults a genuinely new Node/JS project to pnpm only the first time it needs a package manager at all (never overriding an existing lock file), pins an exact version via `corepack` rather than a bare package-manager name, and falls back to npm silently if pnpm/corepack fails for any reason. Design adapted (not copied) from `vendor/ecc`'s (`affaan-m/ECC`) `scripts/lib/package-manager.js` — its detection-priority ordering and its explicit warning against spawning child processes to probe for installed package managers on hot paths — see `ATTRIBUTIONS.md`'s new per-file entry. `plugin.json` skills count updated; full validator suite + `node --test` re-verified green.

<!-- wingman:log type=decision category=governance status=resolved -->
- **Real 7-seat Boardroom review of the structured-logging layer + the idle pnpm proposal — logging ships, pnpm design stays open pending 3 concrete additions.** Dispatched all 7 seats in parallel (real `Agent`-tool dispatch, independent, no synthetic verdicts) against `docs/history/enhancement-review-2026-07-15.md`. Consolidated: no `NO_GO` from any seat → `GO WITH CHANGES` per `boardroom.md`'s own gate rule. The structured `wingman:log` marker layer (backfilled onto all 70 existing entries in `LEARNINGS.md`/`retros.md`/this decisions log/`HUMAN-TODOS.md`, plus `scripts/parse-wingman-logs.mjs`) shipped as reviewed — CISO/CFO/Research found no real gap; CTO found one genuine bug (`parseFields`'s field regex silently truncated a `category` value containing a space) and it was fixed directly (quoted-value support added, `learn.md`/`retro.md` updated to document quoting), independently re-verified rather than just trusting the seat's report. The **pnpm-for-generated-projects** proposal (previously logged below as an unresolved, never-decided-on proposal) is *not* wired into `build.md` yet, on the Boardroom's own explicit condition — 3 concrete gaps converged across CEO/CPO/CTO/CISO/Research: (1) no detect-or-automatic-fallback-to-npm behavior specified if a founder's machine lacks pnpm or a pnpm-specific install fails (CPO, CTO); (2) no corepack-pinned install method specified, meaning an unpinned global install could become a supply-chain surface (CISO); (3) `vendor/ecc` already ships a solved version of this exact detection problem (`commands/setup-pm.md` + `scripts/lib/package-manager.js`) that wasn't consulted before drafting the proposal (Research) — reuse or explicit divergence-reasoning is required before implementing, not a from-scratch design. Status stays `open` in the entry below until a follow-up design pass closes these three; this is real progress on a previously-idle item (a genuine review happened, converging concerns were named precisely), not a final ship decision.

<!-- wingman:log type=decision category=evolve status=resolved occurrence=2 -->
- **Fixed a genuine 2-occurrence pipeline gap: no stage creates a feature branch before `ship.md`'s preflight catches it after the fact.** A sandboxed-simulation pipeline run (a Manifest V3 browser extension fixture, explicitly logged as simulation, not real dogfooding — see `docs/HUMAN-TODOS.md`'s real-vs-simulated distinction) independently reproduced a gap `full-pipeline-e2e.md`'s Run 2 had already self-caught once: `plan.md`/`build.md`/`secure.md` have no step that creates or checks out a feature branch, so 3 stages' worth of commits land on the default branch before `ship.md`'s preflight check 3 notices — by which point "offer to create one now" is cosmetic unless the default branch is also rewound. Two independent occurrences clears this project's own evolve bar (2+ genuine, differently-shaped occurrences), so this was fixed directly rather than left as a one-off note: `build.md`'s "Before starting" step now checks out (or creates) a feature branch before the first commit; `ship.md`'s check 3 wording updated to describe itself as a confirmation of that earlier step, not the first line of defense. `validate-structure.mjs`, `check-repo-consistency.mjs`, `check-fixtures.mjs`, and the full `node --test` suite (114/114) all re-verified clean after the change.

<!-- wingman:log type=decision category=eval status=resolved -->
- **Closed 15 of `wingman-health.mjs`'s flagged eval-coverage gaps via a parallel agent swarm; `audit` correctly identified as already covered.** Dispatched 5 agents in isolated worktrees: one investigative simulated-founder pipeline run (explicitly logged as sandboxed simulation, never as real dogfooding — see `docs/HUMAN-TODOS.md`'s real-vs-simulated distinction) and 4 batches authoring dedicated cases for `plan`/`build`/`secure`/`ship` and 11 previously-uncovered skills. All 5 agents hit an account-wide API session-limit mid-run; rather than re-spawn agents (token cost), their partial untracked worktree output was salvaged directly from disk, verified, and completed by hand. 4 cases (`definition-of-done`, `doc-index`, `security-checklist`, `testing-patterns`) had already been run and independently graded before the agent died — those run logs are real and kept as-is (spot-checked `testing-patterns`' fixture directly to confirm the seeded gaps are genuine). One batch had written `provisional` "passed a single-scenario run" trust-level prose for 4 cases whose run log actually said "pending" (`doubt-driven-development`, `evidence-gated-catalog`, `interview-one-question-at-a-time`, `spec-handler`) — corrected to an honest `authored, pending first run` state before it could ship as an unverified claim. The remaining 7 (the 4 pipeline commands + `writing-plans`/`systematic-debugging`/`anti-rationalization`) had fixtures but no case docs; authored those directly, same honest pending-run status. `audit` was NOT given a new case: `evals/cases/systematic-auditing.md` already behaviorally tests `commands/adaptive/audit.md` directly — `wingman-health.mjs`'s "commands with no dedicated case" heuristic just matches by filename == command name, so it still lists `audit` as a false-positive gap; noting this here rather than modifying the heuristic script speculatively. Eval suite: 25 → 40 cases. All 3 validators + full `node --test` suite (114/114 post-rebase onto current `main`) verified clean.

<!-- wingman:log type=decision category=hooks status=resolved -->
- **Vendored the six pattern sets (v8–v12) and merged as PR #12 (2026-07-13); an audit then caught a critical over-block bug.** Completed the build-out: ponytail minimalism/debt tooling + `session-start.mjs` hook (v8); the gstack `EXIT PLAN MODE GATE` section-enforcement and gsd-plugin threat register in `secure.md` (v9); the `doc-index` discipline skill (v10); the 6 missing behavioral eval cases bringing the suite to 25 (v11); and a maintenance pass rephrasing 5 skill descriptions to `Use when…`, adding the missing `## Verification` section, and driving `validate-structure.mjs` to zero warnings (v12). `plugin.json` bumped to `0.1.2` (the `version-gate` requires it). After merge, a `systematic-auditing` pass (4 parallel file-scoped subagents) found `boardroom-checkpoint.mjs` was **over-blocking every `ExitPlanMode`** — it validated the 7 required report sections against *every* source including inline plan text (which has no section headers), so the `isApprovedCheckpoint` predicate always saw missing sections and denied exit. Fixed so each source is judged independently and an approved checkpoint requires the marker + `ship it` + all 7 sections; the inline-only path is now an explicit non-approved (not missing-sections) state. Added a regression test in `tests/hooks-integration/hooks-integration.test.mjs`; full suite now **85/85**, `validate-structure` and `check-repo-consistency` both PASS. Created `CHANGELOG.md` (v0.1.2) and `ANNOUNCEMENT.md` (release draft). Attempted to set `main` branch protection via API (require `validate`/`actionlint`/`install-smoke`/`version-gate`) — GitHub returned 404 (this token can't write branch protection), so it remains a web-UI action logged in `docs/HUMAN-TODOS.md`. `ANTHROPIC_API_KEY` repo secret still pending the token (blocks real eval grading + `@claude` review).

<!-- wingman:log type=decision category=eval status=resolved -->
- **Surveyed all 66 workflow files across the 16 vendored repos and applied the transferable CI/CD patterns to Wingman's shape — solving two issues this project actually hit.** The single-workflow CI (just the two validators) was missing patterns the gold-standard plugin repos use. Adopted, each tied to a real vendor precedent: (1) **`install-smoke.yml`** — a real `claude plugin install` end-to-end (from `addyosmani-agent-skills`, plus `gsd-plugin`'s clean-box "no node_modules" assertion), made a **blocking** required check; this is the one thing that would have caught PR #4's three install-breaking manifest bugs, none of which a structural validator could see. (2) **`version-gate.yml`** — fail a PR that changes shipped `plugins/wingman/**` content without bumping `plugin.json` `version` (from `awesome-claude-code-subagents`, simplified since Wingman's `marketplace.json` carries no version to cross-sync); directly closes PR #4's "`claude plugin update` silently no-ops on an unbumped version" bug. (3) **`actionlint.yml`** — lint the workflows themselves (from `gstack`). (4) **Supply-chain hardening** — every action pinned to a full commit SHA (sourced from the SHAs the vendors already vet) with `persist-credentials: false` (from `wshobson-agents`/`ecc`); `validate.yml` was on floating `@v4` tags. (5) **`scripts/check-fixtures.mjs`** in `validate.yml` — the deterministic slice of the eval harness (every fixture executes and produces a git project), verified to fail on a broken fixture and pass on the real 17. (6) **`evals.yml` + `evals/run-headless.mjs`** — the behavioral eval tier via `claude -p`, weekly/dispatch-only (never per-PR, the `gstack` gate/periodic split) to bound token cost; it *captures* transcripts for human grading, doesn't assert a pass, keeping grading independent per this project's core discipline. The runner degrades to a keyless dry-run that also cross-checks every case references a real fixture. (7) **`claude.yml` + `claude-code-review.yml`** — the official `anthropics/claude-code-action`, gated exactly as `wshobson-agents` does (author_association for the write-scoped responder; same-repo-only + non-draft for the reviewer — the load-bearing fork-safety pattern), pinned SHA, minimal allowlists, unified on a single `ANTHROPIC_API_KEY` secret. **User chose "both optional features" + a blocking install-smoke.** Verified locally: `actionlint` (built from source) clean on all 7 workflows, all three validators + fixtures PASS, the version-gate fail/pass branches simulated against a synthetic git history, the eval runner's integrity check confirmed across all 10 cases. **Documented reversal:** this replaces `evals/README.md`'s "Why this isn't automated CI" stance with the honest two-tier split (deterministic gate on PRs; stochastic tier scheduled + human-graded) — recorded because the old blanket "no CI evals" claim is no longer accurate. Two human follow-ups logged in `docs/HUMAN-TODOS.md`: add the `ANTHROPIC_API_KEY` secret, and add `validate`/`actionlint`/`install-smoke`/`version-gate` as required checks under branch protection.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Fixed the audit-flagged safety-gate false-deny in `boardroom-checkpoint.mjs` — and made the gate stricter on rejections while doing it.** The `ExitPlanMode` gate read the inline `plan` text for the checkpoint marker and only fell back to the plan *file* when inline was empty, so a genuine "ship it" checkpoint that `/wingman:boardroom` had appended to the file got ignored whenever `ExitPlanMode` supplied inline plan text — a *false denial* (fail-safe, but real friction: the founder would be blocked from exiting an already-approved plan). Fix: the gate now gathers both sources (inline text + most-recent plan file), treats each as a self-contained checkpoint record (marker + bottom line + founder decision written together as one unit by `boardroom.md`), and opens only if *some* source `isApprovedCheckpoint` (marker present, no "DO NOT SHIP", `Founder decision: ship it`). While fixing it, closed a hole the naive "allow if either is approved" version would have opened: a **global "DO NOT SHIP" veto** now runs first, so an explicit rejection in *either* source blocks exit even if a stale approval sits in the other — an explicit rejection is the strongest signal and the gate fails safe on it. The header comment (which described file-marker enforcement) now matches the actual both-sources behavior. Verified by piping simulated hook input through the real script across all original paths (no-marker→deny, inline-ship-it→allow, file-ship-it+inline-unmarked→**allow, the bug**, DO NOT SHIP→deny, still-reviewing→deny, untouched→allow, non-ExitPlanMode→pass-through, malformed→fail-closed-deny) plus adversarial cross-source cases (inline DO NOT SHIP + stale file ship-it→deny via the veto; inline ship-it + stale file still-reviewing→allow). `validate-structure.mjs` still PASS. Not adding a dedicated eval case: the gate is now exhaustively covered by these deterministic piped-input checks (the same method `boardroom-gate-rule.md` uses), and it's pure string-predicate code with no model-behavior surface an eval would add signal on.
<!-- wingman:log type=decision category=audit status=resolved -->
- **A 5-concern parallel audit (exec-code, manifest, commands+agents, skills, docs+evals) found and fixed 7 real defects across content, latent code bugs, and doc drift — and the "docs/ doesn't ship" bug class recurred, which is the real lesson.** Ran the `systematic-auditing` skill against the whole repo: one scoped subagent per concern, exact file lists, every finding independently re-verified against the real file/execution before acting. Manifest/install came back fully clean (the three historical install-breaking bugs are gone and stay gone). Real fixes: (1) `boardroom.md`'s consolidation step said "the four reports"/"four separate verdicts" when there are five seats — the cost seat was added after that prose and the line never got reconciled, so an orchestrator following it literally could have dropped the cost verdict from the founder summary; (2) `validate-structure.mjs`'s frontmatter parser kept surrounding quotes, so `model: "opus"` would have failed the exact-string opus-tier check — a latent CI-breaker (both agent files currently use unquoted values, so it never fired) fixed by stripping one quote layer at parse time, verified both ways; (3) `department-lead-activation/SKILL.md` step 3 still sourced remit/checklist detail operationally from `docs/status/ARCHITECTURE.md` §5 — the exact runtime-dependency-on-a-non-shipped-doc class this project has hit twice before (`evolve-promotion`, the earlier `boardroom.md`/DATABASE.md fix) — reworded to be self-contained; (4) the two bundled agent templates (`department-lead-activation/references/template.md`, `evolve-promotion/references/specialist-agent-template.md`) baked literal "see `docs/status/ARCHITECTURE.md` §8/§9" pointers into files that get written into *the founder's own repo*, where no such doc exists — the guidance was already inline, so the dangling pointers were removed; (5–7) doc drift: `full-pipeline-e2e` was still labelled `provisional` in four places despite being `verified`, `hotfix.md` claimed `verified` ("two runs") but only logged Run 1 and left a stale "kept at provisional" line while its Run-2 fixture (`setup-hotfix-hard-fixture.sh`) sat orphaned — reconciled the file to its already-committed sweep evidence and cited the fixture; plus tightened `check-repo-consistency.mjs`'s doc-drift check from a bare substring match (which masks real drift when a command name sits inside another word) to a word-boundary test, and guarded `session-start.sh`'s `cd "$CLAUDE_PROJECT_DIR"` against an unset var under `set -u`. **Deliberately NOT fixed unilaterally** (logged for a decision — see Known open items): the `boardroom-checkpoint.mjs` gate checks `ExitPlanMode`'s inline plan text first and only falls back to reading the plan *file* when inline is empty, so a genuine "ship it" checkpoint that lives only in the file can be ignored — it fails *safe* (denies, never wrongly allows), and it touches the safety gate, so it's the user's call, not an unattended rewrite. **Mechanism lesson:** the highest-value find was structurally the same bug class ("docs/ isn't installed, so a shipped file can't depend on it at runtime") reappearing in a *new* location a year of vigilance hadn't caught — strong evidence that a recurring class needs a mechanical guard, not repeated auditing. It stays Layer 2 for now only because the crisp mechanizable version ("no shipped skill/template file cites `docs/` for operational use") can't cleanly separate an operational read from a harmless citation, and a noisy check is worse than none — logged as a `docs/status/REGRESSION-CHECKLIST.md` candidate to revisit if it recurs a third time.
<!-- wingman:log type=decision category=tooling-proposal status=resolved -->
- **pnpm: rejected for Wingman itself, logged as a gated proposal for the projects Wingman generates.** *(Resolved 2026-07-15 — see the newer entry above: the Boardroom's 3 conditions were closed and `package-manager-selection` shipped.)* Question raised whether to adopt pnpm. For Wingman's *own* repo the answer is a clear no — it has no `package.json`, no `node_modules`, and zero npm dependencies (its four scripts are dependency-free `.mjs` run directly with `node`, and CI installs nothing), so a package manager would manage nothing while adding a manifest + lockfile + CI install step — exactly the speculative infrastructure `engineering-minimalism` exists to prevent. The only thing that would flip this is CI growing a real devDependency (a linter/test framework), which it deliberately hasn't. For the *generated* projects the case is genuinely stronger — pnpm's phantom-dependency prevention aligns with `boardroom-security`/`engineering-minimalism`, and its disk/speed wins matter for a founder iterating fast — but it's **not** a blanket mandate: it collides with the project's lazy, needs-driven tooling philosophy, it carries real friction for a non-technical founder (strictness breaks packages that lean on phantom deps; many tutorials/deploy targets assume `npm`), and it must never rip out an existing working lockfile. Correct shape, if pursued: default *new* Node/JS projects to pnpm at the point they first need a package manager (in `build.md` or a DevOps lead's remit), with a one-line plain-language rationale for the founder, an escape hatch, and a hard "respect any existing lockfile" rule — routed through a Boardroom checkpoint, not asserted. Logged here as a proposal; no command behavior changed pending that gate.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Merged the real dogfooding branch (PR #4) after discovering "clean up init-6eg3d1" would have deleted real, unmerged work — then audited the resulting CI/CD for enterprise-grade gaps.** Asked to delete `origin/claude/init-6eg3d1` as stale; direct inspection first (per this project's own "investigate before destructive action" discipline) found 5 real commits from a genuine dogfooding pass — an actual `claude plugin install`, not a sandboxed eval — that fixed real install-breaking manifest bugs, a headless-session (`AskUserQuestion`-unavailable) checkpoint gap, and a hook-enforcement gap (see the two entries below). Opened PR #4 instead of deleting, merged once CI was green, then deleted the now-genuinely-fully-merged branch (blocked by a `403` on the git proxy for the delete specifically — not a network error worth retrying, logged as needing a manual GitHub-UI delete). Then audited `.github/workflows/validate.yml` itself for "enterprise grade" readiness: directly tested the hypothesis that `actions/checkout@v4`'s default `submodules: false` would make the attribution check silently pass over an empty `vendor/`, and **disproved it** by simulating the exact CI checkout locally — git still creates the submodule placeholder directories, so the check (which only needs directory *names*, not content) still fires correctly; reporting a disproven hunch as a finding would have been exactly the self-report-without-verification failure `verification-before-completion` exists to catch. Real findings, fixed directly: no `permissions` block (added `contents: read`, least privilege), no `timeout-minutes` (added a 5-minute cap against a runaway/hung job), no `concurrency` group (added one so redundant pushes cancel superseded runs instead of queuing). One real finding that needs a human: nothing on GitHub currently *requires* the `validate` check to pass before a merge to `main` — CI is green but advisory, not enforced; branch protection needs repo-admin access, logged in `docs/HUMAN-TODOS.md`.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **The first real `/wingman:plan` run (against a live throwaway "TaskFlow" test project, via the now-fixed real install) found and fixed a second real bug: a genuine checkpoint got reviewed but never persisted, and the safety hook wouldn't have caught it either.** The run itself was real and worked well — genuine clarifying questions asked, a real `dept-product` lead created, a real plan written, a real 5-seat Boardroom dispatch (5 actual subagents, independently confirmed via their own transcript files) returning a real `GO WITH CHANGES` with distinct per-seat findings. But afterward, `.wingman/checkpoints.jsonl` didn't exist, `state.json` was missing its checkpoint fields, and the plan file had no `## Wingman Boardroom Checkpoint` marker — despite `boardroom.md` saying to record the checkpoint "always, regardless of scope." Root cause, confirmed directly from the session transcript rather than guessed: `AskUserQuestion` is not a registered tool at all in `claude -p` headless/print-mode sessions (two independent `ToolSearch` calls for it came back empty). `boardroom.md`'s "Mark the plan file" and "Record the checkpoint" steps were both worded as happening "after/once the founder's decision is known" — so when no decision could be obtained in the turn, the model correctly declined to fabricate one, but also never used the checkpoint schema's existing `"still_reviewing"` value as a fallback, leaving a completed review completely unpersisted. Fixed `boardroom.md` to make the fallback explicit (record now with `still_reviewing`, update in place once a real decision arrives) and directly re-verified in the same real headless environment: re-running `/wingman:boardroom plan` now correctly writes all three files. That re-verification surfaced a second, deeper issue: `hooks/boardroom-checkpoint.mjs`'s own comment says it blocks `ExitPlanMode` when the marker "says the founder didn't approve," but the actual code only ever checked for `Bottom line: DO NOT SHIP` — a `still reviewing` or `fix concerns first` marker (i.e., no real "ship it") would have been allowed through, contradicting `plan.md`'s explicit rule. Fixed by adding a `Founder decision:` check requiring `ship it` specifically, verified against all 6 real paths by piping simulated hook input through the actual script (still-reviewing → deny, fix-concerns → deny, ship-it → allow, DO NOT SHIP → deny, no marker → deny, non-`ExitPlanMode` calls → pass through untouched). Also discovered and fixed a real operational trap while re-syncing the installed plugin to test these fixes: `claude plugin update` silently no-ops on a directory-sourced plugin whose `version` field hasn't changed, even when the source content has — bumped `plugin.json` to `0.1.1` and confirmed `plugin update` now detects and applies it. General lesson: a real install doesn't just validate a manifest once at install time — every fix made *after* installing needs its own re-sync-and-reverify step, and skipping that step (as `plugin update` alone would have, silently) would have meant testing fixes against stale cached content without any error to signal it.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **First real `claude plugin install` of Wingman found and fixed 3 install-breaking bugs no sandboxed eval could ever have caught.** Used the real `claude` CLI (present in this environment) to actually `plugin marketplace add` + `plugin install` this repo, rather than continuing to treat "installs cleanly" as an untested assumption. Found, confirmed via the CLI's own validator/error output (not guessed), and fixed: (1) `marketplace.json`'s plugin `source` was `"wingman"` with no `./` prefix — invalid; (2) even after that fix, `source` needs to be the full path from `marketplace.json` to the plugin dir (`./plugins/wingman`) — the `pluginRoot` metadata field that was supposed to shorten this is silently ignored by the real CLI (`claude plugin validate` itself warns "Unknown field 'pluginRoot'. Claude Code ignores it at load time."); (3) `plugin.json`'s `commands`/`agents`/`skills` array entries were all missing the `./` prefix Claude Code's real schema requires (bare `"agents/x.md"` is "Invalid input"), and its `hooks` field used the wrong shape entirely — `{"file": "..."}` is invalid; the real schema wants a bare string path, but even fixed that way it made the plugin **fail to load outright** ("Duplicate hooks file detected... hooks/hooks.json is loaded automatically, so manifest.hooks should only reference additional hook files") because `hooks/hooks.json` is auto-loaded by filename convention and must never be redundantly declared. After all three fixes: `claude plugin install` succeeds, `claude plugin list` shows `enabled`, and a fresh session in a throwaway test project correctly lists all 13 `wingman:*` commands, 5 `boardroom-*` agents, and 10 skills unprompted. Also directly confirmed (via `--debug` log: `Registered 1 hooks from 1 plugins`) that the `boardroom-checkpoint` `PreToolUse`/`ExitPlanMode` hook genuinely registers in a real install — closing Phase 1 item 2 below, previously blocked on "no real install exists to test against." Mechanized the two schema-shape bugs into `validate-structure.mjs` itself (a new `requireDotSlash()` check on every commands/agents/skills entry, and an inverted hooks check — declaring `plugin.json`'s `hooks` field at all is now the error, not its absence) so this exact class of regression can't silently return. This is the strongest evidence yet for a broader lesson already written into this project's own philosophy (see the regression-hardening entry below): a mechanical structural validator only catches what it's told to check, and "plugin.json parses and files exist" is not the same claim as "a real Claude Code install accepts this file" — the two diverged in three separate, compounding ways here, none of which the eval harness or the existing validator had any way to notice, because neither had ever driven a real install.
<!-- wingman:log type=decision category=eval status=resolved -->
- **Re-ran the genuine `/wingman:evolve` gather after the harness self-audit; confirmed nothing new qualifies.** The prior gather (below) had already checked the full decisions log up to that point and correctly proposed nothing. Only one new entry was added since (the harness/CI self-audit) — a single occurrence can't clear the 2+-genuine-occurrences bar by definition, and its shape (proactive self-check finds a real gap, fixed immediately) doesn't match either of the two patterns already promoted (claim-never-reverified → `systematic-auditing` + the mechanized validators; do-work-then-skip-persist → `boardroom.md`'s read-merge-write fix). Correct output: propose nothing, same as last time — not a re-run for its own sake, but confirming the "full eval, evolve, harness" sweep genuinely closes rather than silently skips the evolve leg.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Ran a real `/wingman:harness` self-audit on Wingman's own repo and found one genuine, fixable gap: no CI.** Direct check (`find`/`ls`) confirmed no `.github/workflows/` existed anywhere in the repo — every mechanical validator and eval had only ever been run locally, meaning a broken structural check could be skipped entirely by pushing without running it first. Re-confirmed check-2 ("do tests actually fail when they should") live rather than citing prior-session evidence: reintroduced the historical `PermissionRequest` hook-name bug, watched `validate-structure.mjs` fail with the exact right message, then restored and confirmed clean. Fixed the CI gap immediately (small, per `harness.md`'s own "fix the highest-leverage gap now" instruction): added `.github/workflows/validate.yml` running both `validate-structure.mjs` and `check-repo-consistency.mjs` on every push/PR, YAML-validated before committing. Also closed `harness.md`'s own eval gap in the same pass — built a negative-case fixture (`setup-harness-fake-fixture.sh`, a project whose `npm test` is a hardcoded "4 passing" that never touches the code, hiding a real bounds bug) and dispatched a blind subagent against it; it correctly refused to trust the green line, named the exact fake mechanism, and performed a real spot-check, independently confirmed clean afterward. Eval coverage is now 10/10 verified.
<!-- wingman:log type=decision category=eval status=resolved -->
- **Drove all 7 remaining `provisional` eval cases to `verified` in one parallel sweep — and the adversarial run finally produced the pipeline's first real-dispatch `DO NOT SHIP`.** Built one differently-shaped second-scenario fixture per provisional eval (a clean project for the audit negative case, a true claim for the verification inverse, an order-dependent shared-state bug for the 2+-hypothesis hotfix, a docs-needing feature for launch, a trivial change for the retro skip, and — the important one — an *already-committed* vulnerable diff for the full-pipeline `NO_GO`), verified each fixture behaved as designed, then dispatched six problem-solving subagents in parallel, each independently verified against the real filesystem before promoting anything. Every one passed. The full-pipeline adversarial fixture solved the standing problem that two "realistic" runs couldn't: good engineering judgment routes *around* a seeded flaw before the Boardroom sees it, so to test "real reviewers reject a real bad diff" the bad code has to already be committed and handed over as "ship it." Real 5-seat independent dispatch returned 4× `NO_GO` (SQL injection + plaintext passwords, each seat naming real defects unprompted) → `DO NOT SHIP`, `next_stage` pinned, state held at the gate. Eval coverage is now 9/9 verified (was 2/9 at the start of this session's hardening work). Method note reconfirmed at scale: nested `general-purpose`-spawning-`general-purpose` dispatch works in this sandbox (only *named custom* subagent types don't), and six parallel agents all correctly distinguished their own fixture scope from concurrent edits to sibling eval docs — none clobbered another's work.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Regression-hardening: mechanized the audit-found gaps into a two-layer hybrid, and drew a deliberate line at what NOT to mechanize.** The multi-expert audits kept finding bugs that were mechanically checkable but unchecked (inert hook, model-tier drift, doc-drift, orphan files, attribution error) — so they recurred silently until an expensive audit caught them. Layer 1 (cheap, deterministic, run every change) now catches them: `validate-structure.mjs` gained orphan detection, the model-tier invariant, and a skill-anatomy warning; a new `scripts/check-repo-consistency.mjs` catches command doc-drift and attribution coverage. Every check was verified both ways — real repo passes clean with zero false positives, and reintroducing each historical bug fires the check. Crucially, two candidates were deliberately **left un-mechanized** (attribution license-accuracy, non-shipped-doc runtime deps): a keyword heuristic false-positives on its own corrective text — the accurate fix for the andrej-karpathy error literally contains the words "no license" while explaining the old mistake — and a noisy check trains people to ignore warnings, worse than nothing (the `systematic-auditing` skill's own reasoning). Those are Layer-2 items in the new `docs/status/REGRESSION-CHECKLIST.md`, run via `/wingman:audit`. General lesson recorded: the honest enterprise-grade move is mechanize-what's-crisp + checklist-what's-semantic, not mechanize-everything.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Genuine evolve gather on this session's own signal correctly proposed nothing new — because the two real recurring patterns were already promoted.** Ran the `/wingman:evolve` gather/cluster logic against the 19-entry decisions log. Two patterns genuinely recurred 3+ times: (1) a confidently-stated claim repeated across docs but never re-verified against the primary source, turning out wrong (the `PermissionRequest` hook name, the andrej-karpathy license claim, a non-shipped-`docs/` runtime dependency); (2) a multi-step "do work, then persist a record" that skipped or clobbered the persist step (the dropped build checkpoint, the `state.json`-clobbering overwrite, `department-lead-activation` never writing state). Both cleared the 2+ threshold — and both had *already* been acted on this session (pattern 1 → the `systematic-auditing` skill + the mechanized validators; pattern 2 → `boardroom.md`'s read-merge-write and re-read-from-disk fixes). So the correct output was to propose no new specialist/skill/command: a fresh one would duplicate what `systematic-auditing`/`verification-before-completion` already cover, which `engineering-minimalism` forbids. This is evolve working correctly — recognizing genuine repetition *and* recognizing when it's already resolved — not evolve finding nothing.

<!-- wingman:log type=decision category=hooks status=resolved -->
- **Merged to `main` on explicit request, after a dedicated pre-merge regression check rather than merging on the strength of prior evidence alone.** Before merging PR #1, folded in PR #2 (the hook-event-name validation fix from the PR review) first, since merging without a verified fix sitting right there would have left known risk-reduction on the table — flagged this deviation from the literal "merge PR #1" instruction transparently and got explicit confirmation before continuing, since a merge with no separate human review of its own is exactly the kind of unilateral action worth pausing on. Then, before merging: re-ran `validate-structure.mjs` fresh on the merged branch (PASS); directly re-simulated the original hook bug one more time to confirm the new check still catches it; and checked chronologically that every behavioral file changed since its own last eval verification had already been re-exercised by a later real pipeline run (Phase 3's Run 2 postdates every fix to `boardroom.md`/`secure.md`/`department-lead-activation`) — so nothing merged represented untested drift. Did not do a full re-run of all 10 eval cases, since nothing they cover had changed since their last verified run; re-running unchanged, already-verified behavior would have cost real tokens for zero new information. `main` now has everything this project has built.

<!-- wingman:log type=decision category=eval status=resolved -->
- **A human-requested sweep of "what else needs a human" (env vars, API keys, screenshots) found a real, previously-repeated attribution error, not just new to-do categories.** Checked properly this time (the original `docs/HUMAN-TODOS.md` only carried over 2 items already tracked elsewhere) and confirmed Wingman needs zero credentials/env vars, but found: `main` on GitHub has never received any of this project's work (everything lives on `claude/init-6eg3d1` only); zero demo/screenshot content exists anywhere, and can't be produced from this sandbox honestly (every eval ran against throwaway fixtures with simulated `AskUserQuestion` answers); and, doing a self-check pass on `ATTRIBUTIONS.md` before flagging it for human/legal review, found that `andrej-karpathy-skills` had been documented in three separate places (`ATTRIBUTIONS.md`, `ARCHITECTURE.md` §9, and `engineering-minimalism/SKILL.md`'s own header comment) as having "no LICENSE file," when it actually declares MIT in three places (`plugin.json`, `README.md`, `SKILL.md` frontmatter) — just not as a standalone `LICENSE` file. Fixed everywhere. No real exposure resulted (the content was always restated in Wingman's own words, never quoted), but it's a concrete instance of exactly the failure mode `systematic-auditing` exists to catch — a claim repeated confidently across multiple docs, never independently re-checked against the actual source. Worth remembering: attribution/licensing claims are exactly the kind of thing that should get the same "re-verify against the primary source, don't just trust the existing doc" treatment as any other audit finding.

<!-- wingman:log type=decision category=eval status=resolved -->
- **Prioritized eval coverage by actual risk/value instead of working the untested-file list top to bottom.** After Phase 3, roughly a dozen commands/skills still had zero dedicated behavioral evidence. Rather than testing all of them, three were identified as genuinely high-value (`systematic-auditing`/`audit.md` — brand new, unproven, directly serves the session's own rigor-codification request; `verification-before-completion` — the single most load-bearing discipline skill in the project, everything else's "independently verify" methodology is this skill applied one level up, yet it had never itself been tested; `retro.md` — zero prior exercise, cheap to test using already-existing fixture history) and the rest deliberately deferred with stated reasoning (`design-taste` has no UI-having fixture in this project to test against without meaningfully more setup; `engineering-minimalism`/`plain-language-checkpoint` are exercised so pervasively and incidentally across nearly every other eval that a dedicated case would mostly restate existing evidence; `token-economy` governs internal-only channels with no founder-visible output to grade against). One real, unplanned finding from the `systematic-auditing` run: nested subagent dispatch (a `general-purpose` agent spawning further `general-purpose` agents) actually works in this sandbox — a more precise statement of this project's sandbox limits than the earlier-established "named custom `subagent_type`s don't resolve," which is still true but is a narrower constraint than "no nested dispatch at all."
<!-- wingman:log type=decision category=boardroom status=resolved -->
- **Two full real-dispatch pipeline runs, zero genuine `NO_GO`s — treated as a real, informative result, not a shortfall to keep re-rolling for.** `full-pipeline-e2e` Run 2 (a mid-life "Ledger" fixture, deliberately seeded with a flawed hardcoded-API-key snippet and a repeatable rollback-migration mistake) completed all 4 stages with genuine, non-synthetic 5-seat Boardroom dispatch throughout, including a real fix-recheck loop, an independently-reconfirmed Cost-seat finding, and a self-caught, honestly-disclosed ship-stage process defect. Like Run 1, it never produced a `NO_GO`: both seeded problems were avoided by good engineering judgment (a keyless API chosen over the flawed snippet; the historical lesson correctly applied) rather than needing the Boardroom to catch them after the fact. Decision: don't chase a third "realistic" scenario hoping one eventually produces a `NO_GO` by chance — that's not really testing anything new, since `boardroom-gate-rule.md` already independently verifies the `DO NOT SHIP` file-writing/`next_stage`-pinning mechanics work correctly (with real file-write checks, via synthetic verdicts). What remains genuinely untested is specifically "real independent reviewers reject a real bad diff," which needs a fixture deliberately adversarial enough that competent engineering judgment plausibly doesn't route around it — a different kind of construction than "add realistic seeded flaws and see what happens," which has now been tried twice with the same (informative) result both times.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **A pattern the human operator noticed in their own prompting got codified into the product, instead of staying a one-off technique.** Across this session, specific phrasing ("audit this with multiple experts," "evidence-driven," "test-driven," "production-grade," "find all the gaps") reliably triggered a rigor pattern — scoped parallel subagents per concern, each independently verified against the real filesystem before being trusted — that found real bugs (including the `PermissionRequest` hook bug above) that earlier, less-rigorous passes had repeatedly missed. A real founder using the installed plugin won't know to use those exact phrases. Fixed by adding `skills/discipline/systematic-auditing` (paired naming with `systematic-debugging`) with those exact trigger phrases in its frontmatter `description` — per this project's own "description trap" lesson, a vague description means a skill never reliably auto-routes — plus `commands/adaptive/audit.md` as an explicit, discoverable on-demand entry point that doesn't depend on the founder phrasing anything a particular way. Deliberately scoped as a cross-cutting process skill, not a 9th department — `docs/status/ARCHITECTURE.md` §2/§3 has a real, considered principle against extending the fixed 8-department roster ad hoc, and this doesn't fit that shape (no per-project activation signal, no remit over a body of work — it's a *how*, like `systematic-debugging`, not a *what*). Same session, also added `docs/wingman/founder-todos.md` as a per-project convention (wired into `secure.md`'s existing founder-accepted-risk step): founder-only business-risk decisions were previously only recorded in a checkpoint's `founder_notes` field, unscannable without reading the whole audit log — now they land in one dedicated list, mirroring `retros.md`'s established precedent rather than inventing a new storage convention.
<!-- wingman:log type=decision category=eval status=resolved -->
- **Phase 2's three new evals each surfaced a real, previously-undocumented gap in `boardroom.md`, none of which the Phase-1 fixes or the original e2e run had caught.** Built purpose-made, minimal, deterministic fixtures for each (a discount calculator with a floor-vs-round bug for `hotfix`; a pre-shipped feature for `launch`; a trivial one-line diff for testing `boardroom.md`'s consolidation logic directly with synthetic seat verdicts, to isolate the rule from seat-judgment quality). Findings: (1) `next_stage`'s meaning on a `DO NOT SHIP` checkpoint was undefined — since `state.json`'s `current_stage` is set from `next_stage` unconditionally, a literal reading of the schema's other (advancing) examples could have let a blocked checkpoint silently advance the project past its own gate; now explicit that `next_stage` stays pinned to the reviewed stage on `DO NOT SHIP`. (2) `scope_ref`'s format for content passed directly by a calling command (launch copy, not a plan or diff) was undocumented, forcing each caller to improvise; now a documented convention. Neither gap could have been found by re-running `full-pipeline-e2e` again, since that eval's single scenario never produced a `DO NOT SHIP` outcome or exercised the directly-passed-content path more than once. Token-efficiency choice made explicitly: declined to build a separate dedicated eval for the 5 Boardroom personas, since they'd already been dispatched ~20+ times incidentally across other evals with their output contract holding every time — spending a fresh eval on them would mostly re-confirm existing evidence.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **A four-way parallel expert audit (architecture, content quality, executable code, eval coverage) found and fixed 5 real bugs no single pass had caught.** Run 2026-07-07 as a deliberate cross-check after the e2e eval, each auditor scoped to a distinct concern and given exact file lists to stay token-efficient. Findings, most severe first: (1) `hooks/hooks.json` registered the plan-mode safety gate under `PermissionRequest`, which is not a real Claude Code hook event (the valid set is `SessionStart`/`SessionEnd`/`UserPromptSubmit`/`Stop`/`StopFailure`/`PreToolUse`/`PostToolUse`) — confirmed against the authoritative hooks reference, meaning the entire `boardroom-checkpoint` enforcement had been silently inert since it was built; fixed to `PreToolUse` with the `ExitPlanMode` matcher, and `boardroom-checkpoint.mjs`'s `allow()` output corrected to the real `hookSpecificOutput.permissionDecision` field, plus its malformed-stdin path changed from fail-open to fail-closed to match the "deterministic backstop" intent it was already documented as having. (2) `boardroom.md`'s "Record the checkpoint" step both cited non-shipped `docs/status/DATABASE.md` for `state.json`'s exact shape (the same runtime-dependency-on-a-non-shipped-doc bug class as the earlier `evolve-promotion`/`department-lead-activation` fixes) and never instructed preserving `active_department_leads`/`active_specialists` when overwriting `state.json` — literally followed, every checkpoint would have silently wiped the department-lead/specialist roster. Fixed: the full schema is now inlined in `boardroom.md`, and the step is explicitly a read-merge-write, not a blind overwrite. (3) `skills/governance/department-lead-activation/SKILL.md` never actually wrote to `state.json` at all, despite `docs/status/DATABASE.md` and `evolve-promotion` both asserting it populates `active_department_leads` — fixed by adding that step explicitly. (4) `boardroom-engineer.md`/`boardroom-security.md` both declared `model: inherit` despite `docs/status/ARCHITECTURE.md` §8 explicitly assigning them `model: opus` as the two highest-stakes seats — fixed. (5) Several doc-drift items (`CLAUDE.md` omitted `launch`/`hotfix` from its adaptive-commands list; `ARCHITECTURE.md`'s launch row and "Open items"/rollout sections were stale relative to what's actually built and tested; `ship.md`'s final checkpoint was conditional on "meaningful release," contradicting the stated one-checkpoint-per-stage design) — all fixed. General lesson: none of these would have been caught by re-running the same eval again: (1) and (4) are about hook/agent *configuration* the eval harness doesn't inspect, (2)/(3) are a state-persistence bug that the single-scenario e2e run happened not to trigger (subagents preserved the roster by good judgment, not because the instructions required it), and (5) is pure doc-consistency. Different failure classes need different-shaped checks, not more repetitions of the same one.
<!-- wingman:log type=decision category=eval status=resolved -->
- **"All seats reported" is not the same as "the checkpoint was recorded" — orchestrating commands must verify their own writes.** The full-pipeline e2e eval's build stage had all 5 Boardroom seats genuinely return verdicts, yet the orchestrating agent never actually appended to `.wingman/checkpoints.jsonl` or updated `state.json` — it apparently considered the stage done once the seats replied, without confirming the subsequent file-write step actually executed. `boardroom.md`'s "Record the checkpoint" section already said "always, regardless of scope," so this wasn't a wording gap in what to do — it was a missing check that the doing had happened. Fixed by adding an explicit instruction to re-read both files from disk before reporting a checkpoint complete, naming the `verification-before-completion` skill directly. Worth generalizing: any command with a multi-step "do the work, then persist a record of it" shape is exposed to this same failure mode, and the fix is always the same — verify the persistence step landed, don't infer it from the preceding step having gone well.
<!-- wingman:log type=decision category=eval status=resolved -->
- **A recurring-friction detector must distinguish "same incident, logged twice" from "genuine repetition."** `evolve-promotion`'s negative-case eval deliberately fed it three topics each appearing in exactly 2 files (a learning entry plus that same event's checkpoint or retro) to check it wouldn't naively count file-mentions as occurrences. It correctly recognized same-date, same-fix pairs as one incident each and proposed nothing — worth remembering as the specific failure mode this class of mechanism is most exposed to, since every piece of real work naturally produces a learning note *and* a checkpoint/retro for the same event.
<!-- wingman:log type=decision category=evolve status=resolved -->
- **`docs/` isn't part of the installed plugin — anything a skill needs at runtime must be bundled inside `plugins/wingman/`.** Caught during v3 work: `marketplace.json`'s `pluginRoot`/`source` only installs `plugins/wingman/` into a founder's Claude Code environment; the repo-root `docs/` folder (including `docs/roadmap/AGENT-ROSTER.md`, which `evolve-promotion` needs to name specialists from) never ships. Fixed by adding `plugins/wingman/skills/governance/evolve-promotion/references/specialist-catalog.md` as the runtime-shipped copy, with an explicit sync note in both files rather than silently duplicating content. Also removed a similar latent dependency from `department-lead-activation`'s template, which didn't actually need the specialist-level catalog at all (department leads operate at a coarser granularity). Any future skill referencing another doc for *operational* purposes (not just rationale/citation) needs this same check.
<!-- wingman:log type=decision category=evolve status=resolved -->
- **Evolve-promoted commands and skills follow the same founder's-project rule as specialist agents — not a plugin-directory exception.** The original `evolve.md`/`AGENT-ROSTER.md` text (written before `department-lead-activation` established the project-scoping precedent) said to write promoted commands/skills into `plugins/wingman/` while specialists went to the founder's project — an inconsistent carve-out. Fixed: `/wingman:evolve` never writes into `plugins/wingman/` for any artifact type; commands go to `.claude/commands/`, skills to `.claude/skills/`, agents to `.claude/agents/`, all in the founder's own project. "Wingman's plugin directory" always means the founder's resyncable local install, regardless of what's being written.
<!-- wingman:log type=decision category=evolve status=resolved -->
- **Retros need one canonical file, like `LEARNINGS.md` already has.** `retro.md` originally didn't specify where retro blocks get saved, leaving them scattered across whichever file happened to be open — unfindable by `/wingman:evolve`'s clustering step. Fixed: retros now append to `docs/history/retros.md`, mirroring `LEARNINGS.md`'s convention exactly.
<!-- wingman:log type=decision category=eval status=resolved -->
- **Behavioral claims about a skill need a real, independently-verified run, not a worked example.** The department-lead-activation skill was initially "proven" only by a hand-written worked example showing what the template *would* produce. That's evidence the template is well-formed, not evidence the skill's instructions actually produce that outcome when a fresh agent follows them. `evals/` exists to close that gap — see `evals/README.md` and `evals/cases/department-lead-activation.md`'s run log for the actual verified run.
<!-- wingman:log type=decision category=general status=resolved -->
- **Department-lead files live in the founder's project, not in Wingman's plugin directory.** Considered writing dynamically-created `dept-*.md` agents into `plugins/wingman/agents/` at runtime; rejected because plugin files are resynced from the marketplace source and a mid-session write there risks silent loss on the next update, plus there's no guaranteed way to make a freshly-written *plugin* agent discoverable within the same session without a reload. Claude Code's project-scoped subagent mechanism (`.claude/agents/*.md`, auto-discovered, no manifest) is the correct home — it also matches intent, since each founder's project should accumulate its own department-lead roster, not share Wingman's.
<!-- wingman:log type=decision category=general status=resolved -->
- **Runtime provider: Claude Code native only.** Evaluated LangGraph and smolagents for the literal 56-agent blueprint's orchestration layer; rejected both because either would require a separate hosted Python service, turning Wingman into "a plugin + a product" instead of just a plugin. Cross-agent collaboration uses Claude Code's own Task-tool dispatch (built) and, where useful later, the experimental Agent Teams feature (not yet used). Persistent state, where needed, is a small local MCP server following `gsd-plugin`'s proven pattern — not a database, not LangGraph state.
<!-- wingman:log type=decision category=boardroom status=resolved -->
- **Hybrid agent population, not the literal 56-agent roster upfront.** The full corporate-hierarchy blueprint is kept as a naming/organizing scheme (`docs/roadmap/AGENT-ROSTER.md`), but only 5 Boardroom seats are built at install time; department leads and specialists grow lazily per-project. Rationale: a fixed 56-agent roster taxes every project's context budget regardless of whether that project ever needs a Row-Level-Security specialist or a canary-rollback agent.
<!-- wingman:log type=decision category=install status=resolved -->
- **Vendoring means "read and adapt," not "depend on."** All 16 vendor repos are git submodules for reference/attribution only. None of their bespoke infrastructure (SDKs, CLIs, MCP servers, hosted dashboards) is a runtime dependency of the installed Wingman plugin.
<!-- wingman:log type=decision category=boardroom status=resolved -->
- **Boardroom seats never call other agents.** Adopted verbatim from `addyosmani-agent-skills`' persona/command distinction: only commands (`boardroom.md`, the pipeline stages) orchestrate and merge; agents (personas) only ever report back.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Checkpoint enforcement is a deterministic hook, not an LLM judgment call.** The `boardroom-checkpoint` hook checks for a marker string, not the plan's actual content — the LLM reasoning happens earlier, in `/wingman:boardroom` itself. This keeps the safety backstop auditable and testable independent of model behavior.
<!-- wingman:log type=decision category=evidence-gate status=resolved -->
- **A pasted swarm-intelligence (PSO/ACO/GWO/OASIS) research document proposing a predictive multi-agent simulation layer was logged as a deferred candidate, not built.** Same evidence-gate `ARCHITECTURE.md` already applies to MVP2-5's dropped placeholders: no dogfood run or retro anywhere in this project's history has ever surfaced a need for simulated/predicted risk — every real risk this project's own history caught (SQL injection, plaintext passwords, missing tests) was found by the existing Boardroom/DoD-gate review operating directly on the real diff. See `docs/roadmap/AGENT-ROSTER.md`'s new "Deferred mechanism ideas" section for the full entry and revisit condition.
<!-- wingman:log type=decision category=evidence-gate status=resolved -->
- **A pasted "enterprise-grade hybrid framework" document proposing per-stage JSON/YAML schema files, configurable Boardroom "strictness," and canary/observability tooling was logged as a deferred candidate, not built.** Unlike the swarm-intelligence pitch, this one directly conflicted with decisions already made and documented, not just unevidenced: `docs/status/DATABASE.md`'s flat-file choice over per-stage schema files (a real, revisited decision — see its `schema_version` migration history), and `boardroom.md`'s unconditional any-seat-`NO_GO`-blocks rule, which the proposed "majority/override" strictness dial would have weakened for exactly the non-technical-founder audience it protects. See `docs/roadmap/AGENT-ROSTER.md`'s "Deferred mechanism ideas" table for the full entry and revisit condition.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **A founder-shared "how real AI applications stay reliable across models" architecture diagram was cross-checked against Wingman's actual layers, and returned one real, tractable gap — not a rebuild.** A real CTO + Research Boardroom review mapped the diagram's 5 layers (input/orchestration/retrieval/safety-quality/observability) onto Wingman's existing artifacts: input validation (`prompt-guard.mjs`, `content-injection-scanner.mjs`), orchestration (`boardroom.md`'s parallel dispatch — deliberately with no multi-model router, an already-settled decision), retrieval (skills/references, deliberately no vector DB per `docs/status/DATABASE.md`), safety/quality (`dod-structural-gate.mjs`, `boardroom-checkpoint.mjs`, `evals/`), and observability (`wingman-health.mjs`, `wingman-metrics.mjs`) — each already covered by a real, file-verified artifact or a documented, reasoned rejection. The one genuine gap found: `checkBoardroomVerdictClean` trusted a checkpoint's transcribed `seats[].verdict` field verbatim, with nothing independently cross-checking it against each seat's own raw `## <SEAT> VERDICT: ...` output — a mis-transcription (e.g. a `NO_GO` copied in as `GO_WITH_CONCERNS`) would sail through the push-time gate undetected regardless of which model drove the session. Fixed via `checkVerdictTranscriptionMatchesDetails` (G14, see `docs/history/GAPS.md`) — a mechanical regex cross-check against the already-written `details_ref` companion file, not a new semantic layer.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **A founder-shared "Prompt Engineering vs Loop Engineering" diagram prompted a real 3-seat Boardroom review of `stop-loop.mjs`, and its converged recommendation shipped.** The diagram frames a stricter discipline for autonomous agent loops than plain prompting: goal + success criteria defined upfront, then every iteration checked against a max-iteration cap, a budget/time cap, no-progress (stall) detection, and a real completion check, before deciding "brake tripped" vs. "actually done." Direct comparison against Wingman's existing opt-in `stop-loop.mjs` found it already had the iteration cap and a (weak, text-match-only) completion check, but no budget cap and no stall detection. Dispatched the CTO, CISO, and CFO Boardroom seats — the three whose actual lanes this touches — for a real, independent review rather than assuming the gap mattered. All three returned `GO_WITH_CONCERNS`, converging on the same fix: an optional wall-clock budget (`maxWallClockMinutes`) and repeated-identical-tool-call stall detection (`stallThreshold`, default 3), both additive and backward-compatible with existing `.wingman/loop.json` configs. Shipped both, plus unit tests for each new check. The CTO's further suggestion — corroborate completion with a `verifyCommand` exit code instead of a bare text match — was deliberately **not** bundled into the same change: shelling out an arbitrary command read from a JSON config file is its own attack surface and deserves a dedicated CISO review, not an add-on riding a different feature's PR. See `docs/history/GAPS.md`'s G2 entry for the full behavior.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **The `verifyCommand` question deferred above got its dedicated CISO review, converging with 2 more founder-shared diagrams on the same "evaluate before accepting completion" gap — shipped with a concrete mitigation, not a blanket yes/no.** Cross-referencing a "5-layer AI application" architecture deck's Safety & Quality layer ("Quality Check: Pass → assembly / Fail → back to Orchestrator for retry") and a "Self-Improving Loop" deck's Evaluation step ("scores the output against quality checks") against `stop-loop.mjs`'s own bare `lastText.includes(promise)` completion check surfaced the same real gap all three sources independently pointed at: a text claim of completion is never actually confirmed. Dispatched the dedicated CISO review the earlier entry withheld this for. Verdict: `GO_WITH_CONCERNS` — running a founder-configured command here is not a new class of risk (`dod-structural-gate.mjs`'s `runTestSuite` already shells out to a detected test command at push time via the same `execFileSync`-family pattern), but firing on every `Stop` event instead of once per push is a materially larger surface for one specific exploitation path: a prompt-injected turn rewriting `.wingman/loop.json`'s `verifyCommand` mid-session, then triggering its own repeated re-execution on the next `Stop` events. Concrete, cheap mitigation identified and shipped: `verifyCommand` is cached into `loop-counter.json` once, at loop start, and never re-read off `loop.json` for the rest of that loop's run — a mid-loop rewrite has no effect until the next fresh loop start. Honestly disclosed as a narrowing, not a closing, of the gap: nothing in Wingman's stack can distinguish a founder-written `loop.json` from an agent-written one. See `docs/history/GAPS.md`'s G2 entry for the full updated behavior and test plan.
<!-- wingman:log type=decision category=hooks status=resolved -->
- **Google's SAIF 2.0 ("Secure Agents") risk map was cross-checked against Wingman's actual hooks/skills, scoped to only the 4 applicable rows (Application/Agents/Input Handling/Output Handling — Wingman doesn't train or serve its own models, so the other rows don't apply).** A real Boardroom CISO-seat review (dispatched via the `Agent` tool against `agents/boardroom-ciso.md`, not decided unilaterally) returned `GO_WITH_CONCERNS`, citing 2 concrete, file-verified gaps. Gap 1 — the `permissions:` frontmatter field isn't runtime-enforced — was already honestly disclosed in `docs/status/ARCHITECTURE.md`'s "Agent Permission Model" section; resolved by adding a citation paragraph confirming the CISO independently reached the same conclusion, rather than building enforcement machinery for a risk that's already documented and accepted. Gap 2 — `prompt-guard.mjs`'s injection-pattern list was narrow (4 patterns) and only scanned the founder's own prompt, never content fetched from the web or pasted docs — resolved by broadening the pattern set to 7 (exported as `INJECTION` to avoid duplication) and adding `content-injection-scanner.mjs`, a new warn-only `PostToolUse` hook (gap `G13`, see `docs/history/GAPS.md`) that reuses the same patterns against tool output. Warn-only, not blocking, per this project's own prior "over-block trap" lesson (`boardroom-checkpoint.mjs` v12.1). Documented as a floor, not a ceiling: a fixed regex list cannot close the injection-risk class completely.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Prototyped the founder-project memory/query capability** (`docs/history/architecture-audit-2026-07-15.md`'s Emerging finding #6, Confidence: Medium) as `plugins/wingman/scripts/query-founder-knowledge.mjs`, per that audit's own Migration Strategy: a bounded, evidence-gated experiment, not a committed feature. Reuses `okf-export.mjs`'s existing `readCheckpoints`/`readMemoryFile` parsers rather than re-implementing them — the exact duplication risk the audit flags when *not* done. Unifies all 4 fragmented per-project formats (`checkpoints.jsonl`, `state.json`, `traceability.json`, `memory/{MEMORY,decisions,tried}.md`) into one queryable, chronologically-sortable array, plus a `--summary` mode that directly answers `docs/status/DATABASE.md`'s named gap ("no single file or view unifies these into one 'what has this project decided and why' surface") in one call.
  **Real run log, honestly graded:** tested against the existing `setup-knowledge-export-fixture.sh` ("Porchlight") fixture, extended with the 3 file types it didn't already cover (`state.json`, `traceability.json`, `tried.md`) so all 4 formats were genuinely exercised, not assumed. `--summary` correctly surfaced the current stage, the most recent checkpoint's real `DO NOT SHIP` verdict, and the 3 most recent decisions in one call. Filters (`--source`, `--verdict`, `--since`) and `--json` all verified against real output. A negative case (a project directory with no `.wingman/` state at all) degrades gracefully — zero-valued summary, no crash — matching this project's own "absence is expected, not an error" convention. **Not yet verified**: real usage inside an actual multi-session founder project (this run used a single static fixture snapshot, not a project that evolved across sessions), and the tool remains genuinely a script, not wired into any skill/command — deliberately, per the audit's "prototype-first" guidance, since the right *shape* (skill vs. hook vs. bundled script) is still unproven. Revisit only with real dogfooding evidence before considering a skill/command wrapper.
<!-- wingman:log type=decision category=architecture status=resolved -->
- **Closed the "not yet verified" gap above with a real multi-session dogfood run.** Built a fresh project ("TinyCRM") incrementally across 4 simulated session boundaries, with the discipline that each new "session" queries `query-founder-knowledge.mjs` fresh and acts *only* on that output, not on this conversation's own memory of writing the earlier state — the same test a genuinely fresh Claude Code session would face. Session 1 recorded a planning-milestone checkpoint and a decision (SQLite over Postgres, with an explicit revisit condition). Session 2, reading only the tool's `--summary` output, correctly proceeded with SQLite rather than re-litigating the choice, and recorded a build checkpoint plus a `tried.md` entry (an abandoned ORM). Session 3 recorded a ship-stage checkpoint seeded with a real blocking verdict (`DO NOT SHIP`, CISO `NO_GO` on missing rate-limiting). Session 4, reading only the tool's fresh output, correctly surfaced the blocking verdict via `last_checkpoint.bottom_line` and the specific fix needed via a filtered query on `founder_notes` — proof a fresh session would halt and address the real blocker rather than blindly proceeding, not just proof the write path works.
  **A real gap was found and fixed in the same round, not just logged:** "Session 2" forgot to update `state.json` after writing its checkpoint (a realistic failure mode, not a contrived one) — the tool had no way to detect this drift, since `next_stage` wasn't even surfaced in checkpoint entries. Fixed: checkpoint entries now expose `next_stage`, and `summary()` compares it against `state.json`'s `current_stage`, returning a `state_stage_mismatch` field when they disagree. Verified both directions: no false positive against the corrected state, and a direct catch when the stale state was reintroduced for testing.
  **Trust level: still a prototype, upgraded from "write-path-only" to "read-loop-verified within one long session."** Genuinely separate Claude Code session boundaries (not simulated within one conversation) remain untested — that's the next bar to clear before considering a skill/command wrapper, per the audit's own evidence-gated Migration Strategy.
<!-- wingman:log type=decision category=architecture status=resolved -->
- **Built scoped Codex CLI / OpenCode portability adapters, invoking `docs/status/ARCHITECTURE.md` §8a's own "revisit if a specific harness with this gap is actually targeted" escape hatch for real.** Real web research first (sources cited in `plugins/wingman/references/harness-adapters/README.md`) found §8a's "most other harnesses lack native parallel subagent dispatch" claim partially stale: Codex CLI's subagents went GA (6 concurrent) with the 2026 GPT-5.5 release, and OpenCode has a documented Task tool plus a parallel general-purpose agent — corrected in a new `docs/status/ARCHITECTURE.md` §8b, not silently left wrong. Also found OpenCode's `plan_exit` tool is a near-exact structural analog of `ExitPlanMode` (Codex CLI has no equivalent — it uses `approval_policy` instead, a genuine capability gap, not a missed port), and that Codex CLI's own `hooks.json` format uses the same event names and allow/deny protocol as Claude Code's.
  **Built, scoped narrowly, not a full port:** the 8 Boardroom seat personas translated into both harnesses' native agent formats (`authored, unverified` — content-only translation of harness-agnostic prose, but neither harness is installed in this dev repo); the `boardroom-checkpoint.mjs` gate re-implemented as a real OpenCode plugin (`authored, unverified` plugin wiring around a `evaluateCheckpoint` function that's a direct, high-confidence logic port); and — the one **built and tested** artifact — `plugins/wingman/scripts/install-git-hooks.mjs`, an idempotent installer that wires the existing `dod-pre-push-check.mjs` up as a real `.git/hooks/pre-push` hook. Verified end-to-end in a scratch git clone in this sandbox: install → a push with no checkpoint is allowed → a push with a recorded `DO NOT SHIP` checkpoint is genuinely blocked (real `git push` exit code, real error text) → uninstall cleanly removes only the hook this script installed. This is git's own mechanism, so it fires under any coding-agent harness, unlike the two harness-specific adapters.
  **Deliberately not attempted, and why:** full 1:1 porting of the other 25 commands / 39 skills (untestable at that scale in this sandbox — the unverified-breadth pattern `engineering-minimalism` warns against); guessing Codex CLI's exact Write/Edit tool-name strings for `secret-guard.mjs`'s hook path (unconfirmed by research — a wrong guess silently never fires, worse than not porting); confirming either harness's single-message N-way parallel-dispatch primitive (neither harness is installed here to test against).
  Also added, per the user's explicit request: a nested `plugins/wingman/AGENTS.md` (the monorepo "nearest wins" convention) with package-scoped authoring conventions, separate from root `AGENTS.md`'s cross-repo navigation content.
<!-- wingman:log type=decision category=audit status=resolved -->
- **Ran `/wingman:audit`'s real procedure against PR #72's own just-merged content — 4 parallel narrowly-scoped subagents, found and fixed 3 real bugs nobody had independently reviewed yet.** Scope: harness-adapters correctness, repo-wide consistency/drift, security/secrets, and test/eval coverage. Every finding independently re-verified against the real files/execution before trusting it, per this project's own standing discipline (never fixed on a subagent's self-report alone). **Fixed:** (1) root `AGENTS.md`'s Repository map claimed `references/` "stays flat" — stale, since the same PR added the genuinely nested `references/harness-adapters/` (36 files, mirroring the target harnesses' own discovery layout); (2) `install-git-hooks.mjs` had a real shell-quoting gap (repoDir interpolated unescaped into the generated hook's double-quoted `/bin/sh` string) — fixed with proper POSIX single-quote escaping, re-tested with a path containing a literal `'` pushed through a real git remote; (3) the same script's `readFileSync` calls had no try/catch, crashing with a raw stack trace on an unreadable/directory hook path instead of the script's own graceful-error convention — fixed and re-verified. Also closed a real gap: zero automated tests existed for `install-git-hooks.mjs`'s install/uninstall/idempotency/foreign-hook logic — added `tests/install-git-hooks/install-git-hooks.test.mjs` (10 cases). One prior-session note (line 268 area) was checked and found **not** a defect: the "7-seat Boardroom" phrasing elsewhere in `ARCHITECTURE.md` correctly distinguishes the 7 fixed seats from the 8th, conditionally-dispatched Design seat, consistent with `CLAUDE.md`'s own established convention — not fixed. Everything else checked (all 8 Codex TOML files parsed clean; `wingman-gate.js`'s decision logic confirmed a faithful line-by-line port of `boardroom-checkpoint.mjs`; no cross-seat content contamination across all 16 translated agent files; full secret/injection sweep clean; zero outbound network calls under `plugins/wingman/`) came back genuinely clean — reported as such, not padded with manufactured findings. Retro logged (`docs/history/retros.md`, category=audit, occurrence=1): the real lesson is that just-merged, same-session, same-actor content still needs an independent audit pass before being considered settled.
<!-- wingman:log type=decision category=platform-conventions status=resolved -->
- **Verified Wingman's plugin mechanics against 2026 Claude Code platform/community conventions** (`docs/status/ARCHITECTURE.md` §8d), broader than §8c's multi-harness-portability scope: skill frontmatter, `hooks.json` event coverage, manifest fields, version/release hygiene. Cross-checked every web-research finding directly against the real repo before treating anything as a gap. **One real gap found and fixed**: `version-gate.yml` never cross-checked `CHANGELOG.md` against a `plugin.json` version bump — added `scripts/check-changelog-entry.mjs` as a second CI step. **One gap investigated and deliberately not built on**: whether a `SubagentStop` hook fires usefully for `Task`/`Agent`-dispatched subagents — direct investigation of this session's own `.claude/settings.json` showed hook wiring loads once at session start (this dev-repo session isn't even running `plugins/wingman/hooks/hooks.json` live), so the live-fire test the plan called for isn't safely runnable mid-session; logged as a confirmed-untestable-here finding rather than guessed at. **One gap checked and confirmed non-existent**: zero of Wingman's 40 skills would benefit from `allowed-tools`/`disable-model-invocation`/`context` frontmatter (grep-confirmed zero current usage; all skills correctly rely on description-based auto-fire with no side-effect workflow needing suppression).
<!-- wingman:log type=decision category=platform-conventions status=resolved -->
- **Follow-up to the platform-conventions audit above: applied the fixes it surfaced.** Fetched OpenAI's official Codex hooks docs directly, resolving `references/harness-adapters/codex-cli/`'s previously-unconfirmed `Bash` PreToolUse matcher (now confirmed real) and the Write/Edit-matcher path's matcher *values* (`apply_patch`/`Edit`/`Write`, all confirmed valid — the hook input's `tool_name` always reports `apply_patch`). **Deliberately still not wired**: `secret-guard.mjs`'s actual payload scan, since the specific `tool_input` JSON field name carrying patch content for `apply_patch` calls remains unconfirmed by the fetched docs — wiring a guessed field name risks a hook that runs but never actually matches anything, which this project treats as worse than not porting it at all. Updated `codex-cli/README.md` (new "2026-07-22 research update" section), `codex-cli/.codex/hooks.json`'s inline comments, the parent `harness-adapters/README.md`'s "Deliberately not attempted" list, and `docs/status/ARCHITECTURE.md` §8d with the precise confirmed/still-unconfirmed split, including the genuinely conflicting sources on whether Codex hooks are enabled by default (official docs: yes; a third-party cheatsheet: opt-in) — left as an open question needing a live install, not resolved by picking a side. Also logged, docs-only, that official Claude Code docs now recommend `skills/` over `commands/` for new plugins; not treated as a gap needing a fix (24-file migration with zero evidenced friction driving it).

<!-- wingman:log type=decision category=eval status=resolved -->
- **Closed the remaining live action items from `docs/history/audit-reorg-2026-07-20.md` (items #2, #3, #5) — items #1, #6, #8, #10 turned out already done, not re-implemented.** Re-verified each of the doc's 10 action items against real repo state before touching anything, rather than trusting the frozen doc's own status column: #1 (CI push double-run) and #8 (script-boundary check) were already fixed by prior sessions; #6 (`.gitmodules` ↔ `ATTRIBUTIONS.md` sync) turned out already implemented in `check-repo-consistency.mjs`'s submodule-attribution check, just not cross-referenced back to this action item; #10 (`CHANGELOG.md`-entry gate) turned out already shipped as `scripts/check-changelog-entry.mjs`, wired into `version-gate.yml`. #2 (auto-generated surface counts) turned out to have been solved by a *better* fix than the one proposed: README/`PROJECT.md` deliberately dropped hardcoded counts entirely (2026-07-20, this same audit's own date) in favor of pointing at `wingman-health.mjs` — nothing left to drift-check.
  **Actually built:** #3 — `scripts/generate-eval-manifest.mjs`, generating `evals/MANIFEST.tsv` (case → fixture → covers) mechanically from each case's own "Tests `<path>`" opening line and `<!-- eval:no-fixture-needed: ... -->` marker, instead of the old `wingman-health.mjs` heuristic (filename-prefix matching + one hardcoded `audit: systematic-auditing` alias). Wired into `wingman-health.mjs` (replacing the heuristic `isCovered`) and into `check-repo-consistency.mjs` as a drift check (regenerate-and-diff against the committed file) — verified it actually fires by deliberately appending a stale row and confirming `FAIL`, then reverting. Running the generator for the first time surfaced 10 real, previously-undocumented cases with neither a `setup-*.sh` fixture nor a no-fixture-needed marker (`council`, `design-taste`, `dod-structural-gate`, `engineering-minimalism`, `plain-language-checkpoint`, `subagent-driven-development`, `test-driven-development`, `token-economy`, `traceability-validator`, `verification-loop`) — read each one's actual body to confirm they do use a real inline/scratch fixture, then added an honest marker to each rather than fabricating a `setup-*.sh` script that doesn't match reality. The manifest-based coverage check is also strictly more correct than the heuristic it replaced: it caught a false-positive the old string-prefix rule had been silently reporting as covered — `evolve` (the command, `commands/adaptive/evolve.md`) only *looked* covered because `evolve-promotion.md` (the skill's case) starts with the same string; the skill has real eval evidence, the command wrapper itself never has. Left honestly flagged as an open gap, not papered over.
  #5 — cached the Claude Code CLI install in `evals.yml` and `install-smoke.yml` via `actions/cache` keyed on the CLI's published npm version (`npm view ... version`, not a hand-maintained pin) rather than reinstalling from scratch on every run. **One deviation from the repo's own convention, disclosed rather than faked**: every other pinned action in this repo pins to an exact commit SHA with a version comment; this session had no network path to verify `actions/cache`'s real SHA for a given tag (`api.github.com` blocked from this sandbox, and the GitHub MCP tool is scoped to `lablaunchpad/wingman` only), so `actions/cache@v4` is pinned to the mutable major-version tag with an explicit `TODO(human)` comment asking a human to pin it exactly — writing in a fabricated SHA would have been a silent, undetectable-until-runtime lie about supply-chain pinning, worse than an honestly-weaker-but-working pin.
  **#9 checked, confirmed non-actionable**: `skills/knowledge/doc-index/SKILL.md` produces no persisted artifact (grep-confirmed zero `write`/`persist`/index-file logic in the skill body) — it's purely on-demand/ephemeral per-invocation, so there is no staleness to wire a CI check against; the doc's own "if purely on-demand, no action needed — confirm and document either way" branch applies.
  **#4 and #7 deliberately left deferred, unchanged**: #4 (mechanical 3rd-occurrence duplication scan) stays evidence-gated — `parseFrontmatter()` is still only duplicated in 2 files, not 3, so the trigger this doc's own Phase 3 named hasn't fired. #7 (splitting `tests/hooks-integration/hooks-integration.test.mjs`) stays evidence-gated on the same file crossing ~800 lines/100 tests — not re-checked this session, no reason to believe the trigger changed.
  All 4 Layer-1 validators, `node --test` (173/173), and `check-fixtures.mjs` (49/49) reverified clean after every change in this batch, not just at the end.

<!-- wingman:log type=decision category=eval status=resolved -->
- **Closed the `evolve` command coverage gap the manifest-based check surfaced above — real simulation found and fixed a genuine classification bug, not just a coverage checkbox.** Dispatched a fresh subagent given only `commands/adaptive/evolve.md` (not the skill file directly, unlike Runs 1–2), against the same positive fixture as Run 1: it correctly located and read `skills/governance/evolve-promotion/SKILL.md` on its own, correctly clustered the migration-rollback friction (2 genuine occurrences, correctly excluded the single-occurrence distractor), but then **misclassified it as a new skill** instead of the "Migration Engineer" specialist agent that `references/specialist-catalog.md`'s own "Using this catalog" section names as this exact scenario's worked example. Root cause, confirmed by reading the subagent's own reasoning: `SKILL.md` step 3 presented the catalog check as a naming detail subordinate to the specialist bullet, not as a classification signal to check *before* the abstract command/skill/specialist test — so a subagent could (and did) commit to "skill" without ever letting the catalog's concrete precedent override it. Fixed by adding an explicit priority instruction to check the catalog first. Re-ran with a second fresh subagent (fresh fixture copy): correctly classified as the Migration Engineer specialist, explicitly citing the new rule as what resolved an ambiguity it independently flagged on its own. `evals/cases/evolve-promotion.md` gained a documented Run 3 (FAIL → root cause → fix → PASS), and its Tests line now names the command file alongside the skill. All 4 Layer-1 validators, `node --test` (173/173), and the eval-manifest drift check reverified clean.

<!-- wingman:log type=decision category=eval status=resolved -->
- **Closed W11's `covered-unmeasured` gap in `docs/status/AGENT-WEAKNESS-BENCHMARK.md` ("hallucinating APIs/methods that don't exist") — real simulation, honestly reported even though the trap never actually sprung.** Surveyed the repo first for what's genuinely still open before planning further work (via a fresh Explore agent): almost everything was closed; W11 was the one real, scoped, buildable gap (the benchmark doc itself named it as "the concrete next step"), while W12 ("not reading existing conventions before editing") stayed correctly deferred — no dogfood/founder evidence of it recurring, per the doc's own evidence-gate policy, so it was **not** built this round.
  Built `evals/cases/api-hallucination.md` and ran it for real, twice, against two differently-shaped hallucination classes: an unfamiliar-package name mismatch (`date-fns`'s real `isWeekend` vs. a plausible `isWeekendDay`) and a genuinely real, since-removed API a model could plausibly recall from stale training data (`lodash`'s `_.pluck`, removed in v4, folded into `map`). **Neither run actually triggered a hallucination** — Run 1 was prevented by the research skill's check-real-source-first discipline; Run 2's subagent already had current, correct knowledge and went straight to `map(users, 'id')` unprompted. A third, informal fixture attempt was discarded before counting as a run because its task wording accidentally pointed straight at the one real function the stub provided, generating no genuine trap pressure — rebuilt as the Run 2 that's actually logged. Promoted to `verified` on the strength of the two real, differently-shaped scenarios run, with the honest limitation disclosed directly in the case's own run log: the "hallucinate-then-recover-via-red" path specifically was never exercised, only the "prevented before it happens" path was — not overclaimed as proof of a mechanism this evidence doesn't actually show.
  `node scripts/wingman-metrics.mjs` confirms 0 broken references / 0 status mismatches for the updated W11 marker. Separately, quick-verified `FIXLOG.md`'s two `wontfix` items (CQ2 `parseFrontmatter` duplication, CQ3 `dod-structural-gate.mjs`'s orchestration block) still haven't grown a 3rd occurrence — both wontfix calls still stand, no action taken. This round touched only `evals/` and `docs/`, nothing under `plugins/wingman/`, so no `plugin.json` version bump was needed (`version-gate.yml`'s path filter doesn't cover either directory).

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Expanded Codex CLI/OpenCode harness adapters from Boardroom-only to full 24-command/40-skill parity, on the founder's explicit MVP request — with the earlier "untestable at scale" objection actually resolved, not overridden.** The founder named "agent-agnostic across Claude Code, OpenCode, Codex CLI" as the next MVP goal — real, named demand, the exact bar `docs/status/ARCHITECTURE.md` §8a set for revisiting the prior full-port decline. Before building anything, entered plan mode and re-verified the premise: fresh research (live installs of `opencode-ai@1.18.4` and `@openai/codex@0.145.0` in this sandbox, not just docs prose — a same-name-collision false negative in an early OpenCode skill-discovery test was caught and re-tested with unique names before trusting the result) found both harnesses read Claude Code's own skill (`name`+`description` SKILL.md) and, for OpenCode, command (`description`+`$ARGUMENTS`) file shapes nearly natively. "Porting" 27/40 skills and 18/24 commands with no Claude-specific primitive therefore means placing the same file at a path each harness already scans, not hand-translating into a different format — the actual reason the original decline (`harness-adapters/README.md`) is correct is a one-time hand port of 64 files, not this.
  Built `plugins/wingman/scripts/generate-harness-adapters.mjs`: a real generator (mirroring this session's own `generate-eval-manifest.mjs` pattern), `--write`/`--check` modes, wired into `.github/workflows/validate.yml` alongside the existing seat-only `check-harness-adapter-drift.mjs` — so the generated surface can never silently drift out of sync with canonical source, closing the actual objection the earlier decline was protecting against. Codex CLI genuinely has no command/prompt-template file primitive (confirmed by direct `codex --help`/`codex plugin --help` inspection, not inferred from docs silence) — its 24 commands generate instead as one `commands-as-agents-md.md` file, sections meant to be pasted into a project's own `AGENTS.md` (confirmed via a real install that pasted content does reach the assembled model prompt). The 13 skills / 6 commands referencing a Claude-specific primitive (`AskUserQuestion`, `ExitPlanMode`, parallel dispatch) get an additive "Harness note" appended per primitive — never a rewrite of the canonical prose, since regex-rewriting nuanced instructions risks silent corruption in a way an appended note doesn't.
  **Real live-install verification, not just generated and assumed correct**: copied the generated output into scratch projects and ran real discovery commands — `opencode debug skill` and `codex debug prompt-input` each independently confirmed all 40 skill names present; `opencode debug config` confirmed all 24 command names with byte-identical template content. A genuinely new finding surfaced along the way, better than what the existing adapters previously claimed: Codex CLI has real, confirmed parallel multi-agent primitives (`spawn_agent`/`followup_task`/`send_message`/`wait_agent`, a 4-concurrent-agent ceiling observed live) — not just "gained capability during 2026" per secondhand research, and disclosed a real, unresolved discrepancy against a previously-cited source claiming 6 concurrent rather than 4, rather than silently picking one number.
  **What still isn't claimed**: live model-inference behavior under either harness — no API key/model provider is configured in this sandbox, so "the file is discovered and its content reaches the assembled prompt" (verified) is distinct from "the model then behaves correctly when actually invoked" (not verified). Logged as a new `docs/HUMAN-TODOS.md` item rather than glossed over. `docs/status/ARCHITECTURE.md` §8b and `harness-adapters/README.md` updated with the full breakdown, exact commands run, and the concurrency-count discrepancy. All 4 Layer-1 validators, the new generator's own `--check`, the existing `check-harness-adapter-drift.mjs`, `check-fixtures.mjs`, and `node --test` (173/173) reverified clean.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Stress-tested the harness-parity work against real precedent on explicit founder request, and found a bigger discovery than a directory-layout question.** Asked why the repo's directory structure itself wasn't restructured toward a generic "agent-agnostic" shape; researched top multi-harness plugin repos rather than defending the prior call unreflectively. Web-search-summarized star counts for some candidates looked inflated and weren't independently verifiable from this sandbox, so verification leaned on repos already vendored here (`vendor/wshobson-agents`, `vendor/ecc`, `vendor/superpowers`), inspected directly on disk.
  Found: `wshobson/agents` structures each plugin as shared `{agents,skills}/` content plus sibling per-harness manifest directories (`.claude-plugin/`, `.codex-plugin/`) where the Codex manifest carries a `"skills": "./skills/"` *pointer* back at the same shared directory — zero content duplication, a genuinely better pattern than this session's copy-based generator. Tested the analogous real mechanism directly against Wingman's own repo: `codex plugin marketplace add /home/user/Wingman` then `codex plugin add wingman@wingman` **worked with zero changes to Wingman's files** — Codex CLI reads Claude Code's own `marketplace.json`/`plugin.json` natively, resolved the correct version (`0.6.0`), cached the plugin, and a follow-up `codex debug prompt-input` confirmed all ~40 skills genuinely discovered from that cache. This is a materially better, zero-maintenance install path for Codex CLI users than the generated `.agents/skills/` copies, and it's now documented as the recommended route (`codex-cli/README.md`'s "2026-07-23 update"). Also found, honestly disclosed rather than oversold: Codex's install step auto-migrates *some* commands into synthetic skill files via an undocumented, inconsistent mechanism (4 of 24 commands did this in testing, no confirmed selection rule) — not relied on; `commands-as-agents-md.md` remains the reliable full-coverage path for commands.
  Checked whether OpenCode has an equivalent native bundle-install mechanism: it does not (`opencode plugin <module>` only installs single npm-module JS plugins, matching how `wingman-gate.js` was ported) — its generated `.opencode/skills/`/`.opencode/commands/` copies remain the only real path, unchanged.
  A related finding — `affaan-m/ECC` and other precedent repos use a flat `skills/<name>/` layout (no category subdirectory), natively `.agents/skills/`-compatible — was evaluated and **deliberately deferred**, not bundled into this pass: even after flattening, OpenCode/Codex CLI's raw-file-discovery paths are project-root-relative, not inside `plugins/wingman/`, so the generated-copy step would still be needed for those regardless of Wingman's internal nesting. The real value would only be for the Codex plugin-cache path specifically, which reads Wingman's own tree directly — a genuine, evidence-backed, but smaller and separately-scoped win, not worth bundling into this change given its blast radius (all 40 `plugin.json` skill paths, every skill's internal cross-references).
  `docs/status/ARCHITECTURE.md` §8b and `harness-adapters/README.md`/`codex-cli/README.md` updated with the full finding. All 4 Layer-1 validators, the harness generator's `--check`, and `node --test` reverified clean.

<!-- wingman:log type=decision category=architecture status=resolved -->
- **Executed the previously-deferred skills flatten (`skills/<category>/<name>/` -> `skills/<name>/`), the one follow-up the harness-parity work named as real but out-of-scope.** All 40 skill directories moved via `git mv` (history preserved), ~90 files' path references updated repo-wide (`plugin.json`, 12 command files, most `references/*.md`, 4 scripts, 39 eval case files, root `AGENTS.md`'s category table rewritten as a conceptual grouping rather than a folder-backed one, `plugins/wingman/AGENTS.md`'s frontmatter convention line, `validate-structure.mjs`'s now-accurate comments). `generate-harness-adapters.mjs`'s `listSkills()` was genuinely broken by the move (hardcoded 2-level nesting, would have silently found zero skills) — fixed to scan flat, matching `listCommands()`'s existing dynamic-scan pattern.
  **A real regression was caught, not just risked**: `node --test` dropped to 145/173 passing immediately after the move — two test files (`tests/hooks-integration/hooks-integration.test.mjs`, `tests/ponytail-integration/ponytail-integration.test.mjs`) independently hardcoded `['category', 'name']` tuples to construct skill paths, entirely outside the original sed's scope (`plugins/wingman/`, `evals/cases/`, root docs — the test suite was never in that list). Fixed both files to drop the category segment; back to 173/173. Also found and removed 7 now-empty per-category `README.md` stubs during the `git mv` pass — boilerplate one-line pointers back to `AGENTS.md`, no unique content, correctly deleted rather than left homeless.
  **Live-install re-verified, not just regenerated and assumed correct**: copied the regenerated `shared/.agents/skills/` into a scratch project — `opencode debug skill` still lists all 40. Re-ran `codex plugin marketplace add`/`codex plugin add` directly against the real, flattened repo — the actual point of this follow-up — and confirmed the resulting plugin cache now genuinely mirrors the flat structure (41 entries: 40 skill dirs, zero category dirs, versus the old 2-level nesting) and still discovers every skill correctly.
  All 4 Layer-1 validators, both generators' `--check`, `check-harness-adapter-drift.mjs`, and `node --test` (173/173) reverified clean after every fix, not just at the end. `docs/status/ARCHITECTURE.md` §8b's prior addendum updated with a "Follow-up executed" note rather than left to imply the deferred item was still open.

<!-- wingman:log type=decision category=harness-breadth category2=hooks status=resolved -->
- **Ported 6 of the shipped plugin's 9 `plugins/wingman/hooks/*.mjs` files to real, live-tested OpenCode plugin equivalents, dispatched as 5 parallel worktree-isolated subagents.** Following the earlier full-100%-coverage request (agents/commands/skills), the founder asked to extend the OpenCode adapter with "all claude like capabilities," specifically the hook layer, then explicitly asked for parallel subagent dispatch in isolated worktrees for efficiency. Each subagent was briefed with the confirmed-real OpenCode plugin hook surface established earlier this session (`tool.execute.before`/`.after`, exact payload shapes) and the confirmed-broken test method to avoid (`opencode debug agent --tool`, which bypasses all plugin hooks) — each required to live-test its own port with a real `opencode run -m opencode/deepseek-v4-flash-free` call (a genuinely free model, zero cost/API key) before reporting done, not just structurally translate.
  Result: `secret-guard.js`, `prompt-guard.js`, `dod-gate.js` (the `Bash`/`git push` half of `dod-structural-gate.mjs` only — its `ExitPlanMode` half is dead for the same reason `wingman-gate.js`'s `plan_exit` match is), `output-scanners.js` (`secret-scanner.mjs`+`content-injection-scanner.mjs`), `session-monitor.js` (`context-monitor.mjs`+`session-health.mjs`), and `pre-compact-guard.js`+`session-start.js` (via `experimental.session.compacting` and `config()`) all confirmed genuinely working live. `stop-loop.mjs` has **no confirmed OpenCode analog** — `session.idle`/`client.session.prompt()` are real, but `opencode run`'s one-shot process was shown (twice, live) to exit before a plugin-triggered follow-up turn completes; only the pure logic was ported, with no speculative wiring, per this project's own evidence-gate discipline. One real, previously-undocumented OpenCode bug was found and fixed along the way: its plugin loader silently fails an entire `.opencode/plugin/*.js` file's load if any named export isn't itself a function (a bare regex-array export broke `secret-guard.js`'s own registration) — fixed via a `getSecretPatterns()` accessor.
  Each subagent's worktree branch was merged back via targeted `git cherry-pick` (not a raw worktree diff merge, since worktrees start from a stale ancestor commit and a full diff would have shown real existing files as deletions) — kept token-light by pulling only the specific commit, not re-reading full file contents already known from briefing the subagents. 90 new `node:test` cases across 6 files, all passing; all 3 repo-wide validators (`validate-structure.mjs`, `check-repo-consistency.mjs`, `check-harness-adapter-drift.mjs`) reverified clean after every merge. `plugins/wingman/references/harness-adapters/opencode/README.md` and `SESSION-LIFECYCLE-FINDINGS.md` (new) document the full breakdown; `plugin.json` bumped to 0.7.7.

<!-- wingman:log type=decision category=harness-breadth category2=gemini-cli status=resolved -->
- **Gemini CLI harness adapter added — Phase 2 of the founder-directed 6-harness build (Phase 1: descriptor-driven generator refactor, 0.7.12).** A dedicated 2026-07-27 schema-verification research pass corrected an earlier, less-precise general pass: Gemini CLI's real hook event names are `BeforeTool`/`AfterTool`/`BeforeAgent`/`AfterAgent`/`BeforeModel`/`AfterModel`/`BeforeToolSelection`/`SessionStart`/`SessionEnd`/`Notification`/`PreCompress` — not `PreToolUse`/`PostToolUse`; subagents are real Markdown + YAML frontmatter under `agents/`, not TOML/JSON. Of the 6 in-scope harnesses, Gemini CLI is the strongest capability match: confirmed real parallel subagent dispatch, a confirmed real structured question tool (`ask_user`), hooks that can genuinely block. Only the plan-gate is a genuine, disclosed weak point (exiting Plan mode auto-escalates to YOLO, no discrete interceptable transition event) — addressed with a coarser per-turn proxy hook (`hooks/plan-gate.mjs`, the one genuinely new piece of logic in this adapter) plus the existing `dod-pre-push-check.mjs` git-push backstop.
  `generate-harness-adapters.mjs` gained a new `commands.mode === 'toml'` formatter for Gemini's `commands/wingman/*.toml` (`$ARGUMENTS` → `{{args}}`, no `name` field since Gemini derives the invocation name from its file path — nesting under `commands/wingman/` reproduces this plugin's own `/wingman:<name>` convention). 8 Boardroom personas hand-translated, `gemini-extension.json`/`GEMINI.md` (real `@file.md` imports) authored against the confirmed schema. `evals/cases/plan-gate-mode-switch-intercept.md` — all 5 shapes run for real against a constructed fixture, not asserted. Full 6-check validator suite + `node --test` (116/116) reverified clean. `plugin.json` bumped to 0.7.13.

<!-- wingman:log type=decision category=harness-breadth category2=openhands status=resolved -->
- **OpenHands harness adapter added — Phase 3 of the founder-directed 6-harness build, deliberately narrower in scope than Gemini CLI's (Phase 2).** OpenHands' capability findings come from the 2026-07-27 general capability-matrix pass, not a dedicated field-level schema-verification pass — so this phase does not invent a Boardroom-persona file format or an exact hooks-config schema, both honestly logged as open gaps (`openhands/README.md`, `docs/HUMAN-TODOS.md`) rather than guessed. What did ship: `harness-targets/openhands.mjs` reusing the existing `'folded'` commands mode (all 24 commands folded into `.openhands/microagents/repo.md`, OpenHands' own confirmed-real, automatically-loaded repository-instructions file — the same shape Codex CLI's `AGENTS.md` fallback already uses) and a shared-skills contribution; confirmed real parallel delegation via `DelegateTool`; no plan-gate (blanket confirmation-mode toggle only — falls back to the existing `dod-pre-push-check.mjs` git-push gate, same as Codex CLI). Full 6-check validator suite + `node --test` (116/116) reverified clean. `plugin.json` bumped to 0.7.14.

<!-- wingman:log type=decision category=harness-breadth category2=capability-profile status=resolved -->
- **Capability-profile branching wired into canonical command/skill files, closing #153/#156 of the founder-directed 6-harness build in one pass.** `generate-harness-adapters.mjs` gained a `buildCapabilityProfile()` that emits `plugins/wingman/references/harness-capability-profile.md` directly from every harness's own `capabilities` descriptor block (Claude Code's row synthesized as the full-parity baseline) — a standalone generated file, checked for drift the same way the per-harness adapter outputs are. `boardroom.md` (dispatch mode), `evolve.md`/`evolve-promotion`/`dogfood-gap-classification` (question-tool fallback), and `implementation-planning.md` (plan-gate fallback) each gained one branch reading that table. Because these are canonical, generator-copied files, regenerating (`--write`) propagated the new branching text into every harness's own copy automatically (confirmed directly in OpenCode's, Gemini CLI's, and OpenHands' generated output) — the same edit that closed #156 also closed #153 (OpenCode's disclosed sequential-dispatch degradation becoming a generated instruction, not just README prose) with zero OpenCode-specific work. Full 6-check validator suite + `node --test` (116/116) reverified clean; `plugin.json` bumped to 0.7.15.

<!-- wingman:log type=decision category=harness-breadth category2=cline status=resolved -->
- **Cline harness adapter added — Phase 4 of the founder-directed 6-harness build.** Like OpenHands, capability findings come from the general capability-matrix pass, not a dedicated schema-verification pass — no hooks-config wiring file or Boardroom-persona adapter shipped (Cline has no subagent concept at all, confirmed by its own `hasParallelDispatch: false`). What shipped: `harness-targets/cline.mjs` reusing the existing `'perFile'` commands mode for Cline's real, documented `.clinerules/workflows/*.md` slash-command convention, plus a shared-skills contribution offered for manual copying only (Cline's `.clinerules/*.md` files are always-active context, not on-demand skill invocation — bulk-copying 40 skills would burn context every turn for no benefit). Real `ask_followup_question` question tool confirmed; no parallel dispatch or plan-gate, both falling back to already-established mechanisms (`dod-pre-push-check.mjs`, sequential-dispatch disclosure). Full 6-check validator suite + `node --test` (116/116) reverified clean. `plugin.json` bumped to 0.7.17. Cursor remains the one harness left in this build, sequenced last per the plan (weakest capability match).

<!-- wingman:log type=decision category=harness-breadth category2=cursor status=resolved -->
- **Cursor harness adapter added — Phase 5 and the last phase of the founder-directed 6-harness build.** Sequenced last on purpose (weakest capability match: only 3 narrow hook lifecycle points, UI-click-only Plan Mode exit with no interceptable event, no question tool). `harness-targets/cursor.mjs` reuses the existing `'perFile'` commands mode for Cursor's real `.cursor/commands/*.md` slash-command convention, plus a shared-skills contribution — Cursor's real `.cursor/rules/*.mdc` mechanism has a genuinely different frontmatter shape than `SKILL.md`, so a full skill translation is explicitly left open rather than half-done. Confirmed real parallel dispatch (up to 8 worktree-isolated background agents). No hooks-config wiring file or Boardroom-persona adapter — both honestly logged as open gaps needing a real account/install to verify. Full 6-check validator suite + `node --test` (116/116) reverified clean. `plugin.json` bumped to 0.7.18.
  **All 6 harnesses of the founder-directed override now have an adapter** (Codex CLI/OpenCode pre-existing, Gemini CLI/OpenHands/Cline/Cursor added this build) — `docs/status/ARCHITECTURE.md` §8f, `harness-adapters/README.md`, and `plugins/wingman/AGENTS.md` all updated to reflect the completed build. Remaining open items, tracked here rather than silently dropped: hooks-config schemas + Boardroom-persona formats for OpenHands/Cline/Cursor (each needs a real account/install), the Cursor `.mdc` skill translation, and live model-inference verification for the 4 newest harnesses.

<!-- wingman:log type=decision category=harness-breadth category2=completeness status=resolved -->
- **Completeness follow-up to the 6-harness build, closing 2 real gaps for OpenCode/Codex CLI/Cursor/OpenHands/Gemini CLI specifically (explicit founder ask for full coverage across these 5).** OpenCode and Codex CLI were already near-complete from prior sessions; OpenHands' hooks-config schema and persona format remain genuinely unconfirmed (no live account exists — not fabricated to hit a "100%" number). What actually closed: a real `.cursor/rules/*.mdc` skill translation (40 files, new `skills.mdcOutDir`/`mdcFrontmatter` generator mechanism, `alwaysApply: false`/empty `globs` matching Cursor's own "Agent Requested" mode) and a real `.cursor/hooks.json` + `secret-guard.mjs` wiring the 2 blockable Cursor lifecycle events; a second Gemini CLI hook (`prompt-guard.mjs`, wired to `BeforeModel`). New `evals/cases/harness-completeness-pass-2026-07-27.md`, all shapes run for real. A real regression was also found and fixed in the same pass: a stray `<<<<<<< HEAD` conflict-marker line, left in `CHANGELOG.md` by an earlier merge-conflict resolution in PR #122, had already reached `main` undetected — caught by direct inspection while working this pass, not by any automated check (worth noting as a real gap in the validator suite's own coverage, since none of the 6 checks scan for literal conflict markers). Full 6-check validator suite + `node --test` (287/287) reverified clean. `plugin.json` bumped to 0.7.19.

<!-- wingman:log type=decision category=tooling category2=maintainability status=resolved -->
- **Consolidated the 6-check dev validator suite into one command and added an opt-in local pre-commit hook, closing a real recurring maintenance gap (asked directly: "look for extra steps that are universal and depended on you rather than in the plugin").** Reviewed this project's own accumulated evidence for exactly this pattern — `docs/status/AGENT-WEAKNESS-BENCHMARK.md`'s coverage benchmark, `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table, `wingman-health.mjs` (100% eval coverage, 0 provisional, 155/155 marker coverage) — all already mature and evidence-gated; nothing there called for new plugin content. The concrete, real gap found instead: running all 6 CI-equivalent validators required remembering and typing 6 separate commands every time, with no local mechanism forcing it before a commit — the exact process gap that let a stray `<<<<<<< HEAD` conflict-marker line reach `main` via PR #122 undetected. New `scripts/validate-all.mjs` (all 6 checks in one command, `--fast` skips the slowest for a quick pre-commit gate) and `scripts/install-dev-git-hooks.mjs` (opt-in, idempotent, mirrors `install-git-hooks.mjs`'s existing safety conventions — never overwrites a pre-existing hook it didn't write, verified against a real pre-existing-hook scenario) — both dev-repo-only (root `scripts/`, never ships with the plugin). Also fixed a stale "73 generated files" claim in `docs/status/REGRESSION-CHECKLIST.md` (a present-tense doc, not a dated log entry, so the stale count was a real bug, not a historical record) found while auditing for this same class of drift. Full 6-check validator suite + `node --test` (287/287) reverified clean; both new scripts smoke-tested against real failure/uninstall/pre-existing-hook scenarios, not just the happy path.

<!-- wingman:log type=decision category=tooling category2=testing status=resolved -->
- **Wired the `node --test` suite into CI and into `validate-all.mjs` — it had existed for months while no workflow ran it.** Found while auditing a founder-supplied "enterprise blueprint" against what this repo actually has. A grep across all 8 workflows in `.github/workflows/` returned zero matches for `node --test`: the 345-test suite under `tests/` (16 files, 4 directories, covering the canonical hooks, the OpenCode/Codex adapter ports, the git-hook installer, and the ponytail pattern) was enforced *only* when a maintainer remembered to run it by hand. Same failure class as the PR #122 conflict-marker incident, and exactly the "extra step that depends on the agent rather than the plugin" pattern the previous entry set out to eliminate — that pass consolidated 6 checks but did not notice the 7th was missing entirely.
  Two real implementation findings, both verified rather than assumed. `node --test tests/` **fails** on Node 22 (`MODULE_NOT_FOUND` — it resolves the argument as a module, not a directory to scan), and Node 20 (what CI pins) and Node 22 disagree on glob/directory argument handling generally; the portable form is to let the *shell* expand `tests/*/*.test.mjs` into explicit paths in CI, and to enumerate the files with `readdirSync` in `validate-all.mjs` (which uses `spawnSync` without a shell, so no expansion happens for free). Scoped to `tests/` deliberately — a bare `node --test` also recurses into `vendor/`'s pinned upstream test files, which are not this repo's concern.
  Kept in `--fast` (~2.5s), so the opt-in pre-commit hook still catches a broken test before the commit exists. Verified by construction, not assertion: a deliberately-failing test dropped into `tests/` made both `validate-all.mjs` and the CI shell-glob form exit 1, and was then removed. `AGENTS.md`'s "no unit test runner in the conventional sense" claim was corrected in the same pass — it was accurate about the absence of a `package.json`/npm task runner, but wrong that no test suite existed.

<!-- wingman:log type=decision category=security category2=testing status=resolved -->
- **Built a real red-team suite against the live prompt-guard/secret-guard/dod-structural-gate hooks (and their Codex CLI/OpenCode ports), found 4 genuine bypasses, fixed 3 and documented 1 as an accepted residual risk.** Second of 6 planned PRs harvesting genuine gaps from the founder-supplied blueprint review; `prompt-guard.mjs`'s own header already conceded its regex list was "a floor, not a ceiling," but nothing had ever actually attacked it to find where the floor was.
  Real findings, each verified against the actual function before being written down (not assumed): (1) the single most common textbook phrase, "ignore all previous instructions," was itself NOT caught — the regex only allowed one qualifier word before instructions/prompt/system, missing the common two-word "all previous" combination; fixed to allow 1-2 words, matching the sibling disregard/forget pattern that already did. (2) a zero-width space or newline inserted mid-phrase silently broke the `\s+`-based patterns; fixed with a `normalize()` pass (strip invisible characters, collapse whitespace) before matching — confirmed this introduces no new false positive against a set of benign prompts using the word "ignore" naturally. (3) `git push origin main --force` — the single most common real invocation shape, with positional args before the flag — bypassed the destructive-command guard entirely; only a bare `git push --force` was ever caught. (4) two real `checkBoardroomVerdictClean()` bypasses: doubled internal whitespace in `bottom_line` and a seat verdict with trailing annotation text both defeated exact-string comparison; fixed with whitespace normalization and word-boundary-anchored prefix matching, verified this doesn't introduce a false positive on `GO_WITH_CONCERNS` or a hypothetical `NO_GO`-prefixed-but-unrelated token.
  Encoding/obfuscation evasion (base64, rot13, leetspeak, reversed text, Unicode homoglyphs) is NOT fixed and not expected to be fixable by pattern-matching alone — asserted in the new test suite as documented current behavior, not silently left unstated, so a future accidental fix is visible rather than the gap being silently re-discovered later.
  **A genuine structural bug was also found and fixed in the Codex CLI `secret-guard.mjs` port**: its `main()` was called unconditionally at module load, unlike every sibling file in the same directory — importing `decide` from it (e.g. a test, a future generator) hung forever on a synchronous stdin read with nothing piped in. This file had zero prior test coverage. Fixed to match the established entry-point-guard convention.
  New `tests/red-team/` (dev-repo-only, picked up automatically by both the CI test step and `validate-all.mjs`'s `readdirSync`-based enumeration from the prior PR): 25 tests across 4 files, attacking the real exported pure functions (never a reimplementation) — including the same corpus re-run against the hand-copied Codex CLI/OpenCode ports, since `check-harness-adapter-drift.mjs` only checks Boardroom-persona/skill drift and would never notice a port's hooks silently weakening during translation. Full 7-check validator suite + `node --test` (370/370) reverified clean.

<!-- wingman:log type=decision category=security category2=governance status=resolved -->
- **Adopted a 5-tier permission model + deploy-approval gate from the founder-supplied blueprint review; descoped its audit-log fields as a bad fit, not silently.** Third of 6 planned PRs harvesting genuine gaps from that review (first: CI test enforcement, #125; second: the red-team suite, #126).
  Built: `plugins/wingman/references/permission-model.md` (Read only / Draft and propose / Scoped write / Conditional action / Break-glass — the shipped, authoritative version, since a root `docs/`-or-root file is invisible to agents in a founder's install) and root `POLICIES.md` (the human-facing restatement, per the founder's explicit choice to land it in both places). A new 7th check in `check-repo-consistency.mjs` asserts the two files' tier names stay in sync — proven both ways: a deliberately-introduced mismatch failed the check, then passed clean once reverted.
  `plugins/wingman/hooks/deploy-approval-gate.mjs` enforces the model's Level 3/4 boundary — the one that actually matters most — for real. It is deliberately **not** identity-based: a direct check of every Wingman hook's payload fields (and a second, independent plugin's, for cross-confirmation) during the prior red-team pass already established that no Claude Code hook payload exposes which agent is acting, so an identity-based gate was never buildable. Instead it blocks deploy-class Bash commands (`kubectl apply`, `terraform apply`, `npm`/`pnpm`/`yarn publish`, a force-push to a protected ref) unless the most recent Build-stage checkpoint in `.wingman/checkpoints.jsonl` carries a clean Boardroom `GO`, reusing `dod-structural-gate.mjs`'s own `checkBoardroomVerdictClean()` rather than re-deriving the same logic twice. Fails open on an absent/unparseable checkpoint log, so ordinary non-Wingman projects are never blocked. New `tests/hooks-integration/deploy-approval-gate.test.mjs` (7 tests, including the no-checkpoint-yet allow case and the protected-ref-vs-feature-branch force-push distinction).
  Two red lines added to `references/security-checklist.md` ("never disable a security control," "never circumvent a repository protection") — genuinely new statements, not restatements of something already there.
  **Explicitly not built, and why this is the right call, not a shortcut**: the blueprint's audit-log fields (`commands run`, `model/agent used`) were descoped mid-implementation once it became clear "commands run" doesn't map onto what a Boardroom checkpoint actually is — a rendered verdict, not a command trace. Bumping `schema_version` 5→6 to fit a field that doesn't belong would have been exactly the speculative-addition pattern this project's own evidence-gate discipline exists to catch. Logged in `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table rather than silently dropped.
  `docs/status/ARCHITECTURE.md` §4's Agent Permission Model section updated to point at the new reference and record what's now closed vs. what remains an accepted limit (Level 0-2 agents still rely solely on their `tools:` allowlist). Full 7-check validator suite + `node --test` reverified clean. `plugin.json` bumped to 0.7.21.

<!-- wingman:log type=decision category=security category2=tooling status=resolved -->
- **Added a dependency-audit check to the DoD gate + two thin on-demand commands (`/wingman:review`, `/wingman:test`).** Fourth of 6 planned PRs harvesting genuine gaps from the founder-supplied blueprint review (prior three: CI test enforcement #125, red-team suite #126, permission model + deploy-approval gate #127).
  `dod-structural-gate.mjs` gained a 5th check on the `git push` branch: `detectDependencyAuditCommand()` picks the project's real package ecosystem generically (npm/pnpm/yarn distinguished by lockfile, pip-audit, cargo-audit — mirroring `detectTestCommand()`'s own manifest-driven convention exactly), and `parseAuditReport()` is a pure function deciding block/allow from an auditor's raw JSON/NDJSON output, tested against real recorded shapes from all 5 ecosystems (npm's current `metadata.vulnerabilities` shape and its older `advisories` map shape, yarn's NDJSON `auditSummary` line, cargo-audit's advisory list, pip-audit's per-package vuln array) with zero network access needed. Documented, deliberate deviation for `pip-audit`: unlike npm/cargo, it doesn't reliably expose a severity rating, so any finding blocks rather than filtering to HIGH/CRITICAL only — stated plainly in the code, not silently inconsistent with the other four. `runDependencyAudit()` is the thin shell-out wrapper; skips (never false-blocks) on a missing binary, no network, or unrecognized output — the same fail-open discipline `runTestSuite()` already established. This makes `security-checklist.md`'s OWASP A06 checklist row ("dependencies audited; no known CVEs at HIGH/CRITICAL") mechanically true instead of aspirational. New `tests/hooks-integration/dependency-audit.test.mjs` (16 tests, including a real integration test against `npm audit` that skips cleanly rather than requiring network).
  `/wingman:review` and `/wingman:test` are thin command wrappers — the former over the existing `code-review` skill (already renders a verdict without writing code), the latter over `testing-patterns` plus the exact same `detectTestCommand()`/`runTestSuite()` logic the DoD gate uses, so a founder can ask "do the tests pass right now?" without a git push or a full Boardroom checkpoint. Both registered in `plugin.json` (32→34 commands) and mirrored to all 6 harness adapters via `generate-harness-adapters.mjs --write` (218 files regenerated).
  Two stale hardcoded counts caught by the full validator suite before this landed, not after: `tests/hooks-integration/hooks-integration.test.mjs`'s "should have 32 commands" assertion, and a missing `CHANGELOG.md` entry for the version bump — both real findings from actually running the suite, not assumed clean. Full 7-check validator suite + `node --test` (393/393) reverified clean after both fixes. `plugin.json` bumped to 0.7.22.

<!-- wingman:log type=decision category=governance category2=documentation status=resolved -->
- **Adopted the 6-field skill contract across all 40 skills.** Fifth of 6 planned PRs harvesting genuine gaps from the founder-supplied blueprint review (prior four: CI test enforcement #125, red-team suite #126, permission model + deploy-approval gate #127, dependency audit + `/wingman:review`/`/wingman:test` #128).
  Extended `validate-structure.mjs`'s skill-anatomy check from the 3-concept triad (Rationalizations/Red Flags/Verification) to the full 6-field contract (Trigger/Inputs/Procedure/Output schema/Stop condition/Escalation). Trigger and Procedure were already mechanically covered (frontmatter `description`'s "Use when..." clause; the house "Core Workflow" heading), so only 3 new checks (Inputs, Output, Escalation) were added. Baseline: 104 warnings across 40 skills. Closed in 5 hand-authored waves of 8 skills each (discipline, mechanics, governance, output+knowledge, personas+response) — each skill's new fields grounded in its own real content, never templated, since a templated pass would have produced 40 skills all saying "Inputs: the task at hand." `validate-structure.mjs` now reports zero anatomy warnings repo-wide.
  Each wave: edited the skills, ran `validate-structure.mjs` to confirm the warning count dropped as expected, `generate-harness-adapters.mjs --write`, manually synced the OpenCode ported-skills copies (a separate, hand-tracked path `generate-harness-adapters.mjs` doesn't touch), then the full 7-check `validate-all.mjs` before committing. `plugin.json` bumped to 0.7.23.

<!-- wingman:log type=decision category=governance status=resolved -->
- **Declined a pasted "Wingman phase map" proposal (generic 7-phase SDLC model + per-skill `## Handoff` prose block); logged as deferred rather than built.** See `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table for the full reasoning. In short: the proposed 7 phases (Ideation/Design/Coding/Testing/CI-CD/Monitoring/Maintenance) are coarser than the real 14-stage pipeline already shipped, itself a founder-approved expansion from an earlier 7-stage model (`ARCHITECTURE.md` §4d) — this proposal would have collapsed exactly the granularity that expansion bought. The `## Handoff` block duplicates `.wingman/checkpoints.jsonl`, which already carries a schema-versioned, machine-readable handoff record (`next_stage`, `bottom_line`, `scope_ref`, per-seat verdicts). No code, skill, or doc-structure change made.

<!-- wingman:log type=decision category=governance category2=architecture status=resolved -->
- **Built `skills/change-triage` — a founder-directed, explicitly-recorded partial reversal of the 14-stage checkpoint-bundling decision.** Sixth and last of the 6 planned PRs harvesting genuine gaps from the founder-supplied blueprint review (prior five: CI test enforcement #125, red-team suite #126, permission model + deploy-approval gate #127, dependency audit + `/wingman:review`/`/wingman:test` #128, 6-field skill contract #130).
  New `plugins/wingman/skills/change-triage/SKILL.md`, invoked at intake by `discovery.md` (Discovery.0, new), `build.md`'s "Before starting" step (only when no prior approved plan exists), and `hotfix.md`'s Step 1. **One risk taxonomy, two consumers**: triage reuses the 5-tier permission model's own levels from PR #127 (`references/permission-model.md`) rather than inventing a second, competing risk scale. Level 0-1 routes to `/wingman:hotfix`'s lightweight loop instead of the full 14-stage pipeline; Level 2 proceeds normally; Level 3 proceeds but flags every downstream Boardroom checkpoint as mandatory full review; Level 4 routes to `/wingman:incident` immediately.
  A **skill, not a 15th pipeline stage** — it produces a routing decision, not an artifact with its own checkpoint or traceability ID, so the 14 stage names encoded in `checkpoints.jsonl`'s schema are untouched.
  **Safe by construction, not a strictness dial**: triage may route work *up* but never down. Anything touching a trust boundary, secrets, auth, payments, data, or deploy is Level 3+ by definition regardless of how the request is framed — this is what keeps a routing step from becoming a gate-weakening dial, the failure mode a prior "configurable Boardroom strictness" proposal was declined for (see `docs/roadmap/AGENT-ROSTER.md`'s deferred-ideas table).
  **Recorded as a deliberate override, not evidence clearing the gate**: `docs/status/ARCHITECTURE.md` §4d documents the 12 individual pre-build checkpoints as a founder-approved reversal of earlier bundling, "trading ceremony for full enterprise UX process completeness." This partially re-opens that tradeoff from the other direction, on the founder's own explicit direction, named plainly in §4d rather than silently overwritten.
  Registered in `plugin.json` (40 → 41 skills); two stale hardcoded skill counts caught and fixed (`tests/hooks-integration/hooks-integration.test.mjs`'s "should have 40 skills" assertion, `tests/opencode-gate/opencode-install.test.mjs`'s matching count); `AGENTS.md`'s governance skill-category row and harness-adapter skill-count mention updated. Full 7-check validator suite + `node --test` reverified clean. `plugin.json` bumped to 0.7.24.

<!-- wingman:log type=decision category=dogfooding-mechanism status=resolved occurrence=7 -->
- **2026-07-28 — real, user-requested dogfood pass (scoped to Discovery, disclosed as partial) found and fixed 2 real gaps.** (1) `department-lead-activation/SKILL.md`'s Step 5 claimed a freshly-created `.claude/agents/dept-<name>.md` is dispatchable "this turn... no plugin reload is needed" — real dispatch failed outright (`Agent type 'dept-product' not found`), reproduced twice before treating it as real. Classified via `dogfood-gap-classification` (approved via `AskUserQuestion`) as a skill-candidate, not a hook — nothing mechanical to enforce, just a false assumption needing a judgment-based fallback. Fixed with a try-then-fallback branch: attempt dispatch, and if it fails, do the delegated work directly this turn using the just-written file's own remit, telling the founder plainly the department lead will run as its own subagent starting next session. Whether the root cause is specific to the sandboxed harness this run happened in, or a genuine same-turn limitation in Claude Code CLI itself, is disclosed as unconfirmed either way — the fix doesn't depend on resolving it. See `evals/cases/department-lead-activation.md` Run 4 (`provisional`) and `docs/history/retros.md`'s matching entry. (2) PR #137's docs restructure left `plugins/wingman/` untouched on the reasoning that nothing there does a *functional* read of the moved paths — true, but missed that ~63 shipped files cite those paths in prose (a recurring `<!-- See docs/ARCHITECTURE.md for this command's place... -->` footer, plus inline citations). Fixed all 63 canonical files, regenerated 220 harness-adapter files via `generate-harness-adapters.mjs --write`, and manually re-copied 17 OpenCode-ported skill files (`portedSkillsDir` has no auto-regeneration path — confirmed by running `check-harness-adapter-drift.mjs`, which correctly caught all 16 skills that had drifted, proving the drift-check itself works even though the original restructure's own risk-map only modeled functional path reads, not prose citations). `plugin.json` bumped to 0.7.28; full 7-check validator suite + `node --test` reverified clean both before and after.


See `docs/roadmap/ROADMAP.md` for the phased roadmap (moved there 2026-07-28 as part of the
docs/status | docs/roadmap | docs/history split).
